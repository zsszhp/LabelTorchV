# 项目规划与功能完善

> **Workspace:** e:\z\project\my\LabelTorchV

---

**User**

/plan 1.我这个项目，是否像素级复刻E:\z\project\my\LabelTorchV\reference\Dihuge DLTools里面的这些描述，E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\gemini-code-1780391652706.html是最终敲定的，以这个为准，其他的做补充；要用户友好型，界面好看，操作方便符合逻辑 
 2.是否实现MVP全链路功能：导入数据集，设置参数（划分数据集、超参数等）训练yolov8/异常检测模型，导出模型的基本功能 
 3.E:\z\project\my\LabelTorchV\docs文档里面提到的功能是否都已经实现；若已经实现，我UI界面上的有一些按钮、功能，可能只是一个UI，并没有实际的功能，你要补充完善 
 4.必须确保软件无bug、并且好用，用户友好，并且达到业界前列的水平 
 5.E:\z\project\my\LabelTorchV\reference里面的项目可以多参考，或者你再在网络上（百度、谷歌、github等）多查找，看一下是否还有值得参考的项目、功能，不要重复造轮子，很多bug的，我希望看到一个功能完善的，很好用的、兼容性强的标注平台

**Assistant**

我将首先探索代码库以了解当前状态，然后制定一份全面的计划。让我启动并行搜索代理来调查不同的方面。

我将系统地探索 LabelTorchV 项目的实现状态。先并行执行多个搜索来快速了解项目结构。

让我并行读取关键的 Service 文件、Schema 文件和 Python 后端文件。

继续并行读取 AnnotationService、Schema.cpp、Python 后端关键文件。

# LabelTorchV 项目文档全面分析报告

我已读取 `E:\z\project\my\LabelTorchV\docs` 目录下全部 10 份文档（7 份顶层文档 + 3 份 superpowers 子目录的 QML 修复记录）。以下是详细中文总结报告。

---

## 一、文档清单与定位

| 文档路径 | 角色 |
|---------|------|
| `docs/原始需求.md` | 用户原始痛点与功能设想（最早期输入） |
| `docs/blueprint.md` v5.0 | 产品蓝图与版本路线图 |
| `docs/architecture.md` v5.0 | 技术架构文档 |
| `docs/执行总计划-LabelTorchV-MVP+.md` v1.0 | **当前阶段总控文档**（最高优先级） |
| `docs/里程碑验收与执行清单.md` v1.0 | M0~M9 里程碑拆解 |
| `docs/UI像素级复刻专项规范.md` v1.0 | UI 复刻标准与页面级要求 |
| `docs/preflight_checklist.md` v3.0 | 开发前自检清单 |
| `docs/superpowers/plans/2026-06-08-qml-bug-fix.md` | QML 修复实施计划 |
| `docs/superpowers/specs/2026-06-08-qml-bug-fix-design.md` | QML 修复设计文档 |
| `docs/superpowers/plans/2026-06-08-qml-bug-fix-walkthrough.md` | QML 修复变更总结 |

---

## 二、所有承诺的功能点（按模块分类）

### 2.1 项目管理模块
- 项目创建
- 项目打开
- 最近项目访问
- 项目级默认配置
- 项目路径初始化
- 项目元信息管理（名称、根路径、任务类型、默认设备、默认模型家族）

### 2.2 数据集模块
- 图片扫描
- 标签配对
- 异常记录
- 类别信息读取
- 样本数量统计
- 入库
- 数据集导入（YOLO/COCO/Anomaly 格式）
- 数据集扫描与校验
- 样本浏览
- 样本属性
- Tag 管理
- 数据异常提示
- 基础样本统计
- 导入向导
- 类别映射向导

### 2.3 标注模块（当前阶段仅做 UI 外壳与结构预留）
- HBB 标注（v1.0+）
- OBB 标注（v1.0+）
- 多边形标注（v1.0+）
- 撤销重做与原子写回（v1.0+）
- 辅助标注（v1.1+）
- 批量审核（v1.1+）

### 2.4 检查模块
- 问题样本浏览
- 筛查视图
- 质检流程 UI 承载

### 2.5 训练模块（当前阶段主战场）
- 数据集快照选择
- 网络结构选择
- 训练参数配置（img_size、batch、epochs、早停等）
- 数据增强配置
- 训练状态展示
- 实时指标展示
- 数据划分
- 快照创建（不可变）
- data manifest 生成
- data yaml/训练配置生成
- 训练配置组装
- `train.start` 调用链
- 状态流转（running/stopped/failed/succeeded）
- 日志回流
- 指标回流
- 停止训练
- 进程中断修复
- 冷启动残留状态修复
- 训练模板与配置复用
- 增量训练入口（v1.1+）

### 2.6 测试模块
- 测试配置
- 统计卡区
- PR 曲线区
- 测试详情区
- 混淆矩阵与检查图像双视图
- 批量修改入口
- 测试页结果视图与图表壳

### 2.7 模型管理模块
- 模型版本注册
- 血缘追踪（model_version → training_run → dataset_snapshot）
- 标签管理（baseline/best/production）
- 指标查询与对比
- 模型版本谱系增强（P2 预留）

### 2.8 导出模块
- pt 导出
- onnx 导出
- `artifact.verify` 验证
- 导出结果落库
- 导出失败状态处理
- 导出参数追踪（格式、路径、目标平台、动态 shape、opset、simplify）
- 导出报告
- TensorRT/TFLite/Engine 完整导出闭环（当前阶段不做，仅预留）

### 2.9 推理与异常检测模块
- 异常检测训练
- 异常检测推理
- YOLO 模型推理（单张/批量）
- 辅助标注审核
- 主动学习（低置信/误检/漏检/难例队列）

### 2.10 硬件兼容模块
- GPU 探测
- CUDA 检测
- 设备列表输出
- CPU 降级
- 显存不足提示
- 设备选择策略
- 支持 NVIDIA 30/40/50 系列
- 支持 CPU-only 训练

### 2.11 IPC 与后端模块
- `environment.check` 环境检测
- `train.start/stop/status/list_adapters/data_split`
- `inference.run`
- `export.run`
- `artifact.verify`
- `anomaly.infer`
- `active_learning.collect_low_conf/prioritize_queue/queue_stats`
- 事件流：`task.started/progress/log/warning/failed/succeeded/stopped`

### 2.12 UI 基础设施
- 主题令牌系统
- 统一图标系统（20+ 图标）
- 通用按钮、输入框、下拉框、卡片、分组标题、状态标签、弹窗、分割器
- 顶栏导航、顶部过滤栏、主内容容器、左右侧栏骨架、底部状态条/日志区
- 7 个主页面（项目/数据集/标注/检查/训练/测试/导出）
- 7 类弹窗（新建项目/打开项目/导入数据集/新建训练模型/Tag 编辑/类别配置/批量修改）
- 右键菜单
- 全局日志与任务中心

---

## 三、MVP 边界定义

### 3.1 当前阶段（MVP+）必须完成（P0）
1. 项目创建与打开
2. YOLO 检测数据导入
3. 异常检测数据导入
4. 数据校验与问题提示
5. 数据划分与快照
6. **YOLOv8 detect 训练**（锁定）
7. **异常检测训练**（锁定）
8. pt 导出
9. onnx 导出
10. onnx 验证
11. 训练状态与日志可视化
12. 设备探测与 CPU 降级
13. UI 达到高保真空壳复刻标准

### 3.2 当前阶段建议完成（P1）
1. 导入向导
2. 类别映射向导
3. 训练模板与配置复用
4. 导出高级参数面板
5. 测试页结果视图与图表壳
6. 全局日志与任务中心

### 3.3 当前阶段允许预留但不作为主验收项（P2 / 明确不做）
1. **匹配页面**（当前阶段不做）
2. OBB 完整训练闭环
3. 分类完整训练闭环
4. 完整标注引擎交付
5. 辅助标注正式版
6. 增量训练完整产品化
7. TensorRT、TFLite、Engine 等完整导出闭环
8. Ubuntu 交付级兼容

说明：以上能力可在结构上预留（入口、壳子、状态位、数据结构），但当前阶段不以这些能力的完整可用为验收门槛。

### 3.4 已锁定的用户决策（不得擅自改写）
1. 当前阶段走 MVP+
2. 异常检测属于当前阶段范围，保留并继续完善
3. 匹配页面当前阶段不做
4. UI 要完整做完，但分步推进（先视觉→再弹窗交互→再数据流转）
5. UI 复刻标准为 B 级（高度贴近参考稿）
6. 真实训练支持锁定 YOLOv8 detect + anomaly
7. 导出链路锁定 pt + onnx
8. 数据集流程按完整流程建设，不做半成品
9. 执行管理采用按里程碑验收

---

## 四、架构设计与功能规范

### 4.1 总体架构
```
Qt 主进程（C++ / QML）
  ├─ UI 渲染层
  ├─ Service 层
  ├─ SQLite 数据层
  └─ IpcClient（QProcess）
        ↓ stdin/stdout JSON-RPC
Python 后端进程
  ├─ IpcServer
  ├─ Handlers
  ├─ Training Adapters
  └─ Export / Verify / Inference / Anomaly
```

**架构目标**：把桌面 UI 稳定性与深度学习运行时隔离开，确保后端训练或导出异常时，前端仍然保持响应。

### 4.2 分层职责

| 层级 | 职责 |
|------|------|
| Qt 主进程 | 应用启动、QML 装载、本地数据库、文件系统、托管 Python 子进程、后端事件同步到 UI |
| QML 界面层 | 视觉表现、页面状态展示、用户交互收集、调用 C++ Service 接口 |
| C++ Service 层 | 项目/数据集/快照/训练/模型/导出管理，与数据库和 Python 后端通讯（业务协调层，不承载深度学习计算） |
| Python 后端 | 环境检查、数据划分、训练启动/停止/状态查询、模型导出、导出验证、异常检测训练或推理 |

### 4.3 IPC 契约
- 通讯方式：stdin/stdout JSON-RPC
- 核心命令：`environment.check`、`train.start/stop/status/list_adapters/data_split`、`export.run`、`artifact.verify`、`anomaly.infer`
- 核心事件：`task.started/progress/log/warning/failed/succeeded/stopped`
- 当前阶段要求：
  1. 所有训练任务都必须可追踪状态
  2. 所有失败都必须返回明确错误
  3. 所有导出结果都必须可验证
  4. 所有长任务都必须通过事件流回传进度

### 4.4 数据与状态边界（硬约束）
1. **数据库是真实状态源**：项目、数据集、快照、训练任务、模型版本、导出记录等元数据以 SQLite 为单一真实状态源
2. **训练必须基于快照**：训练不得直接依赖实时数据集目录，必须通过快照或等价的不可变训练清单启动
3. **模型版本必须可追溯**：链路为 `model_version -> training_run -> dataset_snapshot`
4. **导出产物必须验证**：pt 与 onnx 导出后，必须记录导出参数、输出路径和验证结果

### 4.5 核心模块关系
- 项目与数据集：`ProjectService` 管理项目元信息和默认配置；`DatasetService` 管理导入、扫描和样本记录
- 快照与训练：`SnapshotService` 生成训练输入快照；`TrainingService` 管理训练生命周期；`TrainingModel` 承载任务列表或状态视图数据
- 模型与导出：`ModelRegistry` 管理模型版本；`ExportService` 负责导出流程管理
- UI 与服务连接：QML 页面通过上下文注入连接各服务与模型，**禁止把复杂业务逻辑直接堆在 QML 中**

### 4.6 训练适配器体系
- 抽象基类 `TrainingAdapter`（7 个抽象方法：validate_config、prepare_dataset_snapshot、start_training、stop_training、parse_logs、collect_metrics、export_model）
- `UltralyticsAdapter`：YOLO 系列（yolov5/yolov8/yolov8_obb/yolov8_cls/yolov10/yolov11）
- `AnomalibAdapter`：异常检测（efficient_ad/patchcore/padim/stfpm/cflow/dfkde/dfm/ganomaly/fastflow/reverse_distillation/csflow/devnet）
- 注册机制：`TrainingAdapterRegistry.register(name, class)` 插件式注册
- Anomalib 为可选依赖，未安装时跳过注册

---

## 五、UI/UX 设计要求

### 5.1 最高参考源
- **`reference/Dihuge DLTools/gemini-code-1780391652706.html`**（最高权威）
- 补充参考：`reference/Dihuge DLTools/UI.pdf`、`reference/Dihuge DLTools/UI设计.md`

### 5.2 视觉总基调
1. 深色工业风
2. 冷调蓝灰主基底
3. 高亮青蓝色主强调
4. 细描边、轻阴影、弱发光
5. 信息密度偏高，但结构清晰
6. 桌面工具型，不走消费级卡通风

### 5.3 核心色彩规范
| 用途 | 色值 |
|------|------|
| 主背景 | `#0F131A` |
| 侧栏背景 | `#151A22` |
| 卡片背景 | `#1E2530` |
| 输入背景 | `#121720` |
| 边框 | `#262F3D` |
| 主文本 | `#E2E8F0` |
| 次文本 | `#64748B` |
| 主强调 | `#0077FF` |
| 主发光 | `#00E5FF` |
| 成功 | `#00E676` |
| 危险 | `#FF1744` |
| 警告 | `#FF9100` |

### 5.4 字体与密度
- 字体优先使用 `Segoe UI` 与 `Microsoft YaHei`
- 主字号 11px~14px 紧凑桌面布局
- 标题更重，正文轻量，辅助说明更淡

### 5.5 交互质感统一要求
1. hover 亮度提升
2. active 发光或底边高亮
3. selected 明确边框或底色反馈
4. disabled 透明度降低
5. error 使用红色强调
6. running 使用绿色或青色强调

### 5.6 页面级复刻要求

#### 项目页
- 左侧项目管理栏
- 主按钮"新建项目"
- 次按钮"打开项目"
- 右侧最近使用项区域
- 项目卡片包含名称、路径、最近修改时间

#### 数据集页
- 顶部过滤栏
- 左侧图库选择
- 数据集列表
- 可见性、添加、导入图像等操作位
- 数据集进度条与数量
- 图像属性区
- Tag 区
- 中部画廊缩略图区

#### 标注页（当前阶段仅外壳）
- 左侧数据集与类别区
- 中部画布区
- 画布顶部工具条
- 右侧实例列表
- 右侧文件列表
- 为未来画布能力保留足够空间
- 快捷键和工具图标先做结构位

#### 检查页
- 结构上与标注页保持亲缘关系
- 左侧保持数据选择与筛选能力
- 主区域适配检查视图

#### 训练页（最重要页面之一，必须尽早接真实数据）
- 左侧模型任务列表
- 顶部子页签：`配置参数` 与 `实时训练结果`
- 配置页采用四宫格结构：
  - 数据集配置
  - 网络配置
  - 训练设置
  - 数据增强配置
- 结果页采用左状态栏 + 右图表区结构
- 图表包含损失、mAP、召回率、精确率等位置

#### 测试页
- 左侧复用训练任务或模型选择区
- 右侧测试配置
- 统计卡区
- PR 曲线区
- 测试详情区
- 混淆矩阵与检查图像双视图
- 批量修改入口

#### 导出页（必须尽早接真实导出能力）
- 左侧模型选择区
- 右侧导出配置
- 导出报告或验证报告区域
- 导出参数位：格式、路径、目标平台、动态 shape、opset、simplify

### 5.7 弹窗规范
统一风格：半透明遮罩 + 深色卡片主体 + 清晰标题区 + 表单区紧凑对齐 + 底部操作按钮区 + 关闭按钮

必须优先支持的弹窗：
1. 新建项目
2. 打开项目
3. 导入数据集
4. 新建训练模型
5. Tag 编辑
6. 类别配置
7. 批量修改

### 5.8 页面状态规范（每个页面必须预留）
1. 空态
2. 加载态
3. 成功态
4. 失败态
5. 禁用态
6. 未选择项目态
7. 无设备态

### 5.9 图标系统
推荐图标集合至少包含 20 个：项目、数据集、标注、检查、训练、测试、导出、新增、打开、可见、隐藏、编辑、删除、设置、用户、设备状态、播放、停止、展开、折叠

### 5.10 UI 复刻总顺序
1. 步骤 1：主题与组件基座
2. 步骤 2：主框架（顶栏导航、过滤栏、主内容容器、侧栏骨架、底部状态条）
3. 步骤 3：页面空壳
4. 步骤 4：弹窗与上下文菜单
5. 步骤 5：静态交互状态（折叠展开、页签切换、高亮/加载/空/错误态）
6. 步骤 6：真实数据接线（用户确认 UI 架子方向后）

### 5.11 UI 严禁事项
1. 严禁为了赶进度直接在页面中大量硬编码样式
2. 严禁在 MVP 链路未稳定前大量填充页面业务细节
3. 严禁每个页面自造一套交互风格
4. 严禁偏离参考 HTML 的整体视觉语言

---

## 六、各模块详细功能要求

### 6.1 数据集导入（业务闭环执行原则）
必须覆盖：
1. 图片扫描
2. 标签配对
3. 异常记录
4. 类别信息读取
5. 样本数量统计
6. 入库

### 6.2 训练快照
必须保证：
1. 训练永远基于不可变快照
2. 快照与原始数据集状态隔离
3. 快照可追溯到训练运行

### 6.3 训练运行
必须具备：
1. 启动前校验
2. 状态流转
3. 日志事件流
4. 指标回传
5. 失败事件
6. 停止事件
7. 冷启动修复

### 6.4 导出验证
必须保证：
1. 导出产物路径可追踪
2. 导出参数可追踪
3. 导出后自动验证
4. 验证结果可持久化

### 6.5 硬件兼容
1. 支持 NVIDIA 30/40/50 系列 GPU
2. 支持无 GPU 的 CPU-only 设备训练
3. 启动时完成环境探测
4. UI 中展示设备状态
5. 训练前给出设备建议
6. GPU 不可用时可直接降级到 CPU
7. 显存不足时给出可执行的 batch 建议

---

## 七、功能验收标准（按里程碑）

### M0：现状审计与差距清单
- 能明确说清楚哪些功能已通
- 能明确说清楚哪些功能是假实现或半实现
- 能明确说清楚当前最优修复顺序

### M1：项目与数据集基础链路修复
- 至少一套 YOLO 检测数据能成功导入
- 至少一套异常检测数据能成功导入
- 扫描异常能在系统中被标识

### M2：快照、划分与训练启动链路修复
- 检测训练可启动
- 异常检测训练可启动
- 配置和快照能追溯到训练记录

### M3：训练运行、事件回流与稳定性修复
- 训练状态能在 UI 或数据层正确呈现
- 异常退出后不会长期残留错误状态

### M4：导出与验证闭环修复
- 检测模型可导出 pt 和 onnx
- 异常检测模型具备当前阶段约定的导出与验证路径
- 验证结果可追踪

### M5：硬件兼容与环境自检
- 有 GPU 时能正确识别
- 无 GPU 时仍可训练
- 训练前能给出设备状态

### M6：UI 基础设施建设
- 新组件可被多个页面复用
- 视觉风格统一

### M7：UI 空壳完整复刻
- 页面结构接近参考 HTML
- 状态风格统一
- 用户确认方向正确

### M8：UI 接真实数据与服务
- 页面操作走真实服务
- 页面之间状态流转正常

### M9：功能填充与产品补强
- 用户在主要页面可完成本阶段主流程
- 系统具备演示和继续迭代基础

### 横向验收项（每个里程碑都必须验证）
1. 是否破坏现有可用能力
2. 是否引入 UI 风格分裂
3. 是否引入状态不一致
4. 是否有未处理错误路径
5. 是否有明显的假交互占位没有标识清楚

### 当前阶段总验收口径
1. MVP+ 核心闭环稳定
2. 支持 YOLOv8 detect + anomaly
3. 支持 pt + onnx 导出与验证
4. 支持 GPU + CPU-only 训练路径
5. UI 达到高保真空壳复刻标准
6. UI 骨架确认后具备继续填充功能的稳定基础

---

## 八、版本路线图

| 版本 | 阶段 | 交付重点 |
|------|------|---------|
| v0.1.x | MVP+ 稳定化阶段 | 项目/数据集/训练/导出主链路稳定；YOLOv8 detect + anomaly 可用；pt + onnx 导出与验证可用；高保真 UI 骨架完成 |
| v0.2.x | UI 接线与测试补强阶段 | UI 与真实数据流打通；测试页和导出页补强；全局状态、日志、任务中心增强 |
| v1.0.x | 标注引擎阶段 | HBB 标注；OBB 标注；多边形标注；撤销重做与原子写回 |
| v1.1.x | 辅助标注与增量训练阶段 | 历史模型辅助标注；批量审核；增量训练入口；模型血缘链增强 |
| v2.x | 数据治理与全生命周期阶段 | 快照体系强化；主动学习；模型版本治理；多模型家族扩展 |

---

## 九、关键设计边界（硬约束）

1. Qt/C++ 主进程负责 UI、本地状态、数据库和 Python 子进程管理
2. Python 后端负责训练、导出、验证、推理等计算任务
3. 训练必须基于不可变快照
4. 模型版本必须可追溯到训练运行和快照
5. 导出产物必须经过验证
6. 软件必须支持离线使用和 CPU 降级路径
7. 主进程必须保持高可用
8. Python 计算失败不能拖垮 UI
9. 训练和导出优先于标注高级能力
10. 当前阶段以 YOLOv8 detect + anomaly + pt/onnx 为主交付范围

---

## 十、开发前自检要点（preflight_checklist）

### 10.1 环境自检
- Qt 6.11.1、MSVC 2022、CMake 3.22+、Ninja 可用
- Python 3.11+、ultralytics、onnxruntime、opencv-python 可用
- nvidia-smi 可用、PyTorch 能识别 CUDA

### 10.2 常见风险检查
1. **路径问题**：项目路径/数据集路径是否包含中文或空格
2. **设备选择问题**：训练配置是否支持 cpu/cuda:0/cuda:1，是否写死设备
3. **训练残留状态问题**：训练异常退出后数据库状态是否会残留为 running，冷启动时是否有残留修复逻辑
4. **Python 输出阻塞问题**：Python 进程是否启用非缓冲输出，日志和事件是否会卡在缓冲区
5. **GPU 不可用问题**：environment.check 是否能正确给出 GPU 不可用状态，GPU 不可用时是否能走 CPU 降级路径

### 10.3 数据库与状态检查
- Schema 中的表定义和当前代码一致
- 项目、数据集、快照、训练任务、模型版本、导出产物相关表已覆盖当前阶段需求
- 状态值命名在 C++、Python、数据库和 UI 之间一致

---

## 十一、superpowers 子目录补充信息

该子目录记录了 2026-06-08 的一次 QML 缺陷修复任务，包含 3 份文档：

### 11.1 修复内容
1. **ProjectPage.qml**：增加 `import QtQuick.Effects`，消除 `MultiEffect is not a type` 加载期致命错误，恢复"新建项目"和"打开项目"按钮可见性
2. **AnnotationPage.qml**：将 RowLayout 直接子项中误用的 `anchors.verticalCenter` 改为 `Layout.alignment: Qt.AlignVCenter`，消除布局锚点冲突警告
3. **TestingPage.qml**：合并重复的 `Component.onCompleted` 代码块，解决 `Property value set multiple times` 编译报错
4. **test_database.cpp / test_taxonomy.cpp**：尾部空行追加触发增量重编译，解决 LNK2019 链接报错
5. **ModalDialog.qml / Theme.qml**：弹窗居中显示；边框色、分割线色、卡片底色、悬浮态、选中态、辅助文本、禁用态文本亮度上调，提升对比度

### 11.2 验证结果
- Release 预设（x64-release）100% 编译通过，无报错
- LabelTorchV.exe 成功拉起，主界面渲染正常
- 项目页按钮恢复显示
- 标注页无布局警告
- 退出确认弹窗居中显示
- 暗黑科技风界面文本对比度提高

---

## 十二、关键文件路径汇总（绝对路径）

### 文档文件
- `E:\z\project\my\LabelTorchV\docs\architecture.md`
- `E:\z\project\my\LabelTorchV\docs\blueprint.md`
- `E:\z\project\my\LabelTorchV\docs\原始需求.md`
- `E:\z\project\my\LabelTorchV\docs\执行总计划-LabelTorchV-MVP+.md`
- `E:\z\project\my\LabelTorchV\docs\里程碑验收与执行清单.md`
- `E:\z\project\my\LabelTorchV\docs\UI像素级复刻专项规范.md`
- `E:\z\project\my\LabelTorchV\docs\preflight_checklist.md`
- `E:\z\project\my\LabelTorchV\docs\superpowers\plans\2026-06-08-qml-bug-fix.md`
- `E:\z\project\my\LabelTorchV\docs\superpowers\plans\2026-06-08-qml-bug-fix-walkthrough.md`
- `E:\z\project\my\LabelTorchV\docs\superpowers\specs\2026-06-08-qml-bug-fix-design.md`

### UI 最高参考源（文档中提及）
- `reference/Dihuge DLTools/gemini-code-1780391652706.html`
- `reference/Dihuge DLTools/UI.pdf`
- `reference/Dihuge DLTools/UI设计.md`

---

## 总结

LabelTorchV 项目文档体系完整、层次清晰，由"原始需求 → 蓝图 → 架构 → 执行总计划 → 里程碑清单 → UI 专项规范 → 自检清单"七层构成，形成闭环可执行的开发指引。

**当前阶段（MVP+）的核心交付**锁定为：YOLOv8 detect + anomaly 训练 + pt/onnx 导出与验证 + 高保真 UI 空壳复刻，硬件兼容覆盖 NVIDIA 30/40/50 系列 + CPU-only。所有其他能力（OBB、分类、完整标注引擎、辅助标注、增量训练、TensorRT/TFLite/Engine 导出、Ubuntu 兼容、匹配页面）均明确为当前阶段不做，仅允许结构预留。

执行顺序严格按 M0~M9 里程碑推进，**未通过当前里程碑验收不进入下一个**，且 UI 大规模功能填充必须在用户确认 UI 架子方向无误后才能开始。

让我并行读取关键的 QML 页面文件、Python 适配器实现，以及一些 Service 的 .cpp 实现。

I notice that the `reference` directory does not exist directly under `E:\z\project\my\LabelTorchV\`. However, I found a sibling project `E:\z\project\my\LabelTorch\` (without the "V") that contains a `reference/` directory with a `REFERENCE_LINKS.md` file. Let me investigate this and also check the LabelTorchV project's documentation for any reference information.

Let me check the sibling LabelTorch project's reference directory more thoroughly and also look for any UI design reference documents in LabelTorchV.

让我并行读取 TrainingPage.qml 的关键部分、ProjectPage.qml、AnnotationPage.qml，以及验证 Service 实现的关键部分。

让我并行检查 Service .cpp 实现的关键部分、main.cpp 注入、Python server.py，以及 TrainingPage 的训练启动逻辑。

Let me search for the "Dihuge DLTools" reference mentioned in the UI specification and check if there are any other reference-related files in the project.

Let me read the original requirements document to fully understand the intent for the reference directory.

让我并行检查 TrainingPage 的训练启动按钮、ProjectService、DatasetService 实现关键部分，以及 anomaly/testing/active_learning handler。

我已经完成了对 `E:\z\project\my\LabelTorchV\reference` 目录及相关参考信息的全面探索。下面是详细的中文报告。

---

# LabelTorchV 参考项目探索报告

## 一、重要发现：reference 目录当前状态

经过全面搜索，**`E:\z\project\my\LabelTorchV\reference` 目录目前并不存在**。该目录在项目文档中被多次引用，但实际并未创建，也没有克隆任何参考项目代码。

### 1.1 文档中对 reference 目录的引用

通过 Grep 搜索发现，项目文档中有 12 处引用了 `reference/` 路径，主要分布在：

- `E:\z\project\my\LabelTorchV\docs\blueprint.md`（第 42 行）
- `E:\z\project\my\LabelTorchV\docs\architecture.md`（第 201 行）
- `E:\z\project\my\LabelTorchV\docs\preflight_checklist.md`（第 85 行）
- `E:\z\project\my\LabelTorchV\docs\UI像素级复刻专项规范.md`（第 6、26、27、28 行）
- `E:\z\project\my\LabelTorchV\docs\执行总计划-LabelTorchV-MVP+.md`（第 19、50、248 行）
- `E:\z\project\my\LabelTorchV\docs\原始需求.md`（第 37、60 行）

### 1.2 用户原始意图（来自 `docs/原始需求.md`）

用户在原始需求中明确表达了两个意图：

1. **第 37 行**：「有哪些开源的项目开源参考，先 clone 下来到 reference 文件夹，x-anything，labelme，labelimg 等，还有我没考虑到的都得搜索完善」
2. **第 60 行**：「clone reference\REFERENCE_LINKS.md 提到的项目到 reference，你之后的一些功能、实现方案可以参考里面的一些项目」

### 1.3 兄弟项目的 reference 目录

在兄弟项目 `E:\z\project\my\LabelTorch\reference\` 中只找到一个文件：
- `E:\z\project\my\LabelTorch\reference\REFERENCE_LINKS.md`（仅维护候选项目链接清单，未实际 clone 代码）

---

## 二、计划中的参考项目清单

虽然 reference 目录不存在，但通过阅读 `REFERENCE_LINKS.md` 和 `LabelTorchV/README.md` 的「参考项目」章节，可以梳理出用户计划借鉴的参考项目清单。

### 2.1 第一优先级：核心参考项目

| 项目名称 | 类型 | 技术栈 | 仓库地址 | 参考价值 |
|---|---|---|---|---|
| **X-AnyLabeling** | 标注平台 | PySide6 + YOLO | https://github.com/CVHub520/X-AnyLabeling | YOLO 生态标注集成、模型推理集成、标注交互架构 |
| **Ultralytics** | 训练框架 | Python | https://github.com/ultralytics/ultralytics | 训练 API 设计、模型导出流程、配置参数体系 |
| **labelme** | 标注平台 | PyQt5 | https://github.com/wkentaro/labelme | QGraphicsView 标注画布实现、轻量标注交互 |
| **ImageViewer-Qt6** | 图像浏览器 | Qt6 + C++ | https://github.com/p-ranav/ImageViewer-Qt6 | Qt6 高性能图像浏览器、缩放平移架构参考 |

### 2.2 第二优先级：训练与格式参考

| 项目名称 | 类型 | 技术栈 | 仓库地址 | 参考价值 |
|---|---|---|---|---|
| **YOLOv5** | 训练框架 | Python | https://github.com/ultralytics/yolov5 | YOLOv5 训练配置组织方式、数据加载 |
| **JIETStudio** | YOLO 训练 GUI | Python GUI | https://github.com/hazergreleases/JIETStudio | 端到端 YOLO 训练桌面 GUI 工作流参考 |
| **CVAT** | 标注平台 | Web/Docker | https://github.com/cvat-ai/cvat | 复杂标注流程组织、标注审核、AI 辅助标注思路 |

### 2.3 第三优先级：OBB 与特殊场景

| 项目名称 | 类型 | 技术栈 | 仓库地址 | 参考价值 |
|---|---|---|---|---|
| **roLabelImg** | OBB 标注 | PyQt5 | https://github.com/cgvict/roLabelImg | OBB 旋转框标注交互、旋转框数据格式 |
| **mmrotate** | OBB 训练框架 | PyTorch | https://github.com/open-mmlab/mmrotate | OBB 训练框架设计、旋转检测 pipeline |
| **DOTA_devkit** | OBB 数据工具 | Python | https://github.com/CAPTAIN-WHU/DOTA_devkit | OBB 数据格式规范（DOTA 格式） |

### 2.4 其他候选项目（来自 REFERENCE_LINKS.md）

**通用检测/分割/多边形标注**：
- Label Studio（https://github.com/HumanSignal/label-studio）
- LabelImg（https://github.com/HumanSignal/labelImg）
- VoTT（https://github.com/microsoft/VoTT）
- makesense.ai（https://github.com/SkalskiP/make-sense）

**医学/工业可借鉴标注交互**：
- 3D Slicer（https://github.com/Slicer/Slicer）
- ITK-SNAP（https://github.com/pyushkevich/itksnap）

### 2.5 UI 设计参考（缺失）

文档中反复提到的 UI 最高参考源 **`reference/Dihuge DLTools/`** 目录及其文件（`gemini-code-1780391652706.html`、`UI.pdf`、`UI设计.md`）**均不存在**。这是当前 UI 像素级复刻规范的核心参考源，但实际文件缺失。

---

## 三、值得借鉴的功能点（基于参考项目特性分析）

### 3.1 标注平台类（X-AnyLabeling / labelme / CVAT）

| 借鉴功能 | 来源项目 | 对 LabelTorchV 的价值 |
|---|---|---|
| **AI 辅助标注**（模型推理预标注 + 人工审核） | X-AnyLabeling、CVAT | 直接对应 v1.1 辅助标注阶段，本项目已有 AssistedLabelService 骨架 |
| **多格式标签导入导出**（YOLO/COCO/LabelMe/PascalVOC） | labelme、CVAT、X-AnyLabeling | 本项目已实现 YOLO/LabelMe/COCO/Anomalib Handler，可参考其格式转换逻辑 |
| **快捷键体系**（数字键切类、DEL 删除、Ctrl+Z 撤销） | labelme、LabelImg | 本项目标注引擎阶段（v1.0）必需 |
| **画布缩放/平移/坐标变换** | labelme、ImageViewer-Qt6 | 本项目 CanvasController 已有骨架，可参考其高性能实现 |
| **图层管理**（多标注层、可见性控制） | CVAT、labelme | 复杂标注场景必备 |
| **标注审核工作流**（pending/approved/rejected） | CVAT | 本项目辅助标注审核可借鉴 |
| **OBB 旋转框交互**（鼠标拖拽旋转、角度微调） | roLabelImg | 本项目 v3.0 OBB 标注阶段核心参考 |

### 3.2 训练框架类（Ultralytics / YOLOv5 / mmrotate）

| 借鉴功能 | 来源项目 | 对 LabelTorchV 的价值 |
|---|---|---|
| **训练配置参数体系**（yaml 配置 + CLI 覆盖） | Ultralytics、YOLOv5 | 本项目 ConfigPanel 可参考其参数组织方式 |
| **训练进度回调**（epoch 级 loss/mAP 推送） | Ultralytics | 本项目已实现 task.progress 事件，可参考其指标计算 |
| **数据自动划分**（train/val split） | Ultralytics | 本项目 SnapshotService 已实现，可参考其分层抽样 |
| **多模型家族注册机制** | Ultralytics | 本项目 TrainingAdapter 注册机制已借鉴 |
| **模型导出流程**（pt→onnx→engine） | Ultralytics | 本项目 ExportService 可参考其导出参数体系 |
| **早停/AMP/断点续训** | Ultralytics、YOLOv5 | 本项目训练配置已支持 |
| **OBB 训练 pipeline** | mmrotate | 本项目 v3.0 OBB 训练阶段参考 |

### 3.3 YOLO 训练 GUI 类（JIETStudio）

| 借鉴功能 | 来源项目 | 对 LabelTorchV 的价值 |
|---|---|---|
| **端到端桌面 GUI 工作流** | JIETStudio | 本项目同为桌面 GUI，可参考其页面流转设计 |
| **训练参数可视化配置** | JIETStudio | 本项目 ConfigPanel 四宫格结构可参考 |
| **训练日志实时展示** | JIETStudio | 本项目 LogView 已实现 |

### 3.4 工业视觉/医学标注类（3D Slicer / ITK-SNAP）

| 借鉴功能 | 来源项目 | 对 LabelTorchV 的价值 |
|---|---|---|
| **高精度标注交互**（亚像素级、十字线辅助） | 3D Slicer、ITK-SNAP | 工业缺陷检测对精度要求高，可借鉴 |
| **多视图协同**（同步缩放/平移） | 3D Slicer | 批量审核场景可借鉴 |
| **标注修订历史** | 3D Slicer | 本项目 annotation_revisions 已实现，可参考其 UI 展示 |

---

## 四、值得借鉴的 UI/UX 设计

### 4.1 整体布局模式

| 设计模式 | 来源项目 | 借鉴建议 |
|---|---|---|
| **三栏布局**（左导航 + 中内容 + 右属性/日志） | CVAT、X-AnyLabeling、JIETStudio | 本项目已采用类似布局，可参考 CVAT 的可折叠侧栏 |
| **顶部过滤栏 + 主内容区** | CVAT、Dihuge DLTools（参考稿） | 本项目 UI 规范已要求 |
| **子页签切换**（配置/结果） | JIETStudio、Dihuge DLTools | 本项目训练页已采用「配置参数」+「实时训练结果」子页签 |

### 4.2 标注画布交互

| 交互模式 | 来源项目 | 借鉴建议 |
|---|---|---|
| **工具栏切换标注模式**（HBB/OBB/多边形/选择） | labelme、X-AnyLabeling、roLabelImg | 本项目标注页工具条可参考 |
| **实时标注列表**（右侧实例列表 + 选中高亮） | labelme、CVAT | 本项目标注页右侧实例列表已规划 |
| **类别面板 + 颜色编码** | labelme、LabelImg | 本项目 ClassPanel 已有骨架，Theme.classColors 已定义 |
| **缩略图导航 + 主画布** | CVAT、3D Slicer | 大图浏览场景可借鉴 |

### 4.3 训练监控 UI

| UI 设计 | 来源项目 | 借鉴建议 |
|---|---|---|
| **实时损失曲线**（loss/mAP/precision/recall） | JIETStudio、Ultralytics Hub | 本项目 MetricChart 可参考 |
| **训练状态卡片**（epoch/进度/剩余时间） | JIETStudio | 本项目训练页状态栏可参考 |
| **多版本指标对比图** | Ultralytics Hub | 本项目 ComparePage 可参考 |

### 4.4 数据集管理 UI

| UI 设计 | 来源项目 | 借鉴建议 |
|---|---|---|
| **缩略图画廊 + 懒加载** | CVAT、X-AnyLabeling | 本项目数据集页可参考 |
| **样本属性面板**（尺寸/类别/标注状态） | CVAT | 本项目 DatasetStatsView 可参考 |
| **Tag 管理与筛选** | CVAT | 本项目数据集页已规划 Tag 区 |

### 4.5 视觉设计语言（来自 UI 像素级复刻规范）

根据 `docs/UI像素级复刻专项规范.md`，本项目 UI 应参考 `Dihuge DLTools` 的视觉语言：

- **深色工业风**：主背景 `#0F131A`、侧栏 `#151A22`、卡片 `#1E2530`
- **冷调蓝灰主基底 + 高亮青蓝强调**：主强调 `#0077FF`、主发光 `#00E5FF`
- **细描边、轻阴影、弱发光**
- **信息密度偏高，桌面工具型**（不走消费级卡通风）
- **紧凑布局**：主字号 11~14px，Segoe UI + Microsoft YaHei

> 注意：该参考 HTML 文件实际缺失，需要用户补充或重新提供。

---

## 五、建议本项目参考的优秀特性

### 5.1 短期（v0.1~v0.2 MVP+ 阶段）建议优先借鉴

1. **Ultralytics 训练配置体系**
   - 借鉴其 yaml 配置 + 参数覆盖模式，完善本项目 ConfigPanel
   - 借鉴其训练进度回调机制，完善 task.progress 事件 payload

2. **JIETStudio 桌面 GUI 工作流**
   - 借鉴其端到端训练 GUI 的页面流转
   - 借鉴其训练参数可视化配置的四宫格结构

3. **Dihuge DLTools UI 视觉规范**（需补充参考文件）
   - 按规范要求复刻深色工业风视觉
   - 实现统一主题系统与基础组件

### 5.2 中期（v1.0~v1.1 标注引擎阶段）建议借鉴

1. **X-AnyLabeling 的 AI 辅助标注架构**
   - 借鉴其模型推理预标注 + 人工审核工作流
   - 借鉴其多模型切换机制（对应本项目多模型家族）

2. **labelme 的画布实现**
   - 借鉴其 QGraphicsView 高性能画布（本项目用 QML AnnotCanvasItem，可参考其交互逻辑）
   - 借鉴其快捷键体系

3. **CVAT 的标注审核工作流**
   - 借鉴其 pending/approved/rejected 状态机
   - 借鉴其批量审核 UI

### 5.3 长期（v2.0~v3.0 数据治理与 OBB 阶段）建议借鉴

1. **roLabelImg 的 OBB 旋转框交互**
   - 借鉴其鼠标拖拽旋转、角度微调交互
   - 借鉴其旋转框数据格式

2. **mmrotate 的 OBB 训练 pipeline**
   - 借鉴其 OBB 数据加载与增强
   - 借鉴其 OBB 评估指标计算

3. **CVAT 的主动学习工作流**
   - 借鉴其难例队列与优先级排序
   - 借鉴其低置信样本回流机制

### 5.4 工程实现建议

1. **尽快创建 reference 目录并克隆核心参考项目**
   - 用户原始需求第 9 条明确要求 clone 参考项目到 reference 文件夹
   - 建议优先克隆：X-AnyLabeling、labelme、Ultralytics、roLabelImg、JIETStudio
   - 可编写脚本批量 clone，并添加 `.gitignore` 排除 reference 目录避免污染主仓库

2. **补充缺失的 Dihuge DLTools UI 参考文件**
   - `reference/Dihuge DLTools/gemini-code-1780391652706.html`
   - `reference/Dihuge DLTools/UI.pdf`
   - `reference/Dihuge DLTools/UI设计.md`
   - 这是当前 UI 复刻的最高参考源，缺失会阻断 UI 开发

3. **建立参考项目分析文档**
   - 每克隆一个参考项目，建议在 `reference/{项目名}/ANALYSIS.md` 中记录其架构亮点、可借鉴点、不借鉴原因
   - 避免参考流于形式

---

## 六、相关文件路径汇总

### 6.1 已存在的参考信息文件

- `E:\z\project\my\LabelTorch\reference\REFERENCE_LINKS.md`（兄弟项目的参考链接清单）
- `E:\z\project\my\LabelTorchV\README.md`（第 100~144 行：「参考项目」章节，含开源致谢表）
- `E:\z\project\my\LabelTorchV\docs\原始需求.md`（第 37、60 行：用户对 reference 目录的原始意图）
- `E:\z\project\my\LabelTorchV\docs\UI像素级复刻专项规范.md`（UI 复刻规范，引用了缺失的 Dihuge DLTools 参考稿）
- `E:\z\project\my\LabelTorchV\docs\blueprint.md`（产品蓝图，第 42 行引用 reference 路径）
- `E:\z\project\my\LabelTorchV\docs\architecture.md`（架构文档，第 201 行引用 reference 路径）

### 6.2 缺失但被引用的文件

- `E:\z\project\my\LabelTorchV\reference\`（整个目录不存在）
- `E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\gemini-code-1780391652706.html`
- `E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\UI.pdf`
- `E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\UI设计.md`

---

## 七、结论

**核心结论**：用户要求探索的 `E:\z\project\my\LabelTorchV\reference` 目录**当前并不存在**，没有实际克隆的参考项目代码可供分析。但通过阅读项目文档（README.md、原始需求.md、UI像素级复刻专项规范.md、blueprint.md），已完整梳理出用户计划借鉴的 13+ 个参考项目清单及其借鉴价值。

**关键建议**：
1. 立即创建 `reference/` 目录并克隆第一优先级的 4 个核心参考项目（X-AnyLabeling、Ultralytics、labelme、ImageViewer-Qt6）
2. 补充缺失的 Dihuge DLTools UI 参考文件（这是 UI 开发的阻断项）
3. 将 `reference/` 目录加入 `.gitignore`，避免参考项目代码污染主仓库
4. 后续可基于本报告的「值得借鉴的功能点」和「值得借鉴的 UI/UX 设计」章节，按版本阶段逐步消化参考项目的优秀特性

让我并行检查 TrainingPage 的训练启动按钮完整逻辑、ModelRegistry、SnapshotService 实现关键部分，以及 anomalib_adapter。

让我并行检查 TrainingService 的 startTraining 完整实现、ModelRegistry 注册版本逻辑、TestingService 实现，以及一些剩余 QML 页面。

让我并行检查 TrainingService 的事件处理（task.succeeded 后是否注册模型版本）、TestingService 实现、ModelRegistry 关键方法。

让我最后并行检查 TestingService.cpp 实现、ModelPage.qml、ActiveLearningPage.qml，以及一些剩余的关键信息。

我已经完成了对 LabelTorchV 项目的全面探索。现在整理所有发现，撰写详细的中文报告。

---

# LabelTorchV 项目实现状态调查报告

## 一、QML 界面层（src/shell/qml/ + src/features/*/qml/）

### 1.1 主窗口 Main.qml（V5 布局）
**文件**：`e:\z\project\my\LabelTorchV\src\shell\qml\Main.qml`

**实际功能（有真实逻辑）**：
- 顶栏（50px）：Logo + 7 个导航标签（项目/数据集/标注/检查/训练/测试/导出）+ GPU 状态灯 + Python 后端连接状态灯
- 全局筛选栏（FilterBar，38px）：数据集筛选 + TAG 过滤 + 标签类别筛选（仅 dataset/annotation/check 页显示）
- 主内容区：StackLayout + 7 个 Loader 按需异步加载页面 + 预加载定时器
- 底栏（34px）：工作区名称 + 选中文件 + 标注进度条（实时计算 `updateAnnotationProgress()`）
- 关闭确认对话框（ModalDialog）
- IPC 信号监听：`onResponseReceived`（GPU 检测）、`onConnectedChanged`（自动发 environment.check）、`onEventReceived`（训练进度脉冲灯）

**仅 UI 占位**：
- 顶栏右侧 signal/gear/user 三个图标仅有 hover 效果，无点击逻辑

### 1.2 各功能页面实际功能

| 页面 | 文件 | 实际功能 | 状态 |
|------|------|----------|------|
| 项目 | `ProjectPage.qml` | 新建项目对话框、打开项目（FolderDialog）、类别体系管理（CollapsibleSection）、路径校验 | 已实现 |
| 数据集 | `ImportPage.qml` + `DatasetPage.qml` | 单目录自动探测/分别指定路径两种模式、拖拽+浏览、异步扫描、格式徽章、确认导入、删除数据集 | 已实现 |
| 标注 | `AnnotationPage.qml` | HBB/OBB/分类/异常四种模式、画布加载、保存标注、前后导航、绘制工具切换 | 已实现 |
| 检查 | `CheckPage.qml` | 数据集统计、缺陷图像网格、类别过滤、程度图像模式 | 已实现（含 1 处 TODO） |
| 训练 | `TrainingPage.qml`（142KB，超大） | 模型家族选择、4 宫格配置、环境检测、开始/停止训练、实时 4 图表、创建快照对话框 | 已实现 |
| 测试 | `TestingPage.qml` | 模型列表、任务配置、测试启动、指标展示、混淆矩阵、PR 曲线 | 已实现 |
| 导出 | `ExportPage.qml` | 模型列表、格式选择（pt/onnx）、ONNX 配置、导出按钮、导出历史、验证详情、导出报告 | 已实现 |

### 1.3 次要页面
- `TaxonomyPage.qml`、`SnapshotPage.qml`、`ComparePage.qml`、`MetricChart.qml`、`LogView.qml`、`ConfigPanel.qml`、`OnnxConfigPanel.qml`、`DatasetBrowserPage.qml`、`ClassMappingPage.qml`、`DatasetStatsView.qml`、`ClassManagerPanel.qml`、`ProjectCard.qml`、`TaskTypeSwitcher.qml`、`AnnotCanvas.qml`、`ClassPanel.qml`、`SampleList.qml`、`ReviewDialog.qml`、`AssistedLabelPanel.qml`、`AnomalyInferPanel.qml`、`LowConfQueue.qml`、`HardCaseQueue.qml`、`QueueStatsPanel.qml`、`ActiveLearningPage.qml`、`TestingPage.qml`

**问题发现**：`ActiveLearningPage.qml` 使用了 `Theme.surface`、`Theme.onSurface`、`Theme.fontH6`、`Theme.spacingMd` 等属性（第 20、28、29、24 行），这些属性在 Theme.qml 中可能不存在（项目规范使用 `bgSide`/`textMain`/`fontFamily`/`spacingNormal` 等命名），可能导致该页面运行时显示异常。

---

## 二、C++ Service 层（src/features/）

### 2.1 DatasetService（数据集导入）— 已完整实现
**文件**：`e:\z\project\my\LabelTorchV\src\features\dataset\DatasetService.h/cpp`

**已实现方法**：
- `importDataset`（YOLO txt）、`importDatasetJson`（COCO JSON）、`importDatasetV2`（统一入口，自动分发）、`importDatasetSeparate`（分别指定图片/标签路径）
- `importLabelMeDataset`、`importClassifyFolderDataset`、`importAnomalyDataset`（私有方法，多格式支持）
- `listDatasets`、`getDataset`、`deleteDataset`、`getSampleStats`、`detectAnomalies`、`getClassDistribution`
- `listSamples`、`getSampleCount`、`appendImport`、`resplitDataset`、`updateClassName`
- `scanFolder`、`scanSeparate`、`scanFolderAsync`、`scanSeparateAsync`（异步扫描 + 信号通知）
- `syncClassesToTaxonomy`（导入类别同步到 taxonomy）

**无 stub/TODO**。

### 2.2 TrainingService（训练任务）— 已完整实现
**文件**：`e:\z\project\my\LabelTorchV\src\features\training\TrainingService.h/cpp`

**已实现方法**：
- `createRun`：创建 draft 状态训练任务
- `startTraining`：状态校验 → 后台线程 `QtConcurrent::run` 调用 `prepareSnapshotPhysicalDir` → 回到 UI 线程更新状态为 running → `sendRequest("train.start", payload)`
- `stopTraining`：`sendRequest("train.stop")`
- `handleTrainingEvent`：处理 `task.succeeded` 事件，**自动注册模型版本**（INSERT INTO model_versions，含 best_weight_path/last_weight_path/metrics）
- `listRuns`、`getRun`、`deleteRun`、`updateRunStatus`、`listAdapters`、`reconcileStaleRuns`（冷启动自检）

**关键链路**：训练完成 → `task.succeeded` 事件 → `handleTrainingEvent` → 注册 model_versions 记录。链路完整。

### 2.3 SnapshotService（数据快照）— 已完整实现
**文件**：`e:\z\project\my\LabelTorchV\src\features\training\SnapshotService.h/cpp`

**已实现方法**：
- `createSnapshot`：创建不可变快照（sample_manifest_json + split_manifest_json）
- `prepareSnapshotPhysicalDir`：准备 YOLO 格式物理目录 + 生成 data.yaml
- `prepareAnomalySnapshotDir`：准备 Anomalib 格式目录（train/good、test/good、test/defective）
- `listSnapshots`、`getSnapshot`、`deleteSnapshot`、`getSampleManifest`、`getSplitManifest`、`isImmutable`、`isOBBDataset`

### 2.4 ExportService（导出模型）— 已完整实现
**文件**：`e:\z\project\my\LabelTorchV\src\features\export\ExportService.h/cpp`

**已实现方法**：
- `exportModel`：创建 export_artifacts 记录 → `sendRequest("export.run", payload)` → 状态更新为 running
- `verifyExport`：状态 succeeded → verifying → `sendRequest("artifact.verify", payload)`
- `handleIpcResponse`：**导出成功后自动触发验证**（export.run 响应成功后自动发 artifact.verify）
- `getExportStatus`、`listExports`、`updateExportStatus`、`reconcileStaleExports`、`exportReport`

**关键链路**：导出 → 自动验证。链路完整。

### 2.5 InferenceService（推理）— 已完整实现
**文件**：`e:\z\project\my\LabelTorchV\src\features\inference\InferenceService.h/cpp`

**已实现方法**：
- `runInference`：创建 assisted_label_batches 记录 → `sendRequest("inference.run", payload)`
- `cancelBatch`：`sendRequest("inference.cancel")`
- `getBatchStatus`、`listBatches`

### 2.6 AnnotationService（标注）— 已完整实现
**文件**：`e:\z\project\my\LabelTorchV\src\features\annotation\AnnotationService.h/cpp`

**已实现方法**：
- `loadAnnotations`/`saveAnnotations`（HBB）、`loadOBBAnnotations`/`saveOBBAnnotations`（OBB）
- `loadPolygonAnnotations`/`savePolygonAnnotations`（多边形）
- `loadClassificationLabels`/`saveClassificationLabels`（分类，单/多标签）
- `loadAnomalyLabels`/`saveAnomalyLabels`（异常检测）
- `setShapeType`、`createRevision`（修订追踪）、`listSamples`、`getSample`

### 2.7 其他 Service
- **ProjectService**：`createProject`/`importProject`/`deleteProject`/`setTaskType`/`validateProjectPath` — 已实现
- **TaxonomyService**：类别体系版本管理 — 已实现
- **ModelRegistry**：`registerModelVersion`/`listModelVersions`/`getModelVersion`/`deleteModelVersion`/`setParentVersion`/`setTag`/`removeTag`/`importExternalModel` — 已实现
- **MetricService**：指标查询对比 — 已实现
- **TestingService**：`createTestTask`/`startTestTask`/`stopTestTask`/`getTestResults`/`getConfusionMatrix`/`getPRCurveData`/`reconcileStaleTasks` — 已实现，通过 IPC 调用 `testing.start`/`testing.stop`
- **AssistedLabelService**：辅助标注审核 — 已实现
- **AnomalyService**：异常检测推理 — 已实现
- **ActiveLearningService**：`collectLowConfSamples`/`prioritizeQueue` — 已实现
- **ClassMappingService**：类别映射 — 已实现

### 2.8 main.cpp 上下文注入（28 个对象）
**文件**：`e:\z\project\my\LabelTorchV\src\app\main.cpp`（第 266-293 行）

所有 Service/Model 均已通过 `setContextProperty` 注入 QML：appSettings、appController、projectService、projectModel、taxonomyService、taxonomyModel、datasetService、datasetModel、classMappingService、annotationService、annotationModel、canvasController、ipcClient、snapshotService、snapshotModel、trainingService、trainingModel、modelRegistry、metricService、modelVersionModel、inferenceService、assistedLabelService、anomalyService、anomalyDetector、exportService、activeLearningService、testingService、testingModel。

---

## 三、Python 后端（backend/labeltorch_backend/）

### 3.1 server.py — IPC 服务端主循环
**文件**：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\server.py`

**已注册的 16 个命令处理器**：
- `environment.check`、`train.start`/`stop`/`status`/`list_adapters`/`data_split`
- `inference.run`、`export.run`、`artifact.verify`
- `anomaly.infer`、`active_learning.collect_low_conf`/`prioritize_queue`/`queue_stats`
- `testing.start`/`stop`/`status`、`shutdown`

异步主循环 + stdin 读取线程 + 30 秒超时 + JSON-RPC 响应/事件推送。

### 3.2 训练 handler（training.py）— 真正调用 ultralytics
**文件**：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\handlers\training.py`

- `handle_start`：从 `TrainingAdapterRegistry` 获取适配器 → 设置 epoch 回调 → `asyncio.create_task(_run_training(...))`
- `_run_training`：发 `task.started` 事件 → `adapter.start_training(config)` → 根据结果发 `task.succeeded`/`task.failed`/`task.stopped` 事件，含 `best_weight_path`/`last_weight_path`/`metrics`
- epoch 回调 `on_epoch_end`：解析 mAP50/mAP50-95/precision/recall/auroc/top1/top5 等指标，发 `task.progress` + `task.log` 事件
- `_find_best_weight`/`_find_last_weight`：支持 YOLO `weights/best.pt` 和 anomalib `*.ckpt` 两种格式

### 3.3 推理 handler（inference.py）— 真正调用 ultralytics
**文件**：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\handlers\inference.py`

- `handle_run`：`from ultralytics import YOLO` → `model.predict(...)` → 返回 predictions（含 class_id/class_name/confidence/xyxy）
- CUDA 不可用时自动回退 CPU
- 使用 `loop.run_in_executor` 避免阻塞事件循环

### 3.4 导出 handler（export.py）— 真正调用适配器导出 + ONNX 验证
**文件**：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\handlers\export.py`

- `handle_run`：从 `TrainingAdapterRegistry` 获取适配器 → `adapter.export_model(weight_path, format, options)` → 拷贝到 output_path → 返回 file_size_bytes
- `handle_verify`：ONNX 格式用 `onnxruntime.InferenceSession` 验证（输入/输出 shape+type），TorchScript 用 `torch.jit.load` 验证，其他格式跳过
- 使用 `asyncio.to_thread` 避免阻塞

### 3.5 异常检测 handler（anomaly.py）— 真正调用 anomalib
**文件**：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\handlers\anomaly.py`

- `handle_infer`：`from anomalib.engine import Engine` + `from anomalib.models import get_model` → 加载 checkpoint → 推理 → 返回异常分数和热力图

### 3.6 测试 handler（testing.py）— 已实现
**文件**：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\handlers\testing.py`

- `handle_start`/`handle_stop`/`handle_status` + 后台 `_run_testing` 协程，发 `test.started`/`test.progress`/`test.succeeded`/`test.failed` 事件

### 3.7 主动学习 handler（active_learning.py）— 真正调用 ultralytics
**文件**：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\handlers\active_learning.py`

- `handle_collect_low_conf`：`from ultralytics import YOLO` → `model.predict` → 收集低置信样本
- `handle_prioritize_queue`/`handle_queue_stats`

### 3.8 训练适配器
**UltralyticsAdapter**（`adapters/ultralytics_adapter.py`）— 完整实现：
- 支持 yolov5/yolov8/yolov8_obb/yolov8_cls/yolov10/yolov11
- `start_training`：在线程池中执行 `self._model.train(...)`，注册 `on_fit_epoch_end` 回调捕获 epoch 进度
- `export_model`：`model.export(format=..., imgsz=..., opset=..., dynamic=..., simplify=...)`
- `collect_metrics`：从 results.csv 读取最终指标
- `stop_training`：通过 `threading.Event` 中断训练（抛 `StopTrainingException`）

**AnomalibAdapter**（`adapters/anomalib_adapter.py`）— 完整实现：
- 支持 efficient_ad/patchcore/padim/stfpm/cflow/dfkde/dfm/ganomaly/fastflow/reverse_distillation/csflow/devnet
- `start_training`：`from anomalib.engine import Engine` → `engine.train(...)`

---

## 四、数据库 Schema（src/core/database/Schema.cpp）

**文件**：`e:\z\project\my\LabelTorchV\src\core\database\Schema.cpp`

**实际创建了 15 张表**（CLAUDE.md 中说 14 张，实际多了 `testing_runs` 表）：

| 序号 | 表名 | 用途 | 状态 |
|------|------|------|------|
| 1 | projects | 项目信息 | 已创建 |
| 2 | taxonomies | 类别体系 | 已创建 |
| 3 | datasets | 数据集 | 已创建 |
| 4 | dataset_samples | 样本记录 | 已创建 |
| 5 | imported_label_schemas | 导入标签 schema | 已创建 |
| 6 | class_mapping_revisions | 类别映射修订 | 已创建 |
| 7 | annotation_revisions | 标注修订 | 已创建 |
| 8 | dataset_snapshots | 数据快照 | 已创建 |
| 9 | training_runs | 训练运行 | 已创建 |
| 10 | model_versions | 模型版本 | 已创建（支持 trained + imported 两种 source） |
| 11 | assisted_label_batches | 辅助标注批次 | 已创建 |
| 12 | export_artifacts | 导出产物 | 已创建 |
| 13 | task_events | 任务事件审计 | 已创建 |
| 14 | run_metrics | 训练指标 | 已创建 |
| 15 | testing_runs | 测试运行 | 已创建（CLAUDE.md 未列出） |

所有表都有完整 DDL，含外键约束、默认值、时间戳。

---

## 五、端到端流程分析（导入→快照→训练→导出）

### 5.1 主链路已打通

```
1. 导入数据集
   ImportPage.qml → datasetService.importDatasetV2()
   → 多格式分发（YOLO/COCO/LabelMe/异常/分类）
   → INSERT datasets + dataset_samples + imported_label_schemas
   → syncClassesToTaxonomy() 同步类别到 taxonomy
   ✅ 链路完整

2. 创建数据快照
   TrainingPage.qml → createSnapshotDialog → snapshotService.createSnapshot()
   → INSERT dataset_snapshots（sample_manifest_json + split_manifest_json）
   ✅ 链路完整

3. 启动训练
   TrainingPage.qml "开始训练"按钮 → trainingService.createRun() + startTraining()
   → 后台线程 snapshotService.prepareSnapshotPhysicalDir()（拷贝文件 + 生成 data.yaml）
   → sendRequest("train.start", payload)
   → Python: adapter.start_training() → ultralytics YOLO.train()
   → epoch 回调 → task.progress 事件 → TrainingService::handleTrainingEvent → trainingProgress 信号 → UI 实时图表
   → 训练完成 → task.succeeded 事件 → handleTrainingEvent → INSERT model_versions
   ✅ 链路完整

4. 导出模型
   ExportPage.qml "导出模型"按钮 → exportService.exportModel()
   → INSERT export_artifacts → sendRequest("export.run", payload)
   → Python: adapter.export_model() → ultralytics YOLO.export()
   → 响应成功 → handleIpcResponse 自动发 artifact.verify
   → Python: onnxruntime 验证 → 返回 inputs/outputs shape
   → 更新 export_artifacts.validation_result
   ✅ 链路完整
```

### 5.2 端到端流程中的断点/隐患

**严重程度低（不影响主链路）**：

1. **InteractionManager 是 stub**（`src/features/annotation/canvas/InteractionManager.cpp:7`）
   - 日志输出 `"InteractionManager initialized (stub)"`
   - 影响：标注交互管理功能可能不完整，但 AnnotationPage 通过 CanvasController + AnnotCanvasItem 已实现标注核心功能

2. **Polygon 几何是 stub**（`src/features/annotation/geometry/Polygon.cpp:1`）
   - 注释 `// Polygon is a stub struct for a later phase.`
   - 影响：多边形标注功能可能不完整，但 HBB/OBB 标注已完整

3. **AnomalyDetector 无 ONNX Runtime 时占位**（`src/features/inference/AnomalyDetector.cpp:275`）
   - 无 ONNX Runtime 时返回 `anomalyScore=0.0, isAnomalous=0` 的占位结果
   - 影响：C++ 端的异常检测推理在无 ONNX Runtime 时无效，但 Python 端的 `anomaly.infer` handler 通过 anomalib 真正实现，主链路不受影响

4. **CheckPage.qml 有 1 处 TODO**（`src/features/dataset/qml/CheckPage.qml:414`）
   - `// TODO: 打开程度模型标注界面`
   - 影响：检查页的"程度模型标注"入口未实现，不影响主链路

5. **ActiveLearningPage.qml 使用不存在的 Theme 属性**
   - 使用 `Theme.surface`/`Theme.onSurface`/`Theme.fontH6`/`Theme.spacingMd`（第 20、24、28、29 行）
   - 项目 Theme.qml 实际使用 `bgSide`/`textMain`/`fontFamily`/`spacingNormal` 命名
   - 影响：主动学习页面可能显示异常，但该页面不在主链路导航中（Main.qml 的 7 个导航页不含主动学习）

6. **CLAUDE.md 中 Schema 表数量与实际不符**
   - CLAUDE.md 说 14 张表，实际 Schema.cpp 创建了 15 张（多了 testing_runs）
   - 影响：文档与代码不同步，建议更新 CLAUDE.md 第 7 章

---

## 六、总结

### 6.1 已实现的功能（有真实逻辑）

| 模块 | 功能 | 验证证据 |
|------|------|----------|
| 项目管理 | 创建/打开/删除项目、任务类型切换、路径校验 | ProjectService.cpp 完整实现 |
| 类别体系 | 版本管理、增删类别 | TaxonomyService 完整实现 |
| 数据集导入 | YOLO/COCO/LabelMe/异常/分类 5 种格式 | DatasetService.cpp 多格式分发 + Python 无 stub |
| 数据快照 | 不可变快照、train/val 划分、物理目录准备 | SnapshotService.cpp 完整实现 |
| 训练 | 创建/启动/停止训练、epoch 进度、自动注册模型版本 | TrainingService.cpp + training.py + ultralytics_adapter.py 真正调用 YOLO.train() |
| 模型版本 | 注册/列表/标签/血缘/外部导入 | ModelRegistry.cpp 完整实现 |
| 推理 | 批量推理、取消 | InferenceService.cpp + inference.py 真正调用 YOLO.predict() |
| 导出 | pt/onnx 导出、ONNX Runtime 验证、自动验证 | ExportService.cpp + export.py 真正调用 YOLO.export() + onnxruntime |
| 标注 | HBB/OBB/分类/异常、修订追踪 | AnnotationService.cpp 完整实现 |
| 测试 | 评估任务、混淆矩阵、PR 曲线 | TestingService.cpp + testing.py 完整实现 |
| 异常检测推理 | anomalib Engine 推理 | anomaly.py 真正调用 anomalib |
| 主动学习 | 低置信收集、队列排序 | active_learning.py 真正调用 ultralytics |
| IPC | 16 个命令处理器、自动重启、事件推送 | server.py + IpcClient 完整实现 |
| 数据库 | 15 张表、WAL 模式、外键约束 | Schema.cpp 完整 DDL |

### 6.2 仅 UI 占位/部分实现的功能

- 顶栏 signal/gear/user 图标（仅 hover，无点击逻辑）
- CheckPage 的"程度模型标注"入口（TODO）
- InteractionManager（stub 日志，但标注核心功能通过其他类实现）
- Polygon 几何（stub，多边形标注可能不完整）

### 6.3 完全缺失/有问题的功能

- ActiveLearningPage.qml 使用不存在的 Theme 属性（可能导致页面显示异常）
- AnomalyDetector C++ 端在无 ONNX Runtime 时仅返回占位结果（但 Python 端 anomalib 推理可用）

### 6.4 端到端流程结论

**"导入→快照→训练→导出"主链路已完全打通**，无阻断性断点。具体表现为：

1. **导入**：5 种格式真实导入，事务批量插入，类别自动同步 taxonomy
2. **快照**：不可变快照 + 物理 data.yaml 生成（后台线程，不阻塞 UI）
3. **训练**：真正调用 ultralytics YOLO.train()，epoch 级进度实时推送，训练完成自动注册模型版本
4. **导出**：真正调用 YOLO.export()，导出后自动用 onnxruntime 验证

**模型版本可追溯性已满足**：`model_versions.run_id → training_runs.snapshot_id → dataset_snapshots` 链路完整，符合工业审计要求。

**与 Ralph.md 进程记录一致**：T-10 端到端测试进行中，T-11 异常场景测试已完成，主链路无阻断，剩余问题均为非主链路的边缘功能。

# Dihuge DLTools 参考设计详细分析报告

## 目录探索结论

`E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\` 目录下仅包含 3 个文件：
- `UI.pdf`（设计稿）
- `UI设计.md`（设计说明文档）
- `gemini-code-1780391652706.html`（最终敲定版本，约 2931 行，222KB）

**重要说明**：目录中只存在一个 HTML 文件（即最终版），不存在其他非最终版 HTML 文件可供对比。因此本报告第 5 部分将基于 HTML 内部注释（如 "V1 顶级美学色彩规范"、"终极融合版" 等）和 `UI设计.md` 的需求描述，推断该最终版相对原始需求的演进点。

---

## 1. 整体 UI 布局、配色方案、视觉风格

### 1.1 全局布局结构（自上而下）

| 区域 | 高度 | 说明 |
|------|------|------|
| **Header 顶栏** | 50px | 导航 Tab + 右上角工具图标（信号/齿轮/用户） |
| **Filter Bar 过滤栏** | 自适应（约 36px） | 仅在非"项目"页显示；含数据集/Tag/标签类别三个筛选下拉 |
| **Main 主内容区** | flex-grow（撑满） | 三栏布局：sidebar-left（240px）+ center-content + sidebar-right（240px，可选） |
| **Footer 状态栏** | 34px | 工作区/选中文件名 + 标注进度条（150×6px，68% 发光渐变） |

### 1.2 配色方案（V1 顶级美学色彩规范，深色科技风）

CSS 变量定义（位于 `:root`，第 11-24 行）：

| 变量 | 色值 | 用途 |
|------|------|------|
| `--bg-main` | `#0F131A` | 主背景（最深） |
| `--bg-side` | `#151A22` | 侧栏背景 |
| `--bg-card` | `#1E2530` | 卡片背景 |
| `--bg-input` | `#121720` | 输入框背景 |
| `--border-color` | `#262F3D` | 通用边框 |
| `--text-main` | `#E2E8F0` | 主文字（亮灰白） |
| `--text-muted` | `#64748B` | 次要文字（暗灰） |
| `--primary` | `#0077FF` | 主色（蓝） |
| `--primary-glow` | `#00E5FF` | 强调发光色（青蓝） |
| `--success` | `#00E676` | 成功（绿） |
| `--danger` | `#FF1744` | 危险（红） |
| `--warning` | `#FF9100` | 警告（橙） |

**辅助色**（散见于内联样式）：
- `#FFD600` 黄色（图表基线、precision 曲线）
- `#D500F9` 紫色（severe_loss、mAP50:95(M)）
- `#9C27B0` 紫色（标签类别色块）
- `#A0AEC0` 中灰（toggle 开关圆点）
- `#07090D` / `#05070A` / `#0c0f14` 极深黑（图表容器、预览区）
- `#12161F` / `#181E29` 次深背景（训练配置子面板、下拉框）

### 1.3 字体与排版

- **字体族**：`'Segoe UI', -apple-system, BlinkMacSystemFont, 'Microsoft YaHei', sans-serif`
- **等宽字体**：`monospace`（数值显示、文件名、坐标轴标签）
- **基础字号**：13px
- **字号体系**：9px（图表轴标签）/ 10px（侧栏任务状态）/ 11px（次要说明、按钮）/ 12px（正文、参数标签）/ 13px（区块标题）/ 14px（项目名、模态标题）/ 15px（侧栏顶部标题）/ 16px（logo 文字、加号图标）
- **字重**：300（步进按钮）/ 500（导航/按钮）/ 600（区块标题、logo）
- **字间距**：`letter-spacing: 0.5px`（logo、区块标题）
- **全局禁用文本选择**：`user-select: none`（除输入框外）

### 1.4 视觉风格特征

- **赛博蓝灰科技风**：深靛蓝灰背景 + 青蓝色发光强调
- **发光效果**：logo 图标 `box-shadow: 0 0 10px rgba(0, 229, 255, 0.3)`；激活 Tab 文字 `text-shadow: 0 0 8px rgba(0, 229, 255, 0.4)`；进度条 `box-shadow: 0 0 6px var(--primary-glow)`
- **渐变文字**：logo-text 使用 `linear-gradient(to right, #ffffff, #94A3B8)` + `-webkit-background-clip: text`
- **渐变按钮**：`btn-primary` 使用 `linear-gradient(135deg, var(--primary), #0055CC)` + `box-shadow: 0 4px 12px rgba(0, 119, 255, 0.2)`
- **渐变图标**：logo-icon 使用 `linear-gradient(135deg, var(--primary), var(--primary-glow))`
- **毛玻璃模态**：`modal-overlay` 使用 `background: rgba(0, 0, 0, 0.7); backdrop-filter: blur(4px)`
- **细滚动条**：6px 宽，3px 圆角，`var(--border-color)` 颜色

### 1.5 间距与圆角体系

- **圆角**：2px（小色块）/ 3px（小标签、滚动条）/ 4px（输入框、小按钮、上下文菜单项）/ 6px（卡片、列表项、按钮、缩略图）/ 8px（模态窗）/ 20px（toggle 开关）/ 50%（圆形指标）
- **内边距**：模态主体 `24px 30px`；侧栏 `16px`（默认）/ `12px`（数据集/标注/检查页）；卡片 `12px`；图表容器 `16px`
- **外边距**：列表项底部 `8px`；参数行底部 `12px`；区块标题底部 `14px`-`20px`
- **间距 gap**：6px / 8px / 10px / 12px / 14px / 16px / 20px

---

## 2. 主要功能模块和页面结构

### 2.1 顶部导航 Tab（7 个模块）

| 顺序 | Tab 名称 | 图标（Font Awesome） | pageId |
|------|---------|---------------------|--------|
| 1 | 项目 | `fa-folder-tree` | project |
| 2 | 数据集 | `fa-images` | dataset |
| 3 | 标注 | `fa-bezier-curve` | label |
| 4 | 检查 | `fa-list-check` | check |
| 5 | 训练 | `fa-brain` | train |
| 6 | 测试 | `fa-vial-circle-check` | test |
| 7 | 导出 | `fa-file-export` | export |

激活态样式：`color: var(--primary-glow)` + 底部 2px 青蓝边线 + 渐变背景 `linear-gradient(to bottom, transparent 70%, rgba(0, 229, 255, 0.05))` + 文字发光。

### 2.2 各页面结构详解

#### 页面 1：项目（page-project）
- **两列布局**（无右侧栏）
- **左栏（240px）**：项目管理
  - "新建项目"主按钮（`btn-primary` + 加号图标）
  - "打开项目"次按钮（`btn-secondary` + 文件夹打开图标）
- **中栏**：最近使用项列表
  - 列表项（`list-item`）：项目图标（26×26px 圆角方块，显示"B1"）+ 项目名（白色 14px 加粗）+ 路径（11px 灰色）+ 最后修改时间
  - 点击列表项跳转到数据集 Tab

#### 页面 2：数据集（page-dataset）
- **两列布局**
- **左栏（240px, padding 12px）**：
  - 顶部标题"111"（15px 加粗，疑似占位符）
  - **图库选择器**：图标 + 下拉 + 加号
  - **数据集列表**：标题"数据集(1)" + 三个图标按钮（眼睛/加号/图片导入）
    - 数据集项：名称 + 进度条（50×6px，青蓝填充）+ "32/32" + 靶心图标
    - 右键弹出上下文菜单（添加/导入/导出/划分/锁定/修改/删除）
  - **图像属性**：名称/路径/大小（如 1579x1713）/标签实例数/类别（如"划痕(4)"，红色）
  - **Tag 区域**：所属数据集说明 + 2×3 网格按钮（默认/良品/漏检/误检/待定/重要），选中态为蓝色填充
- **中栏**：图库网格
  - `grid-template-columns: repeat(auto-fill, minmax(90px, 1fr))`，gap 10px
  - 缩略图卡片（`thumb-card`）：1:1 宽高比，`filter: contrast(1.1) brightness(0.9)`
  - 激活态：青蓝边框 + `box-shadow: 0 0 12px rgba(0, 229, 255, 0.2)`
  - 左下角小标签：`CAM_00X`，9px 字号，半透明黑底

#### 页面 3：标注（page-label）
- **两列布局**（设计文档要求三列 1/5 + 3/5 + 1/5，但 HTML 实现为两列）
- **左栏（240px）**：
  - 图库与数据集（同数据集页）
  - **图像导航**：当前图像名（带图片图标）+ 删除按钮 + 上一张/下一张切换（`< 1/32 >`）
  - **标签类别**：
    - 选中项：蓝色背景 + 白字 + 色块（12×12px）+ 编辑/删除图标
    - 未选中项：暗背景 + 灰字 + 色块 + 编辑/删除图标（hover 变色）
  - Tag 区域（同数据集页）
- **中栏**：图像标注画布
  - 居中显示图片，`max-width:80%`，2px 边框
  - 图片应用 `opacity:0.8; filter:grayscale(1) contrast(1.4)` 灰度滤镜
  - 标注框：`2px dashed var(--danger)` 红色虚线 + `rgba(255,23,68,0.15)` 红色半透明填充

#### 页面 4：检查（page-check）
- **两列布局**
- **左栏**：
  - 图库与数据集（同上）
  - **程度图像**：模式开关（自定义 toggle，34×20px）+ "标注程度模型"按钮
  - **标签类别**：含"未标注的图像"项 + 各类别（划痕 159 个、111 等），右侧显示数量
- **中栏**：缩放检查网格
  - `grid-template-columns: repeat(auto-fill, minmax(130px, 1fr))`，gap 12px
  - 卡片：1:1 宽高比，图片放大 140% 居中裁剪
  - 左下角红色标签：类别名（如"划痕"）

#### 页面 5：训练（page-train）
- **两列布局**，中栏内部有子 Tab
- **左栏（240px, padding 0）**：
  - 标题"模型列表" + 加号图标（点击弹出"添加模型"模态）
  - 任务列表项（`train-sidebar-task`）：
    - 激活态：左侧 3px 青蓝边线 + `rgba(0, 229, 255, 0.05)` 背景
    - 显示模型名（如"YOLOv8_Battery_S"）+ 状态（绿色"训练中... Epoch: 142" / 灰色"排队中..."）
  - 底部"开始训练"主按钮（100% 宽度 + 播放图标）
- **中栏**：
  - **子 Tab 栏**（40px 高）：配置参数 / 实时训练结果
  - **配置子页**：2×2 网格子面板（`split-subview`），每个面板背景 `#12161F`，标题带左侧 3px 青蓝竖线
    - **左上 数据集配置**：训练集 + 验证集下拉
    - **右上 网络配置**：网络结构/预训练模型/图像大小（带 +/- 步进）/图像通道
    - **左下 训练设置**：Epochs/批量大小/优化器/学习率/设备/存储模型周期/融合IOU阈值 + "训练完成后自动测试"复选框 + 可折叠"高级训练参数"（最终学习率/早停/数据加载进程/训练数据过采样权重/best模型失利权重/程度数据过采样）
    - **右下 数据增强配置**：顶部复选框 + "预览"按钮 + 平移/旋转/缩放/水平翻转/垂直翻转/亮度变化范围/色调变化范围/饱和度变化范围 + mosaic 复选框
  - **结果子页**：
    - 左侧 240px 状态面板：训练状态（Epoch/迭代/当前任务/学习速率/损失）+ 时间统计 + 训练进度条 + 评估状态（最佳Epoch/mAP50）+ 指标展示类别下拉
    - 右侧 4 个可折叠图表卡片（260px 高 SVG）：
      - **损失**：5 条曲线（box_loss/seg_loss/cls_loss/dfl_loss/severe_loss），颜色 `#0077FF`/`#00E676`/`#00E5FF`/`#FF9100`/`#D500F9`
      - **mAP**：5 条曲线（mAP50(B)/mAP50:95(B)/mAP50(M)/mAP50:95(M)/划痕），颜色 `#00E676`/`#FFFFFF`/`#00E5FF`/`#D500F9`/`#FF1744`
      - **召回率**：3 条曲线（recall(B)/recall(M)/划痕），颜色 `#FFD600`/`#00E676`/`#FF1744`
      - **精确率**：3 条曲线（precision(B)/precision(M)/划痕），颜色 `#FFD600`/`#00E676`/`#FF1744`
    - 图表网格：5 条横向虚线（`rgba(255, 23, 68, 0.12)`）+ 黄色实线基线（`#FFD600`）+ 等宽字体坐标标签
    - 每个卡片右上角有"展开"图标

#### 页面 6：测试（page-test）
- **两列布局**
- **左栏**：模型列表（同训练页，按钮改为"开始测试"）
- **中栏**：
  - 顶部 40px 任务选择栏：测试任务下拉 + 加号按钮
  - 三个可折叠卡片纵向排列：
    - **设置卡片**：3 列布局（数据集配置 / 测试参数配置 / 高级测试参数配置），列间 1px 分隔线
      - 测试参数：批量大小/IoU 阈值/评估置信度阈值/设备/测试权重
      - 高级参数：分割阈值/程度分类阈值/输出置信度轮廓/融合IOU阈值
    - **测试卡片**：3 列布局
      - 实例统计：2 个圆形指标（64×64px，conic-gradient 圆环 + 52×52px 内圆显示百分比）显示漏检率/误检率 + F1 分数 + "类别配置"按钮
      - 图像统计：超检率/召回率 + 检测时间/总共时间卡片
      - PR 曲线：SVG 简易坐标系 + 红色虚线 + "复制"按钮
    - **测试详情卡片**（420px 高）：
      - 工具栏：混淆矩阵/检查图像 Tab 切换 + 实例保存计数 + 生成测试/批量修改/仅显示异常按钮 + ground truth toggle + 网格图标
      - 三列内容：左 220px（混淆矩阵表格或检查图像列表）+ 中图片预览区 + 右 220px 实例详情面板

#### 页面 7：导出（page-export）
- **两列布局**（中栏内部分两列）
- **左栏**：模型列表（同训练页，无底部按钮）
- **中栏**：
  - **左侧 280px 参数面板**：3 个卡片
    - 导出模型：设备/测试权重/导出路径（输入框 + 选择按钮）+ "导出模型"按钮
    - 导出报告：测试任务/导出路径 + "导出报告"按钮
    - 版本信息要求：`LabelTorchV v0.1.67.4 / 工具库: v2.1.3 / [缺陷检测v1] / [图像分类v3] / [异常检测v1]`（等宽字体）
  - **右侧**：空白预览区（`#0c0f14` 背景）

### 2.3 模态弹窗清单

| 弹窗 ID | 宽度 | 标题 | 字段 |
|---------|------|------|------|
| `create-project-modal` | 500px | 创建项目 | 项目名称/选择路径/项目描述/初始图库名称（默认"默认图库"）/初始图库类型（缺陷检测/图像分类/异常检测）/基准路径 |
| `add-tag-modal` | 440px | 添加Tag | 新Tag名称/快捷键（可空） |
| `add-label-modal` | 500px | 添加标签 | 名称/颜色（色块 + "调色板"按钮）/快捷键/模型index（带 +/- 步进）/下料id + 错误提示"标签类别名称不能为空" |
| `batch-modify-modal` | 500px | 批量修改 | 确认提示 + 2 个复选框（将推理结果添加为标签实例 / 将这批数据添加到新的数据集）+ 数据集名称输入框（联动禁用） |
| `add-model-modal` | 420px（默认） | 添加模型 | 模型名称/模型类型（缺陷检测模型一）/导入权重复选框 |

模态统一结构：标题栏（居中文字 + 右上角 X 关闭）+ 主体（参数行）+ 底部按钮栏（取消 + 确认）。输入框采用下划线样式（`border: none; border-bottom: 1px solid`）。

---

## 3. 关键交互流程和用户操作步骤

### 3.1 主导航 Tab 切换
- 点击顶部 Tab → `switchTab(tabId, element)` 移除所有 `.active`，给当前 Tab 加 `.active` → 隐藏所有 `.page-content`，显示 `#page-{tabId}` → 项目页隐藏过滤栏，其他页显示

### 3.2 训练子 Tab 切换
- `switchTrainSubTab(subTabId, element)` 切换"配置参数"和"实时训练结果"

### 3.3 列宽/行高拖拽调整
- **垂直分隔条** `.resizer-v`（4px 宽，`cursor: col-resize`）：mousedown 记录起始 X 和左面板宽度 → mousemove 计算 `newWidth = startWidth + (clientX - startX)`，>120px 时应用 → mouseup 移除监听
- **水平分隔条** `.resizer-h`（4px 高，`cursor: row-resize`）：同上逻辑，>100px 时应用
- 拖拽中分隔条变青蓝色（`.dragging` 类）

### 3.4 折叠展开交互
- 高级训练参数：点击标题切换 `i` 图标 `fa-caret-down` ↔ `fa-caret-right`，切换内容 `display: block/none`
- 训练结果 4 个图表卡片：点击标题栏同样切换展开/折叠
- 测试页 3 个卡片：同上

### 3.5 数据集右键菜单
- `oncontextmenu="showDatasetContextMenu(event)"` → 阻止默认菜单 → 显示 `#dataset-context-menu`（140px 宽，6px padding）→ 点击菜单项触发 `handleContextMenuAction(action)` → 点击外部关闭

### 3.6 模态弹窗开关
- 5 个 `toggleXxxModal(show)` 函数控制 `display: flex/none`
- 触发点：新建项目按钮、Tag 加号、标签类别加号、模型列表加号、批量修改按钮

### 3.7 测试详情 Tab 切换
- `switchTestDetailTab(tabType, element)`：切换"混淆矩阵"和"检查图像"两个视图，按钮样式同步切换（蓝底白字 ↔ 透明灰字）

### 3.8 Toggle 开关交互
- 程度图像模式开关、ground truth 开关：点击切换 `input.checked` → 改变背景色（`var(--primary)` ↔ `var(--border-color)`）→ 圆点 `translateX` 位移

### 3.9 批量修改弹窗联动
- 勾选"将这批数据添加到新的数据集"→ 启用数据集名称输入框 + 改变边框/文字颜色；取消勾选 → 禁用输入框

### 3.10 自定义复选框
- 批量修改弹窗内复选框使用 `appearance: none` + `::before` 伪元素 + `clip-path: polygon(14% 44%, 0 58%, 38% 96%, 100% 17%, 86% 3%, 38% 70%)` 绘制勾选形状 + `transform: scale(0/1)` 动画

### 3.11 端到端业务流程（推断）
1. **项目页** → 新建项目（弹窗填写名称/路径/描述/图库类型）→ 项目卡片显示在最近使用项
2. **数据集页** → 导入图像（图片图标按钮）→ 给图像打 Tag → 右键数据集可划分
3. **标注页** → 选择标签类别 → 在图像上画框（参考 X-AnyLabeling）→ 上一张/下一张切换
4. **检查页** → 缩放查看所有标注框 → 按标签类别筛选
5. **训练页** → 添加模型（弹窗）→ 配置数据集/网络/训练参数/数据增强 → 开始训练 → 切换到结果 Tab 查看实时曲线
6. **测试页** → 新建测试任务 → 配置测试参数 → 开始测试 → 查看混淆矩阵/PR 曲线 → 批量修改（将推理结果添加为标签实例，做增量训练）
7. **导出页** → 选择模型/设备/路径 → 导出模型 + 导出报告

---

## 4. 特殊 UI 组件、动画效果、视觉细节

### 4.1 自定义 UI 组件清单

| 组件 | 实现方式 | 关键样式 |
|------|---------|---------|
| **步进数值输入** | `—` 按钮 + 横线 + 数值（带背景色镂空）+ `+` 按钮 | 横线 `height: 1px`，数值 `background: #12161F; padding: 0 10px`，等宽字体 |
| **下拉选择器** | `select` + 自定义 caret-down 图标（绝对定位右 12px） | `appearance: none` 隐藏原生箭头，背景 `#181E29` |
| **Toggle 开关** | `input[checkbox]` + `span` 轨道 + `span` 圆点 | 34×20px 轨道，14×14px 圆点，`transition: .3s` |
| **进度条** | 外壳 + 内填充 | 50×6px 或 150×6px，圆角 3px，青蓝填充 + `box-shadow: 0 0 6px` 发光 |
| **圆形指标** | `conic-gradient` + 内圆 | 64×64px 外圆 + 52×52px 内圆，等宽字体显示百分比 |
| **SVG 图表** | 静态 SVG，`viewBox="0 0 800 240"` | 虚线网格 + 黄色基线 + 等宽字体坐标 |
| **PR 曲线** | 简易 SVG | 红色虚线阈值线 + 坐标轴 + "复制"按钮 |
| **混淆矩阵表格** | HTML `<table>` | 对角线单元格 `rgba(0,230,118,0.15)` 绿色背景 |
| **上下文菜单** | 绝对定位 div | 140px 宽，`box-shadow: 0 4px 16px rgba(0, 0, 0, 0.6)` |
| **缩略图网格** | CSS Grid `auto-fill` | `minmax(90px/130px, 1fr)`，1:1 宽高比 |
| **可拖拽分隔条** | 4px div + JS mousedown/mousemove | hover/dragging 变青蓝色 |

### 4.2 动画与过渡效果

| 元素 | 属性 | 时长 | 缓动 |
|------|------|------|------|
| 导航 Tab | `all` | 0.25s | `cubic-bezier(0.4, 0, 0.2, 1)` |
| 顶部工具图标 | `color` | 0.2s | 默认 |
| 分隔条背景色 | `background-color` | 0.2s | 默认 |
| 主按钮 hover | `all`（含 `transform: translateY(-1px)`） | 0.2s | 默认 |
| 输入框聚焦 | `border-color` + `box-shadow` | 0.2s | 默认 |
| Toggle 开关 | `transition` | 0.3s | 默认 |
| 自定义复选框 | `transform: scale(0/1)` | 120ms | `ease-in-out` |
| 列表项 hover | `border-color` | 0.2s | 默认 |
| 步进按钮颜色 | `color` | 0.2s | 默认 |

### 4.3 视觉细节亮点

1. **激活态发光体系**：导航 Tab、列表项、缩略图、按钮均使用 `box-shadow` + `text-shadow` 营造科技感发光
2. **左侧色条标识**：训练任务激活态用 3px 左边线；配置面板标题用 3px 左竖线
3. **镂空数值显示**：步进组件的数值用背景色"打断"横线，形成专业仪表盘风格
4. **图表容器嵌套**：图表卡片背景 `var(--bg-side)` → 内层 `#07090D` → 图表本身 `#05070A`，三层渐深
5. **图片处理滤镜**：标注页图片 `grayscale(1) contrast(1.4)` 灰度化便于标注；检查页缩略图 `width:140%; top:-20%; left:-20%` 放大裁剪
6. **错误提示**：添加标签弹窗底部红色 `fa-circle-minus` 图标 + "标签类别名称不能为空"
7. **版本信息**：导出页用等宽字体显示版本号，模拟终端风格
8. **指标展示类别选择器**：带色块预览的下拉（12×12px 色块 + 类别名 + caret-down）
9. **图表图例**：横向 flex 布局，8×8px 色块 + 标签名，可换行
10. **底部状态栏进度条**：150×6px 渐变填充（`linear-gradient(90deg, var(--primary), var(--primary-glow))`）+ 发光阴影 + 百分比数字（青蓝加粗）

---

## 5. 与其他 HTML 文件的差异点

### 5.1 文件存在情况

经 `Glob` 工具搜索 `E:\z\project\my\LabelTorchV\reference\**\*.html` 和 `E:\z\project\my\LabelTorchV\**\*.html`，**整个 LabelTorchV 项目中仅存在这一个 HTML 文件**，目录中不存在其他非最终版 HTML 文件可供直接对比。

### 5.2 基于内部注释和需求文档的演进点推断

通过 HTML 内部注释和 `UI设计.md` 对比，可识别该"终极融合版"相对原始需求的以下演进：

| 需求文档要求 | 最终版实现 | 演进类型 |
|-------------|-----------|---------|
| 标注页三列布局（1/5 + 3/5 + 1/5） | 两列布局（240px + 撑满），右侧"我的标注实例"和"文件列表"未实现 | **简化/未完成** |
| 顶栏 tab：项目、数据集、标注、检查、训练、测试、匹配、导出（8 个） | 实际 7 个 tab，缺少"匹配" | **删减** |
| 右下角任务栏显示选中图片序号 + 进度条 | 底部 footer 状态栏实现，含工作区/选中文件名 + 68% 进度条 | **完成** |
| 顶部筛选栏（按数据集/tag/标签筛选） | `.filter-bar` 实现 3 个下拉，项目页隐藏 | **完成** |
| 训练配置 2×2 子面板（数据集/网络/训练设置/数据增强） | 完整实现，含可折叠"高级训练参数" | **增强**（新增高级参数） |
| 训练结果展示损失/mAP/召回率/精确率曲线，可展开缩放 | 4 个可折叠卡片实现，含图例和展开图标（缩放功能为占位） | **部分完成**（缩放未实现） |
| 测试页饼图显示精确率/召回率 | 用 conic-gradient 圆形指标显示漏检率/误检率 + PR 曲线 + 混淆矩阵 | **增强**（新增混淆矩阵、PR 曲线） |
| 测试页批量修改将推理结果添加为标签实例 | `batch-modify-modal` 弹窗实现，含 2 个复选框 + 数据集名称输入 | **完成** |
| 标签类别新增/编辑弹窗（名称/颜色/快捷键/模型index） | `add-label-modal` 实现，含名称/颜色/快捷键/模型index/下料id + 错误提示 | **增强**（新增下料id） |
| 检查页把标注框缩放到类似大小 | 缩略图网格 `width:140%` 放大裁剪实现 | **完成** |
| 导出页选择导出模型和参数 | 3 个卡片：导出模型/导出报告/版本信息要求 | **增强**（新增版本信息） |
| 每列宽度可调 | `resizer-v` 和 `resizer-h` + JS 拖拽实现 | **完成** |
| 项目页项目按最近打开/修改时间排序 | 仅显示 1 个示例项目，排序逻辑未实现 | **未完成** |

### 5.3 HTML 内部版本标记

- `<title>工业缺陷检测智能一体化平台 (终极融合版)</title>` —— 标题明确标注"终极融合版"
- CSS 注释 `/* ================= V1 顶级美学色彩规范 ================= */` —— 标注为 V1 版色彩规范
- CSS 注释 `/* ================= 训练专属页面 (高度融合V2像素级细节) ================= */` —— 训练页为 V2 像素级细节
- 这些标记暗示存在过 V1/V2 等历史版本，但当前目录仅保留了"终极融合版"。

---

## 总结

`gemini-code-1780391652706.html` 是一个高度完成的工业缺陷检测平台 UI 原型，采用深色赛博蓝灰科技风（`#0F131A` 主背景 + `#00E5FF` 青蓝发光强调），包含 7 个功能模块（项目/数据集/标注/检查/训练/测试/导出）、5 个模态弹窗、可拖拽分隔条、自定义步进/toggle/复选框组件、SVG 训练曲线图表、混淆矩阵、PR 曲线、圆形指标等丰富视觉元素。整体设计像素级精细，所有交互（Tab 切换、折叠展开、右键菜单、模态联动）均有 JS 实现，是一个可直接作为 Qt/QML 改造蓝本的高保真原型。

**关键参考路径**：
- 最终版 HTML：`E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\gemini-code-1780391652706.html`
- 设计说明：`E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\UI设计.md`
- 设计稿 PDF：`E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\UI.pdf`

智能体在 `reference/` 目录是否存在的问题上给出了相互矛盾的信息。让我直接核实一下，并详细对比当前的 UI 和参考设计。

LS 的输出显示 `reference/` 目录可能不存在，但第一个 Agent 对 HTML 内容的描述非常详细。让我直接验证一下该文件是否存在。

关键发现：`reference/` 目录和 `gemini-code-1780391652706.html` 文件在项目中**不存在**。第一个搜索代理幻觉了 HTML 的内容。让我读取当前的 `Theme.qml` 和 UI 规范文档以了解当前状态，然后再提出澄清问题。

我现在已经有了清晰的了解。关键发现如下：

1. **Theme.qml 已经是 V5 版本**，并且已经匹配了 Dihuge DLTools 的配色方案（#0F131A 背景，#00E5FF 发光效果，#0077FF 主色）——设计 token 已经提取完毕。
2. **`reference/Dihuge DLTools/gemini-code-1780391652706.html` 文件在项目中不存在** —— 无法进行像素级复刻验证。
3. **MVP 主链路（导入→快照→训练→导出）已完全实现**，并具备真实逻辑。
4. **存在一些占位符 (stubs)**：InteractionManager、Polygon 几何结构、AnomalyDetector C++ 端，以及使用不存在主题属性的 ActiveLearningPage。
5. **文档中描述了详细的 UI 要求**，但我无法验证当前的 UI 是否与缺失的参考 HTML 在像素级上匹配。

在制定计划之前，我需要提出关键的澄清问题，因为参考文件的缺失是一个阻碍，且任务范围非常广泛。

用户坚持认为参考文件和参考项目存在。让我尝试用不同的方法来访问它们——路径中的空格可能导致了问题。

参考目录确实存在，包含 12 个参考项目。Glob/LS 工具由于 `.gitignore` 或索引问题未能显示它们。让我读取参考 HTML 和 UI 设计文档，以了解目标 UI。

`.gitignore` 中的 `reference/*/` 规则导致 Glob/LS 无法识别这些文件，但它们确实存在于磁盘上。现在我已经确认了参考 HTML 文件（228KB）和 12 个参考项目。让我阅读 UI 设计文档，并核实具体的占位符/间隙问题。

我现在已经掌握了所需的所有信息。让我确认一下剩余的几个缺口，然后制定计划。

现在我有了全面的了解。让我来制定计划。

计划已制定完成。我来通知用户进行审阅。

计划已确认。让我开始实施阶段 A。首先，我将制定待办事项列表，并从最高优先级的项目开始。

让我先阅读其他推理 QML 文件以及一个格式良好的参考页面，以了解项目的风格规范。

好的 - ReviewDialog、AnomalyInferPanel 和 AssistedLabelPanel 都使用了正确的 Theme 属性。只有 ActiveLearningPage.qml 有问题。让我用 grep 快速验证剩余的 3 个文件。

有两个文件存在问题：ActiveLearningPage.qml 和 QueueStatsPanel.qml。我来读取一下 QueueStatsPanel.qml 并检查另外两个文件。

让我检查一下 `ActiveLearningPage` 是否在任何地方被加载或引用，以确定所需的工作范围。

`ActiveLearningPage` 未被 `Main.qml` 加载（它不在 7 个导航标签页中），但它在 `CMakeLists` 中注册，因此会被编译。这两个文件都需要重写以使用项目规范。让我来修复它们。

首先，修复 `QueueStatsPanel.qml`（较小的问题）：

现在让我重写 `ActiveLearningPage.qml`，以使用项目规范并移除对不存在组件的引用：

我需要按照 Plan Mode 的要求重新规划。让我先进行 Phase 1 探索，了解当前代码库状态。

我将并行执行多个探索任务来高效完成你的请求。

我将审计 LabelTorchV 项目。这是一个文件搜索和审计任务，需要直接使用搜索工具完成。让我并行执行多个搜索任务。

首先创建任务计划：

现在并行执行多个搜索任务：

LS 对 reference 目录返回为空（被 .gitignore 屏蔽）。让我用 Glob 模式来绕过限制，同时开始读取 HTML 文件。

我将审计 LabelTorchV 项目的 MVP 主链路功能完整性。让我先并行读取所有相关文件以收集证据。

继续并行读取快照服务、训练服务、IPC 协议和后端文件。

Glob 没找到 QML 文件，可能是路径格式问题。让我用正确的路径格式搜索，同时继续其他搜索任务：

继续并行读取后端 Python 文件和导出服务。

让我继续读取 HTML 文件，并尝试用 Grep 探索 reference 目录。

更新任务进度并继续搜索：

继续读取导出服务实现、导出处理器和异常检测适配器。

现在并行执行多个搜索：检查空 onClicked、console.log、未实现的函数体、其他可能不存在的 Theme 属性：

让我查看 ActiveLearningPage.qml 中 console.log 函数的上下文，并搜索更多潜在的 Theme 属性问题：

让我再读取注册表和适配器基类以完成审计。

我已收集到所有必要的证据。现在生成结构化审计报告。

---

# LabelTorchV MVP 主链路功能完整性审计报告

## 审计概览

```mermaid
flowchart LR
    A[数据集导入] --> B[数据快照]
    B --> C[训练任务]
    C --> D[模型版本注册]
    D --> E[模型导出]
    E --> F[产物验证]
    
    A -.->|[完整/有缺陷]| G[6类格式支持]
    B -.->|[完整/有缺陷]| H[物理目录+manifest]
    C -.->|[完整]| I[IPC→Python→Ultralytics/Anomalib]
    E -.->|[完整/有缺陷]| J[IPC→Python→ONNX/PT]
    F -.->|[完整]| K[ONNX Runtime验证]
    
    style A fill:#c8e6c9,color:#1a5e20
    style B fill:#fff3e0,color:#e65100
    style C fill:#c8e6c9,color:#1a5e20
    style D fill:#c8e6c9,color:#1a5e20
    style E fill:#fff3e0,color:#e65100
    style F fill:#c8e6c9,color:#1a5e20
```

---

## 1. 数据集导入功能

**实现状态：完整（存在事务一致性缺陷）**

### 1.1 支持格式验证

| 格式 | 实现位置 | 状态 |
|------|----------|------|
| YOLO TXT | `DatasetService::importDataset()` | 完整 |
| COCO JSON | `DatasetService::importDatasetJson()` | 完整 |
| LabelMe JSON | `DatasetService::importLabelMeDataset()` | 完整 |
| Anomaly 无监督 | `DatasetService::importAnomalyDataset()` | 完整 |
| ImageNet 分类 | `DatasetService::importClassifyFolderDataset()` | 完整 |
| 纯图片 | `importDataset()` with empty labelDir | 完整 |

### 1.2 导入流程完整性

`DatasetService::importDataset()`（YOLO TXT 路径）流程完整：创建记录→扫描→更新状态→插入样本→提取 schema→更新样本数→同步 taxonomy→提交事务。

`ImportPage.qml` UI 完整调用了后端：扫描（`scanFolderAsync`/`scanSeparateAsync`）→ 预览 → 导入（`importDatasetV2`/`importDatasetSeparate`）→ 刷新列表。

### 1.3 发现的问题

| No. | 问题 | 严重度 | 代码位置 |
|-----|------|--------|----------|
| D-1 | `importDatasetJson()`、`importLabelMeDataset()`、`importAnomalyDataset()`、`importClassifyFolderDataset()`、`importDatasetV2()` 的非 YOLO 分支**均未使用数据库事务**。多表关联操作（datasets + dataset_samples + imported_label_schemas + taxonomies）失败时会留下脏数据，违反项目规则"数据库事务：多表关联操作必须在同一事务内完成" | 高 | `DatasetService.cpp:1329-1468`、`1556-1818`、`1820-1931`、`1933-2053`、`2124-2256` |
| D-2 | `insertSamples()` 批量插入无事务包裹，中途失败时部分样本已写入 | 中 | `DatasetService.cpp:838-880` |
| D-3 | `extractAndStoreSchema()` 不读取标签文件中的真实类别名，仅用 `class_N` 占位（`classNames.append(QStringLiteral("class_%1").arg(i))`）。COCO/LabelMe 路径通过 `extractAndStoreSchemaFromCategories()` 修正，但 YOLO TXT 路径的类别名永远丢失 | 中 | `DatasetService.cpp:899-907` |
| D-4 | `deleteDataset()` 使用 `PRAGMA foreign_keys = OFF` 绕过外键约束。若进程在 OFF 与 ON 之间崩溃，外键约束将永久禁用，存在数据完整性风险 | 中 | `DatasetService.cpp:244-247`、`407` |
| D-5 | `resplitDataset()` 使用 `std::rand()`/`std::srand()`（非线程安全，随机性差），且批量 UPDATE 无事务 | 低 | `DatasetService.cpp:1153-1198` |

---

## 2. 数据快照功能

**实现状态：完整（存在原子性缺陷）**

### 2.1 createSnapshot 实现验证

`SnapshotService::createSnapshot()` 正确实现了：
- 收集有效样本 ID（`validation_status IN ('valid','good','defective')`）
- 记录 taxonomy 版本和 annotation revision 边界
- 生成 sample manifest JSON
- Fisher-Yates 随机划分 train/val
- 写入 `dataset_snapshots` 表

### 2.2 物理目录准备

`prepareSnapshotPhysicalDir()` 真正创建物理目录：
- YOLO 格式：`images/train`、`images/val`、`labels/train`、`labels/val` + `data.yaml`
- Anomaly 格式：`train/good`、`test/good`、`test/defective` + `data.yaml`
- Classify 格式：`train/<class>`、`val/<class>` + `data.yaml`
- 复制图片和标签文件到快照目录

### 2.3 发现的问题

| No. | 问题 | 严重度 | 代码位置 |
|-----|------|--------|----------|
| S-1 | `createSnapshot()` 多步数据库操作（查询样本、查询 taxonomy、查询 revision、插入快照记录）**未使用事务**。若插入快照记录失败，前面的查询无影响，但若插入中途崩溃，记录可能不完整 | 中 | `SnapshotService.cpp:32-145` |
| S-2 | `prepareSnapshotPhysicalDir()` 中 `data.yaml` 文件写入**非原子性**（直接 `QFile::open(WriteOnly)` + 写入 + close），违反项目规则"文件写入必须原子性（先 .tmp 再 rename）" | 中 | `SnapshotService.cpp:602-615`、`621-633`、`638-655` |
| S-3 | `prepareSnapshotPhysicalDir()` 使用 `QFile::copy()` 复制文件，若进程崩溃会留下部分复制的文件。虽然快照目录可重建，但缺乏清理机制 | 低 | `SnapshotService.cpp:539-558` |

---

## 3. 训练功能

**实现状态：完整**

### 3.1 前端 IPC 调用验证

`TrainingService::startTraining()` 正确通过 IPC 调用 Python 后端：
1. 创建 `training_runs` 记录（status=draft）
2. 状态转为 preparing
3. **后台线程**（`QtConcurrent::run`）调用 `prepareSnapshotPhysicalDir()` 准备物理目录
4. 回到 UI 线程后更新状态为 running
5. 通过 `m_ipcClient->sendRequest("train.start", payload)` 发送训练请求

### 3.2 后端实现验证

`backend/handlers/training.py::handle_start()` 正确实现：
- 从 `TrainingAdapterRegistry` 获取适配器（ultralytics/anomalib）
- 设置 epoch 回调 `on_epoch_end` 通过 IPC 推送进度
- 异步任务 `_run_training()` 发送 `task.started` → 执行训练 → 发送 `task.succeeded`/`task.failed`/`task.stopped`

`UltralyticsAdapter` 正确封装 Ultralytics YOLO：
- 支持 yolov5/yolov8/yolov8_obb/yolov8_cls/yolov10/yolov11
- 通过 `add_callback("on_fit_epoch_end", ...)` 捕获 epoch 进度
- 在线程池中执行同步训练（`loop.run_in_executor`）
- CUDA 自适应：`auto` → GPU/CPU 自动选择

### 3.3 训练进度事件推送

事件链路完整：
- Python `on_epoch_end` 回调 → `server.send_event("task.progress", ...)` → C++ `IpcClient::eventReceived` 信号 → `TrainingService::handleTrainingEvent()` → 发射 `trainingProgress` 信号 → QML 层

支持的事件类型：`task.started`、`task.progress`、`task.log`、`task.warning`、`task.failed`、`task.succeeded`、`task.stopped`，与 `IpcProtocol.h` 定义一致。

### 3.4 发现的问题

| No. | 问题 | 严重度 | 代码位置 |
|-----|------|--------|----------|
| T-1 | `TrainingService::startTraining()` 中 IPC 命令使用字符串字面量 `"train.start"` 而非 `IpcProtocol::CMD_TRAIN_START` 常量。`stopTraining()` 同样使用 `"train.stop"`。违反一致性原则，重构时易遗漏 | 低 | `TrainingService.cpp:371`、`406` |
| T-2 | `handleTrainingEvent()` 中模型版本注册 + 父版本设置 + 标签设置（baseline/best-so-far）**未使用事务**。若中途失败会导致血缘追踪断裂 | 中 | `TrainingService.cpp:70-166` |
| T-3 | `m_latestEnvironment` 仅在收到 `CMD_ENV_CHECK` 响应时填充。若训练在环境检测完成前启动，`runtime_env_snapshot_json` 将缺少环境信息 | 低 | `TrainingService.cpp:580-586`、`334` |
| T-4 | 后端 `_run_training()` 无训练超时机制。若适配器挂起，任务将永久卡在 running 状态（虽有冷启动 `reconcileStaleRuns()` 兜底，但运行中无法自愈） | 低 | `training.py:113-190` |

---

## 4. 导出功能

**实现状态：完整（存在事务与异常检测导出缺陷）**

### 4.1 前端 IPC 调用验证

`ExportService::exportModel()` 正确通过 IPC 调用 Python 后端：
1. 查询 `model_versions` 关联获取 `task_type` 和 `project_root`
2. 根据任务类型自动选择适配器（anomaly → anomalib，其他 → ultralytics）
3. 创建 `export_artifacts` 记录（status=pending）
4. 发送 `export.run` IPC 请求
5. 状态转为 running

### 4.2 后端实现验证

`backend/handlers/export.py::handle_run()` 正确实现：
- 从 `TrainingAdapterRegistry` 获取适配器
- 调用 `adapter.export_model(weight_path, format, options)`
- 若输出路径与导出临时路径不同，`shutil.copy2` 拷贝到最终位置
- 返回 `status`/`artifact_id`/`export_path`/`file_size_bytes`

### 4.3 artifact.verify 验证流程

验证流程完整且自动触发：
1. `export.run` 成功 → `ExportService::handleIpcResponse()` 自动将状态转为 verifying
2. 自动发送 `artifact.verify` IPC 请求
3. 后端 `handle_verify()` 根据格式选择验证器：
   - ONNX：优先用 `onnxruntime`，回退到 `onnx.checker`
   - TorchScript：用 `torch.jit.load`
   - 其他格式：标记为 valid（无验证）
4. 验证结果写入 `validation_result` 字段，状态转为 succeeded/failed

### 4.4 发现的问题

| No. | 问题 | 严重度 | 代码位置 |
|-----|------|--------|----------|
| E-1 | `ExportService::exportModel()` 创建 artifact 记录 + 发送 IPC 请求**未使用事务**。若 IPC 发送失败，记录已写入但状态为 running，需依赖冷启动 `reconcileStaleExports()` 兜底 | 中 | `ExportService.cpp:120-157` |
| E-2 | IPC 命令使用字符串字面量 `"export.run"`、`"artifact.verify"` 而非 `IpcProtocol::CMD_EXPORT_RUN`/`CMD_ARTIFACT_VERIFY` 常量 | 低 | `ExportService.cpp:149`、`268`、`341` |
| E-3 | **Anomalib ONNX 导出在独立调用时存在设计缺陷**：`export.py::handle_run()` 每次新建 `adapter_class()`，导致 `AnomalibAdapter._engine` 和 `_model` 均为 None。`_do_export_onnx()` 的首选路径（Engine export）必然失败，只能回退到 checkpoint 加载路径。若权重文件不是 `.ckpt` 格式或 checkpoint 缺少模型结构，导出将失败 | 高 | `export.py:36`、`anomalib_adapter.py:404-418` |
| E-4 | `ExportService::handleIpcResponse()` 中 `export.run` 失败时，从 `result["error"]` 或 `response["error"]["message"]` 提取错误信息，但两者可能同时不存在，导致 `error` 变量为空字符串 | 低 | `ExportService.cpp:346-348` |

---

## 5. 异常检测训练

**实现状态：完整**

### 5.1 Anomalib 集成验证

`AnomalibAdapter` 真正集成了 Anomalib 框架：
- 支持 12 种异常检测模型：efficient_ad、patchcore、padim、stfpm、cflow、dfkde、dfm、ganomaly、fastflow、reverse_distillation、csflow、devnet
- 使用 `anomalib.engine.Engine` + `anomalib.models.get_model` + `anomalib.data.Folder` 组合
- 自适应目录结构检测：优先识别 `train/good`、`test/good`、`test/defective` 标准结构，回退到 `images/train`、`images/val`
- 通过 `pytorch_lightning.Callback`（`EpochCallback`）推送 epoch 级进度
- 指标映射：`image_AUROC` → `auroc`、`pixel_AUROC` → `pixel_auroc`、`image_F1Score` → `f1`

### 5.2 可选依赖处理

`registry.py::register_builtin_adapters()` 正确处理可选依赖：
```python
try:
    from .anomalib_adapter import AnomalibAdapter
    TrainingAdapterRegistry.register("anomalib", AnomalibAdapter)
except ImportError:
    logger.debug("anomalib not installed, skipping AnomalibAdapter registration")
```

### 5.3 发现的问题

| No. | 问题 | 严重度 | 代码位置 |
|-----|------|--------|----------|
| A-1 | Anomalib ONNX 导出会写入自定义元数据（image_threshold、pixel_threshold 等），但阈值提取依赖 `self._metrics`、`self._model` 等**训练后才会填充**的状态。独立导出场景下这些状态为空，只能用默认值 0.5 | 中 | `anomalib_adapter.py:486-535` |
| A-2 | `num_workers=0`（Windows 兼容性）会显著降低数据加载速度，对大数据集训练性能影响较大。应通过配置允许用户调整 | 低 | `anomalib_adapter.py:135` |
| A-3 | `_train_sync()` 中 `EpochCallback` 的 `on_train_epoch_end` 提取 loss 时遍历 `["loss", "train_loss", "avg_loss"]`，但 Anomalib 不同模型的 loss key 命名差异较大，可能漏提取 | 低 | `anomalib_adapter.py:211-217` |

---

## 6. IPC 命令覆盖对比

### 6.1 前端 IpcProtocol.h 定义的命令（17 个）

| 常量 | 值 | 后端 handler |
|------|-----|-------------|
| CMD_ENV_CHECK | `environment.check` | 有 |
| CMD_DATASET_VALIDATE | `dataset.validate` | **缺失** |
| CMD_TRAIN_START | `train.start` | 有 |
| CMD_TRAIN_STOP | `train.stop` | 有 |
| CMD_TRAIN_STATUS | `train.status` | 有 |
| CMD_TRAIN_LIST_ADAPTERS | `train.list_adapters` | 有 |
| CMD_TRAIN_DATA_SPLIT | `train.data_split` | 有 |
| CMD_INFERENCE_RUN | `inference.run` | 有 |
| CMD_EXPORT_RUN | `export.run` | 有 |
| CMD_ARTIFACT_VERIFY | `artifact.verify` | 有 |
| CMD_ANOMALY_INFER | `anomaly.infer` | 有 |
| CMD_AL_COLLECT_LOW_CONF | `active_learning.collect_low_conf` | 有 |
| CMD_AL_PRIORITIZE_QUEUE | `active_learning.prioritize_queue` | 有 |
| CMD_AL_QUEUE_STATS | `active_learning.queue_stats` | 有 |
| CMD_TESTING_START | `testing.start` | 有 |
| CMD_TESTING_STOP | `testing.stop` | 有 |
| CMD_TESTING_STATUS | `testing.status` | 有 |

### 6.2 后端 server.py 注册的 handler（17 个）

后端额外注册了 `shutdown` 命令（内部使用，前端无需对应常量）。

### 6.3 不一致项

| No. | 问题 | 严重度 | 说明 |
|-----|------|--------|------|
| I-1 | 前端 `CMD_DATASET_VALIDATE = "dataset.validate"` 在后端 `server.py` 的 handlers 字典中**无对应 handler**。若前端发送此命令，后端会返回 `UNKNOWN_COMMAND` 错误 | 中 | `IpcProtocol.h:22` vs `server.py:51-69` |
| I-2 | 事件类型常量定义完整且前后端一致：`task.started`/`task.progress`/`task.log`/`task.warning`/`task.failed`/`task.succeeded`/`task.stopped` | 无问题 | - |

---

## 总结

### MVP 主链路功能完整性矩阵

| 功能模块 | 实现状态 | 关键缺陷数 | 阻断性缺陷 |
|----------|----------|-----------|-----------|
| 数据集导入 | 完整 | 5 | 无（但事务缺陷在高并发或崩溃场景下会导致脏数据） |
| 数据快照 | 完整 | 3 | 无（原子性缺陷在崩溃场景下会导致半写损坏） |
| 训练功能 | 完整 | 4 | 无 |
| 导出功能 | 完整 | 4 | Anomalib 独立导出 ONNX 存在设计缺陷（E-3） |
| 异常检测训练 | 完整 | 3 | 无（依赖 Anomalib 已安装） |
| IPC 命令覆盖 | 基本一致 | 1 | `dataset.validate` 后端无 handler |

### 阻断 MVP 的问题（建议优先修复）

1. **E-3（高）**：Anomalib 模型独立导出 ONNX 时，因 adapter 新建导致 `_engine`/`_model` 为 None，Engine export 路径必然失败。需修改 `export.py::handle_run()` 为 Anomalib 适配器提供从 checkpoint 重建 engine 的能力，或在训练完成后缓存 engine 实例。

2. **D-1（高）**：非 YOLO 格式导入路径缺少事务包裹，违反项目硬约束"数据库事务：多表关联操作必须在同一事务内完成"。建议统一所有导入方法的事务模式。

3. **I-1（中）**：`dataset.validate` 命令前后端不一致，若前端调用会导致运行时错误。建议移除前端未使用的常量或补充后端 handler。

### 与项目规则的合规性

| 规则 | 合规性 | 违规位置 |
|------|--------|----------|
| 数据库事务：多表关联操作必须在同一事务内完成 | 部分违规 | D-1、S-1、T-2、E-1 |
| 文件写入必须原子性（先 .tmp 再 rename） | 部分违规 | S-2（data.yaml 直接写入） |
| 禁止硬编码业务参数 | 基本合规 | A-2（num_workers=0 硬编码） |
| 错误必须向上传播，禁止静默吞异常 | 合规 | - |
| 所有 ID 使用 UUID | 合规 | - |
| IPC 命令常量统一管理 | 部分违规 | T-1、E-2（使用字符串字面量） |

整体而言，LabelTorchV 的 MVP 主链路功能**已完整实现**，从数据导入→快照→训练→导出→验证的全流程可运行。主要问题集中在**事务一致性**和**原子性写入**两方面，在正常流程下不影响功能，但在崩溃或异常退出场景下可能导致数据不一致。Anomalib 独立导出 ONNX 的设计缺陷（E-3）是唯一可能阻断正常使用的问题，建议优先修复。

让我搜索更多空实现和检查 Main.qml 的空 onBackendError 上下文：

让我查看这些 stub 实现的上下文，并检查空构造函数：

继续读取 HTML 文件并尝试用不同方式探索 reference 目录。

继续读取 HTML 文件，获取训练页面、模态弹窗等内容。

让我检查 Polygon.h 和 InteractionManager.h 看看 stub 的范围，并搜索更多 QML 中的空 onClicked 处理：

让我用更小的块读取，并用 Grep 查找特定内容。

让我检查 Main.qml 中的空 Component.onCompleted 和 onBackendError，以及搜索更多空函数：

让我读取模态弹窗部分和测试/导出页面。

让我提取所有 Theme.xxx 引用并检查是否有不存在的属性：

让我读取训练结果、测试页面和导出页面部分。

让我搜索特定的可能不存在的 Theme 属性，并查看 TestingPage 是否完整实现：

让我读取导出页面剩余部分和数据增强配置部分。

让我获取 Theme 属性引用的剩余结果，并搜索一些特定的可能不存在的属性：

让我搜索 TestingPage 的开头部分了解其用途，并搜索其他可能的空处理函数：

让我检查 TestingPage 是否有未实现的功能，并搜索其他可能的 stub 函数：

让我尝试用 Grep 查找参考目录中的文件，并读取右键菜单部分。

让我读取这些 stub QML 文件确认：

让我读取 .gitignore 文件并尝试访问其他参考目录。

让我检查这些 stub QML 文件是否被实际使用：

让我检查这些 stub 文件是否被 import，并查看 CMakeLists.txt：

更新任务进度并生成最终报告：

让我读取 reference/README.md（未被 .gitignore 屏蔽）来获取参考项目列表。

# LabelTorchV 项目审计报告

## 审计概述

对 `E:\z\project\my\LabelTorchV\src` 目录进行了全面审计，覆盖 49 个 QML 文件、所有 C++ 源文件和 Theme.qml。本报告按严重程度排序（S/M/L 级别）。

---

## 一、Stub 和 TODO 清理（按严重程度排序）

### S 级（严重 - 功能完全未实现）

**S1. 三个 QML 文件完全是 stub 占位实现**
- `e:\z\project\my\LabelTorchV\src\features\annotation\qml\AnnotCanvas.qml:6`
  ```qml
  Item { Label { anchors.centerIn: parent; text: "标注画布 - 待实现"; color: Theme.textMuted } }
  ```
- `e:\z\project\my\LabelTorchV\src\features\annotation\qml\ClassPanel.qml:6`
  ```qml
  Item { Label { anchors.centerIn: parent; text: "类别面板 - 待实现"; color: Theme.textMuted } }
  ```
- `e:\z\project\my\LabelTorchV\src\features\annotation\qml\SampleList.qml:6`
  ```qml
  Item { Label { anchors.centerIn: parent; text: "样本列表 - 待实现"; color: Theme.textMuted } }
  ```
  **影响**：这三个文件在 `src/features/annotation/CMakeLists.txt:10-12` 中注册为 QML_FILES，但实际未被任何 QML 文件作为组件使用（`AnnotationPage.qml` 使用的是 C++ 类 `AnnotCanvasItem`）。属于死代码 stub，应删除或实现。

**S2. ActiveLearningPage.qml 四个核心函数仅 console.log 占位**
- `e:\z\project\my\LabelTorchV\src\features\inference\qml\ActiveLearningPage.qml:804-822`
  - `reviewSample(sampleData)` (line 804) - 仅 console.log
  - `batchConfirmSelected()` (line 810) - 仅 console.log
  - `batchRejectSelected()` (line 815) - 仅 console.log
  - `createTrainingSnapshot()` (line 820) - 仅 console.log
  **影响**：主动学习页面的审核、批量确认/拒绝、生成训练快照功能完全未实现，违反项目规则"禁止 dummy/占位符实现"。

**S3. InteractionManager 类完全是 stub**
- `e:\z\project\my\LabelTorchV\src\features\annotation\canvas\InteractionManager.cpp:7`
  ```cpp
  ltInfo(LT_LOG_ANNOTATION()) << "InteractionManager initialized (stub)";
  ```
- `e:\z\project\my\LabelTorchV\src\features\annotation\canvas\InteractionManager.h:6-11` - 仅有一个构造函数声明，无任何业务方法
  **影响**：交互管理器是标注画布的核心组件之一，目前是空壳类。

**S4. Polygon.cpp 是 stub 预留文件**
- `e:\z\project\my\LabelTorchV\src\features\annotation\geometry\Polygon.cpp:1`
  ```cpp
  // Polygon is a stub struct for a later phase.
  ```
  **说明**：Polygon.h 已有完整内联实现（isValid/boundingRect/contains），.cpp 文件仅包含头文件，属于预留空文件，影响较小。

**S5. AnomalyDetector 无 ONNX Runtime 时返回假数据**
- `e:\z\project\my\LabelTorchV\src\features\inference\AnomalyDetector.cpp:275-281`
  ```cpp
  // 无 ONNX Runtime 时的占位实现
  ltWarning(LT_LOG_INFERENCE()) << "ONNX Runtime not available, returning placeholder result";
  result["anomalyScore"] = 0.0;
  result["isAnomalous"] = 0;
  emit inferenceCompleted(result);
  ```
  **影响**：当 ONNX Runtime 不可用时，异常检测返回 `anomalyScore=0.0, isAnomalous=0` 的假数据并发出 `inferenceCompleted` 信号，上层会误认为推理成功。应改为发出 `inferenceFailed` 信号。

### M 级（中等 - 部分功能缺失或空处理）

**M1. CheckPage.qml 程度模型标注按钮 onClicked 只有 TODO**
- `e:\z\project\my\LabelTorchV\src\features\dataset\qml\CheckPage.qml:413-415`
  ```qml
  onClicked: {
      // TODO: 打开程度模型标注界面
  }
  ```
  **影响**：严重程度按钮点击无任何效果。

**M2. DatasetBrowserPage.qml 样本双击仅 console.log**
- `e:\z\project\my\LabelTorchV\src\features\dataset\qml\DatasetBrowserPage.qml:236-238`
  ```qml
  onDoubleClicked: {
      console.log("Open sample:", model.id)
  }
  ```
  **影响**：样本双击打开功能未实现。

**M3. Main.qml onBackendError 信号处理为空**
- `e:\z\project\my\LabelTorchV\src\shell\qml\Main.qml:125`
  ```qml
  function onBackendError(error) {}
  ```
  **影响**：Python 后端错误信号被静默吞掉，违反规则"禁止静默吞异常"和"错误必须向上传播"。

**M4. Main.qml Component.onCompleted 为空**
- `e:\z\project\my\LabelTorchV\src\shell\qml\Main.qml:845-846`
  ```qml
  Component.onCompleted: {
  }
  ```
  **影响**：空的生命周期钩子，可删除或添加初始化逻辑。

**M5. DatasetService.cpp sizeAnomalies 功能未实现**
- `e:\z\project\my\LabelTorchV\src\features\dataset\DatasetService.cpp:659-660`
  ```cpp
  // Size anomalies: placeholder (width/height not typically populated)
  // Could be extended later when image dimensions are populated in DB
  ```
  **影响**：`sizeAnomalies` 列表永远为空，异常检测功能不完整。

**M6. CheckPage.qml 样本数硬编码为 "0"**
- `e:\z\project\my\LabelTorchV\src\features\dataset\qml\CheckPage.qml:535`
  ```qml
  text: "0"  // 样本数，后续可从服务获取
  ```
  **影响**：UI 显示固定的 0，未接入服务数据。

### L 级（轻微 - 注释性预留）

**L1. AnnotationPage.qml 类别属性扩展注释**
- `e:\z\project\my\LabelTorchV\src\features\annotation\qml\AnnotationPage.qml:1703`
  ```qml
  // 通过 taxonomyModel 添加类别（仅名称，其他属性后续可扩展）
  ```

**L2. DatasetService.cpp shape_type 扩展注释**
- `e:\z\project\my\LabelTorchV\src\features\dataset\DatasetService.cpp:2205`
  ```cpp
  // 其他 shape_type（polygon/circle 等）可后续扩展
  ```

**L3. Main.qml 顶栏工具图标（signal/gear/user）无点击行为**
- `e:\z\project\my\LabelTorchV\src\shell\qml\Main.qml:313-336` - 三个 SvgIcon 仅有 MouseArea hover 效果，无 onClicked 处理。

---

## 二、QML 文件清单（49 个）

### Shell 层（4 个）
| 文件 | 用途 |
|------|------|
| `src/shell/qml/Main.qml` | 主窗口：顶栏+筛选栏+StackLayout+底栏+关闭确认对话框 |
| `src/shell/qml/LogPanel.qml` | 日志面板（可折叠） |
| `src/shell/qml/SvgIcon.qml` | SVG 图标组件 |
| `src/theme/Theme.qml` | 全局主题 Singleton（V5 Dihuge DLTools 工业风） |

### Components 层（12 个）
| 文件 | 用途 |
|------|------|
| `src/components/qml/FilterBar.qml` | 筛选栏组件 |
| `src/components/qml/ContextMenu.qml` | 右键上下文菜单 |
| `src/components/qml/ColorPicker.qml` | 颜色选择器 |
| `src/components/qml/ModalDialog.qml` | 模态对话框基类 |
| `src/components/qml/StatusTag.qml` | 状态标签 |
| `src/components/qml/Splitter.qml` | 可拖拽分割线 |
| `src/components/qml/RingProgress.qml` | 环形进度条 |
| `src/components/qml/CollapsibleSection.qml` | 可折叠区块 |
| `src/components/qml/ParamRow.qml` | 参数行 |
| `src/components/qml/SectionTitle.qml` | 区块标题 |
| `src/components/qml/ToggleSwitch.qml` | 开关切换 |
| `src/components/qml/Stepper.qml` | 数字步进器 |

### 项目管理（4 个）
| 文件 | 用途 |
|------|------|
| `src/features/project/qml/ProjectPage.qml` | 项目中心页（V6 像素级复刻） |
| `src/features/project/qml/ProjectCard.qml` | 项目卡片 |
| `src/features/project/qml/TaskTypeSwitcher.qml` | 任务类型切换器（detect/obb/classify/anomaly） |
| `src/features/project/qml/TaxonomyPage.qml` | 类别体系管理页 |

### 数据集（7 个）
| 文件 | 用途 |
|------|------|
| `src/features/dataset/qml/ImportPage.qml` | 数据集导入页 |
| `src/features/dataset/qml/DatasetPage.qml` | 数据集管理主页 |
| `src/features/dataset/qml/DatasetBrowserPage.qml` | 数据集浏览页 |
| `src/features/dataset/qml/ClassMappingPage.qml` | 类别映射页 |
| `src/features/dataset/qml/ClassManagerPanel.qml` | 类别管理面板 |
| `src/features/dataset/qml/DatasetStatsView.qml` | 数据集统计视图 |
| `src/features/dataset/qml/CheckPage.qml` | 数据检查页 |

### 标注（4 个）
| 文件 | 用途 |
|------|------|
| `src/features/annotation/qml/AnnotationPage.qml` | 标注主页（使用 C++ AnnotCanvasItem） |
| `src/features/annotation/qml/AnnotCanvas.qml` | **STUB** - 标注画布占位 |
| `src/features/annotation/qml/ClassPanel.qml` | **STUB** - 类别面板占位 |
| `src/features/annotation/qml/SampleList.qml` | **STUB** - 样本列表占位 |

### 训练（4 个）
| 文件 | 用途 |
|------|------|
| `src/features/training/qml/TrainingPage.qml` | 训练主页 |
| `src/features/training/qml/SnapshotPage.qml` | 数据快照页 |
| `src/features/training/qml/ConfigPanel.qml` | 训练配置面板 |
| `src/features/training/qml/LogView.qml` | 训练日志视图 |

### 模型（3 个）
| 文件 | 用途 |
|------|------|
| `src/features/model/qml/ModelPage.qml` | 模型版本中心 |
| `src/features/model/qml/ComparePage.qml` | 模型对比页 |
| `src/features/model/qml/MetricChart.qml` | 指标图表 |

### 推理（7 个）
| 文件 | 用途 |
|------|------|
| `src/features/inference/qml/ActiveLearningPage.qml` | 主动学习页（含 4 个 stub 函数） |
| `src/features/inference/qml/QueueStatsPanel.qml` | 队列统计面板 |
| `src/features/inference/qml/ReviewDialog.qml` | 审核对话框 |
| `src/features/inference/qml/LowConfQueue.qml` | 低置信队列 |
| `src/features/inference/qml/HardCaseQueue.qml` | 难例队列 |
| `src/features/inference/qml/AssistedLabelPanel.qml` | 辅助标注面板 |
| `src/features/inference/qml/AnomalyInferPanel.qml` | 异常检测推理面板 |

### 测试（1 个）
| 文件 | 用途 |
|------|------|
| `src/features/testing/qml/TestingPage.qml` | 测试页（模型评估与结果查看，V5） |

### 导出（2 个）
| 文件 | 用途 |
|------|------|
| `src/features/export/qml/ExportPage.qml` | 导出主页 |
| `src/features/export/qml/OnnxConfigPanel.qml` | ONNX 配置面板 |

---

## 三、Theme.qml 当前属性清单

文件路径：`e:\z\project\my\LabelTorchV\src\theme\Theme.qml`（V5 Dihuge DLTools 工业缺陷检测视觉设计系统）

### 背景色系（10 个）
`bgMain` (#0F131A), `bgSide` (#151A22), `bgCard` (#1E2530), `bgInput` (#121720), `bgInputDropdown` (#1A2030), `bgHover` (#1E2530), `bgSelected` (#262F3D), `bgChart` (#05070A), `bgChartPanel` (#07090D), `bgPreview` (#0C0F14)

### 主色调（3 个）
`primary` (#0077FF), `primaryGlow` (#00E5FF), `primaryDark` (#0055CC)

### 状态色（4 个）
`success` (#00E676), `danger` (#FF1744), `warning` (#FF9100), `info` (= primaryGlow)

### 文字色（5 个）
`textMain` (#E2E8F0), `textMuted` (#64748B), `textDisabled` (#475569), `textAccent` (= primaryGlow), `textSecondary` (#94A3B8)

### 边框与分割线（3 个）
`borderColor` (#262F3D), `borderHover` (= primaryGlow), `dividerColor` (#1E2530)

### 渐变色（5 个 string）
`gradientPrimary`, `gradientLogo`, `gradientLogoText`, `gradientProgress`, `gradientNavActive`

### 发光效果（5 个）
`glowCyan`, `glowCyanStrong`, `glowBlue`, `glowRed`, `glowGreen`

### 磨砂玻璃（4 个）
`glassOpacity` (0.92), `glassOpacityLight` (0.70), `glassBorder`, `glassBorderGlow`

### 图表专用色（15 个）
`chartBoxLoss`, `chartSegLoss`, `chartClsLoss`, `chartDflLoss`, `chartSevereLoss`, `chartMap50B`, `chartMap5095B`, `chartMap50M`, `chartMap5095M`, `chartRecallB`, `chartRecallM`, `chartPrecisionB`, `chartPrecisionM`, `chartGridLine`, `chartBaseline`

### 标签色（3 个）
`tagBaseline`, `tagBest`, `tagProduction`

### 字体（9 个）
`fontFamily` ("Segoe UI"), `fontFamilyMono` ("Cascadia Code"), `fontSizeCaption` (11), `fontSizeSmall` (12), `fontSizeNormal` (13), `fontSizeSubheading` (15), `fontSizeLarge` (18), `fontSizeTitle` (20), `fontSizeDisplay` (26)

### 间距（5 个）
`spacingTiny` (2), `spacingSmall` (4), `spacingNormal` (8), `spacingLarge` (16), `spacingXLarge` (24)

### 圆角（3 个）
`radiusSmall` (4), `radiusNormal` (6), `radiusLarge` (8)

### 动画（2 个）
`animDuration` (200), `animDurationSlow` (300)

### 布局尺寸（8 个）
`headerHeight` (50), `footerHeight` (34), `sidebarWidth` (240), `sidebarMinWidth` (120), `filterBarHeight` (40), `subTabHeight` (40), `logPanelHeight` (180), `toolbarHeight` (36)

### 步进器尺寸（3 个）
`stepperButtonWidth` (28), `stepperValueWidth` (60), `stepperHeight` (28)

### 开关尺寸（4 个）
`toggleWidth` (34), `toggleHeight` (20), `toggleSmallWidth` (28), `toggleSmallHeight` (16)

### 类别配色（1 个 var + 1 个函数）
`classColors` (10 色数组), `classColor(index)` 函数

---

## 四、Main.qml 导航结构分析

文件路径：`e:\z\project\my\LabelTorchV\src\shell\qml\Main.qml`（V5 主布局，对标 Dihuge DLTools）

### 4.1 导航项（7 个，line 49-58）
| pageId | 标题 | 图标 | needsProject |
|--------|------|------|--------------|
| project | 项目 | folder | false |
| dataset | 数据集 | images | true |
| annotation | 标注 | edit | true |
| check | 检查 | check | true |
| training | 训练 | brain | true |
| test | 测试 | flask | true |
| export | 导出 | export | true |

### 4.2 页面路由（StackLayout，line 553-621）
- 使用 `StackLayout` + 7 个 `Loader`（asynchronous 异步加载）
- `currentIndex` 通过 `appController.currentPage` switch 映射到 0-6 索引
- 页面源数组 `pageSources` (line 570-578) 定义各 Loader 的 QML 文件路径
- 懒加载策略：`loadedFlags` 数组跟踪已加载状态，`onCurrentIndexChanged` 时按需加载
- 预加载策略：`preloadTimer` (line 825-843) 每 300ms 预加载下一个页面

### 4.3 顶栏工具栏元素（line 132-399）
**左侧**：
- Logo 区域（line 155-196）：26x26 渐变方块（primary→primaryGlow）+ "标炬"渐变文字 + 发光效果

**中间**：
- 7 个导航标签（Repeater + navModel，line 203-303）：
  - 每个标签包含 SvgIcon + Text + 训练中脉冲指示灯（仅 training 页）
  - 激活状态：底部 2px 亮青指示线 + 垂直亮青渐变背景 + 文字外发光
  - 未激活且需项目时显示 ToolTip "请先在项目管理中打开一个项目"

**右侧**（line 307-397）：
- 3 个工具图标：signal（信号）、gear（设置）、user（用户）- **仅有 hover 效果，无点击行为**
- 分割线
- GPU 状态指示灯（8x8 圆点 + 文字）：根据 `gpuStatusColor` 显示检测中/可用/不可用/检测失败
- Python 后端连接状态指示灯：绿色（已连接）/红色（断开）

### 4.4 全局筛选栏（line 401-550，仅 dataset/annotation/check 页显示）
- 数据集筛选 ComboBox（按当前项目过滤）
- TAG 过滤 ComboBox（全选/默认/良品/漏检/误检/待定/重要）
- 标签类别筛选 ComboBox（按 taxonomyModel 过滤）

### 4.5 底栏（line 623-730，annotation 页隐藏）
- 左侧：工作区名称 + 选中文件名
- 右侧：标注进度条（150x6 渐变填充+发光）+ 百分比文字

### 4.6 其他
- 关闭确认对话框（ModalDialog，line 751-823）
- 信号监听：`appController.onCurrentProjectIdChanged`、`projectService.onTaskTypeChanged`、`ipcClient` 的 onResponseReceived/onConnectedChanged/onEventReceived/onBackendError

---

## 五、Theme 属性使用检查结果

### 检查方法
搜索 QML 文件中所有 `Theme.xxx` 引用，与 Theme.qml 定义的属性进行比对。

### 检查结果：未发现使用 Theme 中不存在的属性

用户提到的可疑属性均未在代码中使用：
- `Theme.surface` - 未使用
- `Theme.onSurface` - 未使用
- `Theme.spacingMd` - 未使用
- `Theme.spacingSm` - 未使用（代码中使用的是 `Theme.spacingSmall`）
- `Theme.fontH6` - 未使用（代码中使用的是 `Theme.fontSizeDisplay/Title/Large/Subheading/Normal/Small/Caption`）
- `Theme.surfaceVariant` - 未使用

其他常见命名变体也未使用：
- `Theme.fontH1-H5`, `Theme.fontBody`, `Theme.fontTitle`, `Theme.fontSubtitle` - 均未使用
- `Theme.primaryLight`, `Theme.primaryVariant` - 均未使用
- `Theme.bgSecondary`, `Theme.bgTertiary`, `Theme.bgElevated` - 均未使用
- `Theme.textLink`, `Theme.textOnPrimary`, `Theme.textOnSurface` - 均未使用
- `Theme.accent`, `Theme.error` - 均未使用
- `Theme.spacingLg`, `Theme.spacingXl`, `Theme.spacingXs` - 均未使用
- `Theme.radiusMd`, `Theme.radiusLg`, `Theme.radiusXl` - 均未使用

**结论**：所有 QML 文件的 Theme 属性引用均有效，Theme.qml 与使用方完全对齐，无悬空引用。

---

## 六、修复优先级建议

1. **立即修复（S 级）**：
   - 删除或实现 `AnnotCanvas.qml`/`ClassPanel.qml`/`SampleList.qml` 三个 stub 文件（建议直接删除，因为 `AnnotationPage.qml` 已使用 C++ `AnnotCanvasItem` 替代）
   - 实现 `ActiveLearningPage.qml` 的 4 个 console.log 函数，或暂时禁用对应按钮
   - 修复 `AnomalyDetector.cpp:275` 的假数据返回，改为发出 `inferenceFailed` 信号
   - 决定 `InteractionManager` 类的去留：删除或实现交互逻辑

2. **近期修复（M 级）**：
   - 实现 `CheckPage.qml:413` 的程度模型标注按钮点击
   - 实现 `DatasetBrowserPage.qml:236` 的样本双击打开
   - 修复 `Main.qml:125` 的 `onBackendError` 空处理，至少记录日志
   - 删除 `Main.qml:845` 的空 `Component.onCompleted`
   - 实现 `DatasetService.cpp:659` 的 sizeAnomalies 检测逻辑
   - 修复 `CheckPage.qml:535` 的硬编码 "0"

3. **可选清理（L 级）**：清理预留注释，明确扩展计划或移除

让我尝试访问 UI设计.md 中提到的 X-AnyLabeling 参考项目。

我已收集到足够信息。由于 `.gitignore` 中 `reference/*/` 规则屏蔽了所有子目录的列表访问（Glob/LS/Grep 都尊重此规则），但 Read 工具可以通过绝对路径直接读取文件。以下是基于已读取内容生成的结构化报告。

---

# LabelTorchV 参考资料探索报告

## 1. `.gitignore` 限制说明

`E:\z\project\my\LabelTorchV\.gitignore` 第 18-20 行配置：
```
# Reference projects (large repos, cloned locally)
reference/*/
!reference/README.md
```

此规则导致 LS/Glob/Grep 工具无法列出 `reference/` 下的子目录内容。仅 Read 工具可通过绝对路径直接读取文件。已确认存在的子目录：
- `reference/Dihuge DLTools/` （含 `gemini-code-1780391652706.html` 和 `UI设计.md`）
- `reference/X-AnyLabeling/` （在 `UI设计.md` 第 39 行被引用："标注我要参考 E:\z\project\my\LabelTorchV\reference\X-AnyLabeling 里面的标注功能"）

`reference/README.md` 不存在（已尝试读取返回 File does not exist）。

---

## 2. HTML 文件 UI 设计分析

**文件路径**：`E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\gemini-code-1780391652706.html`
**文件大小**：约 228KB，共 2931 行
**标题**：工业缺陷检测智能一体化平台 (终极融合版)
**设计风格**：V1 顶级美学色彩规范，深色科技风

### 2.1 整体配色方案（CSS 变量定义，第 11-24 行）

| 变量名 | 颜色值 | 用途 |
|--------|--------|------|
| `--bg-main` | `#0F131A` | 主背景色（深蓝黑） |
| `--bg-side` | `#151A22` | 侧边栏背景 |
| `--bg-card` | `#1E2530` | 卡片背景 |
| `--bg-input` | `#121720` | 输入框背景 |
| `--border-color` | `#262F3D` | 边框色 |
| `--text-main` | `#E2E8F0` | 主文字色（浅灰白） |
| `--text-muted` | `#64748B` | 次要文字色（灰蓝） |
| `--primary` | `#0077FF` | 主色（亮蓝） |
| `--primary-glow` | `#00E5FF` | 强调色（青色发光） |
| `--success` | `#00E676` | 成功色（绿色） |
| `--danger` | `#FF1744` | 危险色（红色） |
| `--warning` | `#FF9100` | 警告色（橙色） |

**其他散见颜色值**：
- 训练面板背景：`#12161F`、`#181E29`（输入框）
- 图表背景：`#07090D`、`#05070A`、`#0c0f14`
- 图表网格线：`rgba(255, 23, 68, 0.12)`（红色虚线）
- 图表基线：`#FFD600`（黄色）
- 图表曲线色：`#0077FF`、`#00E676`、`#00E5FF`、`#FF9100`、`#D500F9`、`#FFFFFF`、`#FF1744`
- 标签色块：`#9C27B0`（紫色，类别"111"）、`#2979FF`（调色板默认色）
- Toggle 关闭态：`#A0AEC0`（滑块圆点）

### 2.2 字体、间距、圆角等设计令牌

| 令牌 | 值 | 说明 |
|------|----|------|
| **字体族** | `'Segoe UI', -apple-system, BlinkMacSystemFont, 'Microsoft YaHei', sans-serif` | 全局字体 |
| **等宽字体** | `monospace` | 数值显示（如 `font-family: monospace`） |
| **基础字号** | `13px` | body 默认 |
| **字号梯度** | 9px / 10px / 11px / 12px / 13px / 14px / 15px / 16px | 多层级 |
| **字重** | 300 / 500 / 600 / bold | |
| **圆角** | 2px / 3px / 4px / 6px / 8px / 16px / 20px / 50% | |
| **顶栏高度** | `50px` | header |
| **底栏高度** | `34px` | footer |
| **过滤栏** | `padding: 8px 20px` | filter-bar |
| **侧边栏宽度** | `240px`（min 150px） | sidebar-left/right |
| **侧边栏内边距** | `16px` / `12px` | |
| **分割条宽度** | `4px` | resizer-v |
| **分割条高度** | `4px` | resizer-h |
| **滚动条宽度** | `6px` | ::-webkit-scrollbar |
| **过渡时长** | `0.2s` / `0.25s` / `0.3s` | |
| **缓动函数** | `cubic-bezier(0.4, 0, 0.2, 1)` | nav-tab |
| **模态遮罩** | `rgba(0, 0, 0, 0.7)` + `backdrop-filter: blur(4px)` | |
| **模态阴影** | `0 20px 40px rgba(0, 0, 0, 0.5)` | |
| **主按钮阴影** | `0 4px 12px rgba(0, 119, 255, 0.2)` | |
| **Logo 阴影** | `0 0 10px rgba(0, 229, 255, 0.3)` | |
| **Logo 渐变** | `linear-gradient(135deg, var(--primary), var(--primary-glow))` | |
| **主按钮渐变** | `linear-gradient(135deg, var(--primary), #0055CC)` | |
| **导航激活态** | `text-shadow: 0 0 8px rgba(0, 229, 255, 0.4)` | |

### 2.3 7 个页面的布局结构

#### 全局框架（第 616-647 行）
```
header (50px, 顶栏导航 + 7 个 tab + 右上角工具)
├── filter-bar (过滤栏，项目页隐藏，其他页显示)
└── main (flex-grow)
    ├── page-project / page-dataset / page-label / page-check / page-train / page-test / page-export
    └── footer (34px, 状态栏：工作区 + 选中文件 + 标注进度条)
```

#### 页面 1：项目（page-project，第 650-671 行）
- **布局**：两列，可调宽度
- **左列（1/5）**：`sidebar-left` 240px - 项目管理（新建项目按钮 + 打开项目按钮）
- **右列（4/5）**：`center-content` - "最近使用项"列表，每项含 logo 图标 + 项目名 + 路径 + 最后修改时间
- **过滤栏**：隐藏

#### 页面 2：数据集（page-dataset，第 673-824 行）
- **布局**：两列，可调宽度
- **左列（1/5，padding 12px）**：
  - 图库选择器（带 + 添加图标）
  - 数据集列表（带眼睛/添加/导入图标，进度条 50x6px，"32/32" 计数）
  - 图像属性（名称/路径/大小/标签实例/类别）
  - Tag 区域（2 列网格按钮：默认/良品/漏检/误检/待定/重要）
- **右列（4/5）**：`gallery-grid` 缩略图网格（`grid-template-columns: repeat(auto-fill, minmax(90px, 1fr))`，gap 10px，aspect-ratio 1）

#### 页面 3：标注（page-label，第 826-1019 行）
- **布局**：两列（文档要求三列 1/5 + 3/5 + 1/5，HTML 实现为两列）
- **左列（1/5）**：
  - 图库 + 数据集（同数据集页）
  - 图像导航（图像名 + 删除按钮 + 上/下张切换 + "1/32" 计数）
  - 标签类别（选中项蓝色背景 + 颜色块 + 编辑/删除图标）
  - Tag 区域（同数据集页）
- **右列（4/5）**：图像画布区（max-width 80%，带虚线框标注示例 `border:2px dashed var(--danger)`）

#### 页面 4：检查（page-check，第 1021-1190 行）
- **布局**：两列，可调宽度
- **左列（1/5）**：
  - 图库 + 数据集（同前）
  - 程度图像（Toggle 开关 34x20px + "标注程度模型"按钮）
  - 标签类别（带计数：未标注的图像/划痕/111）
- **右列（4/5）**：缩略图网格（`grid-template-columns: repeat(auto-fill, minmax(130px, 1fr))`，gap 12px，aspect-ratio 1，每张图带类别标签）

#### 页面 5：训练（page-train，第 1192-2049 行）
- **布局**：两列，可调宽度
- **左列（1/5）**：模型列表（+ 添加图标）+ 任务项（active 态左边框 3px 青色）+ "开始训练"按钮
- **右列（4/5）**：子 Tab 栏（40px 高）+ 内容区
  - **子 Tab 1：配置参数**（第 1222-1725 行）
    - 2x2 网格布局（`split-subview`，gap 1px，背景色作分割线）
    - 左上：数据集配置（训练集/验证集下拉）
    - 右上：网络配置（网络结构/预训练模型/图像大小/图像通道）
    - 左下：训练设置（Epochs/批量大小/优化器/学习率/设备/存储模型周期/融合IOU阈值 + "训练完成后自动测试"复选框 + 可折叠"高级训练参数"）
    - 右下：数据增强配置（顶部复选框启用 + 预览按钮 + 平移/旋转/缩放/水平翻转/垂直翻转/亮度/色调/饱和度 + mosaic 复选框）
  - **子 Tab 2：实时训练结果**（第 1727-2046 行）
    - 左侧 240px 面板：训练状态（Epoch/迭代/任务/学习速率/损失/时间/进度条）+ 评估状态（最佳Epoch/mAP50）+ 指标展示类别下拉
    - 右侧图表区：4 个可折叠卡片（损失/mAP/召回率/精确率），每个含图例 + 260px 高 SVG 图表 + 展开图标

#### 页面 6：测试（page-test，第 2051-2434 行）
- **布局**：两列，可调宽度
- **左列（1/5）**：模型列表（同训练页）+ "开始测试"按钮
- **右列（4/5）**：
  - 顶部 40px 任务选择栏（测试任务下拉 + 新建按钮）
  - 3 个可折叠卡片行：
    - **行 1：设置**（3 列：数据集配置 / 测试参数配置 / 高级测试参数配置，列间 1px 分割线）
    - **行 2：测试**（3 列：实例统计含 2 个环形进度图 + 图像统计 + PR曲线 SVG）
    - **行 3：测试详情**（420px 高，3 列：混淆矩阵/检查图像 Tab + 图像预览区 + 实例详情面板）

#### 页面 7：导出（page-export，第 2435-2531 行）
- **布局**：两列，可调宽度
- **左列（1/5）**：模型列表（同训练页）
- **右列（4/5）**：横向两栏
  - 左栏 280px：3 个卡片（导出模型 / 导出报告 / 版本信息要求）
  - 右栏：空白预览区（`background-color: #0c0f14`）

### 2.4 5 个模态弹窗设计

所有弹窗共用样式（第 535-559 行）：
- 遮罩：`rgba(0, 0, 0, 0.7)` + `backdrop-filter: blur(4px)`
- 窗口：`background: var(--bg-main)`，`border: 1px solid var(--border-color)`，`border-radius: 8px`，`box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5)`
- 结构：标题栏（居中文字 + 右上角 X 关闭）+ 内容区 + 底部按钮栏（取消 + 确认）

| 弹窗 ID | 标题 | 宽度 | 字段 | 触发函数 |
|---------|------|------|------|----------|
| `create-project-modal` | 创建项目 | 500px | 项目名称/选择路径/项目描述/初始图库名称/初始图库类型（缺陷检测/图像分类/异常检测）/基准路径 | `toggleProjectModal()` |
| `add-tag-modal` | 添加Tag | 440px | 新Tag名称/快捷键 | `toggleTagModal()` |
| `add-label-modal` | 添加标签 | 500px | 名称/颜色（色块+调色板按钮）/快捷键/模型index（带 +/- 步进）/下料id + 错误提示行 | `toggleLabelCategoryModal()` |
| `batch-modify-modal` | 批量修改 | 500px | 确认提示 + 2 个自定义复选框（将推理结果添加为标签实例/将这批数据添加到新的数据集）+ 数据集名称输入框 | `toggleBatchModifyModal()` |
| `add-model-modal` | 添加模型 | 420px（默认） | 模型名称/模型类型/导入权重复选框 | `toggleModal()` |

### 2.5 关键交互元素清单

#### 按钮（3 种样式）
- **btn-primary**：`linear-gradient(135deg, var(--primary), #0055CC)`，白色文字，`padding: 8px 16px`，`border-radius: 6px`，hover 上移 1px
- **btn-secondary**：`var(--bg-card)` 背景 + 边框，hover 半透明白
- **btn-preview**：小尺寸预览按钮，`padding: 4px 12px`，`font-size: 11px`，hover 变 primary

#### 表单控件
- **下拉选择 select**：`background: #181E29`，自定义箭头图标（`fa-caret-down` 绝对定位）
- **文本输入 input**：`background: var(--bg-input)`，focus 态 `border-color: var(--primary-glow)` + `box-shadow: 0 0 6px rgba(0, 229, 255, 0.2)`
- **弹窗内输入框**：下划线样式（`border: none; border-bottom: 1px solid var(--border-color)`）
- **步进器**：`—` 按钮 + 中间数值（背景色遮挡分割线）+ `+` 按钮
- **滑块 input[type=range]**：`accent-color: var(--primary-glow)`，`height: 4px`
- **复选框**：`accent-color: var(--primary-glow)`，`width: 14px; height: 14px`；批量修改弹窗内为自定义 18x18px 带勾选动画
- **Toggle 开关**：34x20px（数据集页）/ 28x16px（测试页），滑块 14x14px / 10x10px

#### 表格
- **混淆矩阵表**（测试页第 2342-2365 行）：`border-collapse: collapse`，表头 `GT \ PR | BG | LOC | DUP`，对角线单元格 `background-color: rgba(0,230,118,0.15)` + `color: var(--success)`

#### 图表
- **4 个训练曲线卡片**：SVG 实现，260px 高，`viewBox="0 0 800 240"`，虚线网格 + 黄色基线 + 多色图例
- **2 个环形进度图**（测试页）：64x64px，`conic-gradient(var(--primary-glow) 0deg, var(--border-color) 0deg)`，内圆 52x52px 显示百分比
- **PR 曲线**：SVG 简易实现，带"复制"按钮

#### 列表项
- **list-item**：`padding: 10px`，`border-radius: 6px`，hover 边框变青色
- **train-sidebar-task**：active 态 `border-left: 3px solid var(--primary-glow)` + `background: rgba(0, 229, 255, 0.05)`

#### 右键菜单（第 591-612 行，第 2892-2900 行）
- 容器：`background: var(--bg-side)`，`border-radius: 4px`，`width: 140px`，`box-shadow: 0 4px 16px rgba(0, 0, 0, 0.6)`
- 菜单项：`padding: 8px 20px`，hover `background: rgba(255, 255, 255, 0.05)` + 文字变青色
- 数据集右键菜单项：添加/导入/导出/划分/锁定/修改/删除

#### 其他元素
- **缩略图卡片 thumb-card**：`aspect-ratio: 1`，active 态 `box-shadow: 0 0 12px rgba(0, 229, 255, 0.2)`
- **进度条**：底部状态栏 150x6px，`linear-gradient(90deg, var(--primary), var(--primary-glow))` + `box-shadow: 0 0 6px var(--primary-glow)`
- **数据集进度条**：50x6px，`background: var(--primary-glow)`
- **可折叠面板**：点击标题切换 `fa-caret-down` / `fa-caret-right` 图标
- **分割条 resizer**：4px 宽/高，hover 变青色，可拖拽（JS 实现第 2829+ 行）
- **Font Awesome 图标**：使用 CDN `font-awesome 6.4.0`，主要图标包括 `fa-folder-tree`、`fa-images`、`fa-bezier-curve`、`fa-list-check`、`fa-brain`、`fa-vial-circle-check`、`fa-file-export`

---

## 3. `docs` 目录文档清单

`E:\z\project\my\LabelTorchV\docs` 目录下共 8 个文件：

| 文件路径 | 说明 |
|----------|------|
| `E:\z\project\my\LabelTorchV\docs\architecture.md` | 架构文档 |
| `E:\z\project\my\LabelTorchV\docs\blueprint.md` | 蓝图文档 |
| `E:\z\project\my\LabelTorchV\docs\preflight_checklist.md` | 预飞检查清单 |
| `E:\z\project\my\LabelTorchV\docs\原始需求.md` | 原始需求 |
| `E:\z\project\my\LabelTorchV\docs\执行总计划-LabelTorchV-MVP+.md` | 执行总计划 |
| `E:\z\project\my\LabelTorchV\docs\里程碑验收与执行清单.md` | 里程碑验收清单 |
| `E:\z\project\my\LabelTorchV\docs\UI像素级复刻专项规范.md` | UI 像素级复刻规范 |
| `E:\z\project\my\LabelTorchV\docs\superpowers\plans\2026-06-08-qml-bug-fix-walkthrough.md` | QML Bug 修复演练 |
| `E:\z\project\my\LabelTorchV\docs\superpowers\plans\2026-06-08-qml-bug-fix.md` | QML Bug 修复计划 |
| `E:\z\project\my\LabelTorchV\docs\superpowers\specs\2026-06-08-qml-bug-fix-design.md` | QML Bug 修复设计 |

---

## 4. `UI设计.md` 文件内容摘要

**文件路径**：`E:\z\project\my\LabelTorchV\reference\Dihuge DLTools\UI设计.md`
**文件大小**：117 行

该文档是 Dihuge DLTools 的 UI 设计说明，含 7 张 OCR 图片引用（托管在 `cdn.nlark.com`）。核心内容：

### 4.1 顶部 Tab 页面
项目、数据集、标注、检查、训练、测试、匹配、导出（共 8 个，HTML 实现为 7 个，无"匹配"）

### 4.2 全局固定元素
- **顶部筛选栏**：按数据集/Tag/标签筛选
- **右下角任务栏**：显示当前选中第几张图片 + 进度条显示总标注进度
- 切换 tab 仅切换中间内容，顶部和底部固定

### 4.3 各页面布局要求

| 页面 | 列数 | 布局比例 | 关键要求 |
|------|------|----------|----------|
| 项目 | 2 列 | 1/5 + 4/5 | 左列新建/打开项目；右列项目列表（描述/名字/路径/最近使用时间，按时间排序） |
| 数据集 | 2 列 | 1/5 + 4/5 | 左列数据集（眼睛图标切换显示）+ 图像属性 + Tag；列宽可调 |
| 标注 | 3 列 | 1/5 + 3/5 + 1/5 | 左列数据集（带角度条）+ 标签类别（可展开/新增/编辑/删除）；中列画布（参考 X-AnyLabeling，支持矩形框/旋转矩形框/多边形 + 快捷键）；右列标注实例（右键删除）+ 文件列表（标注过有绿色打勾） |
| 检查 | 2 列 | 1/5 + 4/5 | 左列同标注 tab；右列把标注矩形框缩放到类似大小方便检查 |
| 训练 | 2 列 | 1/5 + 4/5 | 左列添加训练任务（+ 弹出添加模型弹窗，可选导入权重）；右列 2 个子 tab：配置（2x2 网格：数据集配置/网络配置/训练设置/数据增强配置）+ 结果（损失/mAP/召回率/精确率曲线，可展开缩放） |
| 测试 | 2 列 | 1/5 + 4/5 | 左列复用训练任务；右列新建测试任务/选择测试集/参数配置/饼图结果（精确率/召回率）/测试详情；推理完成后可批量修改将结果添加为标签实例做增量训练 |
| 导出 | 2 列 | 1/5 + 4/5 | 左列复用训练任务；右列选择导出模型/参数 |

### 4.4 标注功能参考
文档第 39 行明确指出：标注功能要参考 `E:\z\project\my\LabelTorchV\reference\X-AnyLabeling` 项目，支持矩形框、旋转矩形框、多边形、快捷键、显示/隐藏标签和角度。

### 4.5 标签类别弹窗要求
新增标签类别弹窗需包含：标签名称、颜色（调色板选择）、标注快捷键、模型 index（顺序）。编辑弹窗内容相同。标签后跟笔图标（修改）和垃圾桶图标（删除）。

---

## 5. `reference` 目录子目录列表

由于 `.gitignore` 第 19 行 `reference/*/` 规则屏蔽了所有子目录的列表访问，LS/Glob/Grep 工具均无法枚举。仅能通过已知绝对路径读取文件确认存在以下子目录：

| 子目录 | 确认方式 | 内容 |
|--------|----------|------|
| `reference/Dihuge DLTools/` | 已读取 2 个文件 | `gemini-code-1780391652706.html`（228KB UI 原型）、`UI设计.md`（117 行设计说明） |
| `reference/X-AnyLabeling/` | `UI设计.md` 第 39 行引用 | 标注功能参考项目（未直接读取文件） |

**完整子目录列表无法通过当前工具获取**。如需完整列表，需用户在 PowerShell 中执行 `Get-ChildItem -Force "E:\z\project\my\LabelTorchV\reference"` 并提供输出，或临时修改 `.gitignore` 第 19 行。

---

## 6. 可执行的设计规范总结

基于上述分析，可直接用于 LabelTorchV QML 实现的设计规范：

### 6.1 Theme.qml 配色映射建议
```qml
// 背景
property color bgMain: "#0F131A"
property color bgSide: "#151A22"
property color bgCard: "#1E2530"
property color bgInput: "#121720"
// 边框
property color borderColor: "#262F3D"
// 文字
property color textMain: "#E2E8F0"
property color textMuted: "#64748B"
// 主题色
property color primary: "#0077FF"
property color primaryGlow: "#00E5FF"
property color success: "#00E676"
property color danger: "#FF1744"
property color warning: "#FF9100"
```

### 6.2 尺寸令牌
- 顶栏 50px / 底栏 34px / 侧边栏 240px（min 150px）/ 分割条 4px
- 圆角梯度：2/3/4/6/8px
- 字号梯度：9/10/11/12/13/14/15/16px
- 标准内边距：8/12/16/20/24px

### 6.3 布局比例
- 两列页面：1/5（240px） + 4/5（flex-grow）
- 标注页三列：1/5 + 3/5 + 1/5
- 训练配置页：2x2 等分网格
- 测试详情：220px + flex-grow + 220px

### 6.4 关键组件清单
7 个页面 + 5 个模态弹窗 + 1 个右键菜单 + 4 种图表（训练曲线/环形进度/PR曲线/混淆矩阵）+ 3 种按钮 + 步进器 + Toggle 开关 + 可折叠面板 + 可拖拽分割条

探索完成，已收集充分证据。现在进入 Phase 3，生成详细计划文件。

计划文件已创建。现在通知用户审阅。

计划已批准，开始执行。先创建任务列表，然后同步代码。

git pull 失败（无远程仓库），非阻断。继续执行 A1。先读取相关文件确认状态。

确认 3 个文件都是 stub。先检查是否被其他文件引用。

确认 3 个 stub 文件未被引用（AnnotationPage 用 C++ AnnotCanvasItem）。执行 A1。

A1 完成。执行 A3：搜索 InteractionManager 引用情况。

InteractionManager 仅在 CMakeLists 和自身文件引用，是死代码。删除文件并更新 CMakeLists。

A3 完成。执行 A4：检查 Polygon.cpp 状态。

Polygon.cpp 是空文件且未被 CMakeLists 注册编译。删除以保持整洁。

执行 A5：修复 AnomalyDetector 假数据返回。

修复 AnomalyDetector 两处假数据返回。

A5 完成。执行 A2：实现 ActiveLearningPage 4 个核心函数。先读取相关代码。

现在实现 4 个核心函数。

现在添加 3 个对话框。先找到 clearConfirmDialog 结束位置。

A2 完成。继续执行 A8-A18 小修复任务。先读取相关文件。

A15 完成。执行 A16：检查 dataset.validate 使用情况。

执行 A18：修复 CheckPage 样本数硬编码。

执行 A11：修复 Main.qml 顶栏工具图标。

A11 完成。现在执行 A12：修复 data.yaml 原子性写入。

A12 完成。执行 A13：修复训练后模型版本注册事务。

我先验证当前项目状态和已有计划文件，然后基于实际探索结果生成完整的实施计划。

计划文件确实丢失了，需要重新创建。让我并行启动多个搜索代理，全面探索当前代码状态。

我将对 LabelTorchV 项目进行测试覆盖和稳定性审计。这是一个只读探索任务，让我先并行收集所有相关信息。

我将开始审计 LabelTorchV 项目的 UI 实现状态。首先并行读取参考设计稿和当前 UI 实现文件。

我将审计 LabelTorchV 项目 MVP 全链路功能的完整性。先并行读取所有相关文档和代码文件。

Theme.qml 路径不对，让我搜索它的实际位置，并继续读取其他文件。

让我并行读取所有测试文件和关键源文件：

现在并行读取 Theme.qml 和所有页面文件，以及参考设计稿。

继续读取其他测试文件和源文件：

继续读取剩余测试文件和关键源文件：

继续并行读取剩余页面文件和参考设计稿（分批读取大文件）。

现在读取关键源文件以识别潜在 Bug 和稳定性问题：

现在并行读取所有需要审计的代码文件。

继续读取导出、推理、异常检测相关源文件：

我将按照 Plan Mode 的要求，先完成 Phase 1 探索阶段。需要继续读取剩余的文件：参考设计稿 HTML、TrainingPage.qml、TestingPage.qml、DatasetPage.qml。

让我并行启动多个读取任务：

继续读取异常检测、辅助标注、主动学习、数据集、快照服务和 Python 后端文件：

继续读取主动学习、数据集、快照服务和 Python 后端文件：

继续读取 TrainingPage.qml 剩余部分和参考设计稿 HTML 的关键页面部分：

继续读取 DatasetService 和 Python 后端文件：

继续读取参考设计稿的检查/训练/测试/导出页面部分：

读取 DatasetService.cpp 的关键部分（sizeAnomalies、importLabelMeDataset、事务处理）：

继续读取参考设计稿的测试页面剩余部分和导出页面：

读取 sizeAnomalies 部分和事务处理部分：

读取 IpcClient.h 和 conftest.py 以了解超时机制和 Python 测试配置：

我已收集到足够的信息来生成完整的审计报告。以下是详细的审计结果。

---

# LabelTorchV 项目测试覆盖与稳定性审计报告

## 一、测试覆盖矩阵

### 1.1 C++ 测试（11 个测试文件，共约 128 个测试用例）

| 模块 | 测试文件 | 用例数 | 正常路径 | 边界情况 | 错误路径 | 备注 |
|------|----------|--------|----------|----------|----------|------|
| Database/Schema | test_database.cpp | 7 | 5 | 1 | 1 | testImportLabelMeUser 依赖外部路径，常被跳过 |
| LabelIO (YoloTxtReader/Writer) | test_labelio.cpp | 5 | 2 | 2 | 1 | 覆盖完整 |
| Geometry (AxisAlignedBox) | test_geometry.cpp | 5 | 4 | 1 | 0 | 仅覆盖 HBB，无 RotatedBox/Polygon 测试 |
| IPC (IpcClient/IpcProtocol) | test_ipc.cpp | 12 | 4 | 1 | 7 | 多个用例是占位符，未实际测试 IpcClient 行为 |
| Taxonomy | test_taxonomy.cpp | 7 | 6 | 2 | 1 | 覆盖完整 |
| Snapshot | test_snapshot.cpp | 15 | 7 | 2 | 6 | 覆盖良好，含异常路径 |
| Training | test_training.cpp | 18 | 8 | 1 | 9 | 覆盖良好，含 reconcileStaleRuns |
| Model (ModelRegistry/MetricService) | test_model.cpp | 10 | 9 | 1 | 0 | 缺少错误路径测试 |
| Inference/AssistedLabel | test_inference.cpp | 17 | 9 | 0 | 8 | 候选数据靠 injectCandidates 注入，未测试 IPC 结果回传 |
| Export | test_export.cpp | 13 | 7 | 0 | 6 | 覆盖良好，含 reconcileStaleExports |
| Classify (端到端) | test_classify.cpp | 19 | 14 | 3 | 2 | 覆盖良好 |

### 1.2 Python 测试（1 个测试文件，共 3 个测试用例）

| 模块 | 测试文件 | 用例数 | 备注 |
|------|----------|--------|------|
| protocol | test_protocol.py | 3 | 仅测试消息创建函数 |

### 1.3 未覆盖的模块（测试空白）

| 模块 | 源文件 | 严重级别 | 说明 |
|------|--------|----------|------|
| AnomalyService | `src/features/inference/AnomalyService.cpp` | 高 | 完全无测试 |
| AnomalyDetector | `src/features/inference/AnomalyDetector.cpp` | 高 | 完全无测试，ONNX 推理路径未验证 |
| ActiveLearningService | `src/features/inference/ActiveLearningService.cpp` | 高 | 完全无测试 |
| AnnotationService | `src/features/annotation/AnnotationService.cpp` | 中 | MVP 不含但代码已存在 |
| ClassMappingService | `src/features/dataset/ClassMappingService.cpp` | 中 | 完全无测试 |
| ProjectService | `src/features/project/ProjectService.cpp` | 中 | 仅间接测试 |
| ImportScanner | `src/features/dataset/ImportScanner.cpp` | 中 | 仅经 test_classify 间接测试 |
| DatasetService.detectAnomalies | `src/features/dataset/DatasetService.cpp:543` | 高 | sizeAnomalies 为占位符 |
| DatasetService.importLabelMeDataset | `src/features/dataset/DatasetService.cpp:2124` | 高 | 仅依赖外部路径的跳过测试 |
| Python handlers (training/inference/export/anomaly) | `backend/labeltorch_backend/handlers/` | 高 | 完全无测试 |
| Python adapters (ultralytics/anomalib) | `backend/labeltorch_backend/adapters/` | 高 | 完全无测试 |

---

## 二、问题清单

### 2.1 假数据 / 占位符返回

| 编号 | 文件:行号 | 问题描述 | 严重级别 | 修复方案 |
|------|-----------|----------|----------|----------|
| F1 | `src/features/dataset/DatasetService.cpp:659-660` | `detectAnomalies` 中 `sizeAnomalies` 始终返回空列表，注释标注 "placeholder (width/height not typically populated)"。违反规则"禁止 dummy/占位符实现" | 中 | 在导入时记录图片宽高到 `dataset_samples.width/height`，并在 `detectAnomalies` 中实现尺寸异常检测（如尺寸偏离均值超过 3σ） |
| F2 | `src/features/inference/AnomalyDetector.cpp:78-84` | `loadModel` 在无 ONNX Runtime 时（`#else` 分支）设置 `m_impl->loaded = true` 并返回 `true`，但 `infer()` 此时永远返回空结果并发射 `inferenceFailed`。状态不一致：上层以为模型已加载，实际无法推理 | 高 | `loadModel` 在 `#else` 分支应返回 `false`，或增加 `isInferable()` 方法区分"元数据可读"与"可推理" |
| F3 | `src/features/inference/ActiveLearningService.cpp:79-81` | `prioritizeQueue` 在 IPC 断连时直接 `emit queuePrioritized(samples, samples.size())` 返回未排序的原始样本，调用方误认为已排序 | 中 | IPC 断连时应 `emit error(...)` 并返回空数组，或明确命名信号为 `queuePrioritizationFailed` |
| F4 | `tests/test_ipc.cpp:170-237` | `testProcessResponseMessage`、`testProcessEventMessage`、`testAutoRestartLimit` 三个测试是占位符，仅验证初始状态，未实际测试消息处理和自动重启逻辑。注释承认"无法直接注入数据到 QProcess 的 stdout" | 高 | 将 `processMessage` 改为 `public` 或提供 `injectMessage()` 测试接口，使测试可注入模拟消息 |
| F5 | `tests/test_database.cpp:74-80` | `testImportLabelMeUser` 依赖硬编码外部路径 `D:/z/work/superstar/robot/img/biaozhu/ExportImage`，路径不存在时直接 `return` 跳过，等于无测试 | 中 | 改用 `QTemporaryDir` 构造 LabelMe 格式测试数据，使测试可独立运行 |

### 2.2 资源泄漏 / 线程安全

| 编号 | 文件:行号 | 问题描述 | 严重级别 | 修复方案 |
|------|-----------|----------|----------|----------|
| R1 | `src/core/ipc/IpcClient.cpp:137-155` | `sendRequest` 将请求加入 `m_pendingCommands`，但**无超时清理机制**。规则要求"所有 IPC 请求必须设置超时（默认 30 秒），超时请求从 pendingCommands 移除"，但代码未实现。若后端卡死（进程存活但不响应），`m_pendingCommands` 会无限增长导致内存泄漏 | 高 | 为每个请求记录时间戳，在 watchdog 定时器中扫描并移除超过 30 秒未响应的请求，发射 `requestTimeout` 信号 |
| R2 | `src/core/ipc/IpcClient.cpp:115-135` | `stopBackend()` 设置 `m_autoRestart = false` 但 `startBackend()` 未重置 `m_autoRestart = true`。首次 `stopBackend` 后再次 `startBackend`，后端崩溃时不会自动重启 | 高 | 在 `startBackend()` 成功启动后添加 `m_autoRestart = true` |
| R3 | `src/features/training/SnapshotService.cpp:28-49` | `writeFileAtomically` 先 `QFile::remove(targetPath)` 再 `tmpFile.rename(targetPath)`，两步之间崩溃会导致文件丢失，非真正原子写入 | 中 | Windows 上使用 `MoveFileExW` 带 `MOVEFILE_REPLACE_EXISTING` 标志实现原子替换；或先写入 `.tmp` 再用 `QFile::rename`（Qt 6 在 Windows 上已支持覆盖） |
| R4 | `src/features/export/ExportService.cpp:496-504` | `exportReport` 同样使用 remove-then-rename 模式，非原子性 | 中 | 同 R3 |
| R5 | `src/features/inference/AssistedLabelService.cpp:399-452` | `readSnapshot` + `writeSnapshot` 的读-改-写操作无事务包裹。并发执行 `confirmCandidate` 和 `confirmAllAboveThreshold` 会丢失更新 | 中 | 使用 `db.transaction()` 包裹读-改-写，或使用 SQL 行锁 `SELECT ... FOR UPDATE` |

### 2.3 错误处理缺陷

| 编号 | 文件:行号 | 问题描述 | 严重级别 | 修复方案 |
|------|-----------|----------|----------|----------|
| E1 | `src/features/training/TrainingService.cpp:88-175` | `handleTrainingEvent` 的 `task.succeeded` 分支用 `try/catch(...)` 包裹事务，但 `QSqlQuery::exec()` 失败时返回 `false` 而非抛异常。若 `registerModelVersion` 返回空 `versionId`（SQL 失败），事务仍会 `commit()`，导致部分数据写入 | 高 | 检查每个 `QSqlQuery::exec()` 返回值，失败时主动 `throw std::runtime_error(...)` 触发 rollback，或改为条件判断式 rollback |
| E2 | `src/features/inference/InferenceService.cpp:19-23` | `setIpcClient` 未连接 `responseReceived` 信号。后端推理结果永远不会被处理，批次永久停留在 `pending` 状态。测试中候选数据靠 `injectCandidates` 直接写入数据库绕过了此问题 | 严重 | 添加 `connect(m_ipcClient, &IpcClient::responseReceived, this, &InferenceService::onResponseReceived)`，并实现 `onResponseReceived` 处理 `inference.run` 响应，更新 `candidate_snapshot_json` |
| E3 | `src/features/export/ExportService.cpp:311-314` | `handleIpcResponse` 中 `artifact_id` 为空时回退到 `response["request_id"]`，但 `request_id`（如 `req_12_3456`）不等于 `artifact_id`（UUID），会更新错误的记录 | 高 | 移除 request_id 回退逻辑，artifact_id 为空时记录错误并返回 |
| E4 | `src/features/training/TrainingService.cpp:330-389` | `startTraining` 在 `QtConcurrent::run` 的 lambda 中创建 `SnapshotService` 并调用 `prepareSnapshotPhysicalDir`。若 `m_ipcClient` 为 null（line 365），run 状态更新为 `running` 但不发送训练命令，run 永远卡在 `running` | 高 | `m_ipcClient` 为 null 时应将状态回滚为 `failed` 并发射警告信号 |
| E5 | `src/features/dataset/DatasetService.cpp:2124-2255` | `importLabelMeDataset` 执行多次 DB 操作（insertSamples、extractAndStoreSchema、updateQuery）但**未使用事务**。与 `importDatasetSeparate`（line 48 使用事务）不一致。中途失败会留下不一致状态 | 高 | 用 `db.transaction()` / `db.commit()` / `db.rollback()` 包裹整个导入流程 |
| E6 | `src/features/inference/AnomalyService.cpp:26-37` | `listModels()` 返回 7 个模型的硬编码列表，但 `anomalib_adapter.py` 的 `MODEL_FAMILIES` 支持 12 个模型。列表不一致，用户无法选择 `efficient_ad`、`fastflow`、`reverse_distillation`、`csflow`、`devnet` | 中 | 从后端动态查询支持的模型列表，或同步硬编码列表 |
| E7 | `src/features/training/TrainingService.cpp:621-662` | `reconcileStaleRuns` 先查询 stale run IDs，再批量 UPDATE，存在 TOCTOU 竞态。查询和更新之间若有新 run 进入 running 状态，会被错误地修正为 stopped | 中 | 使用单条 `UPDATE ... WHERE status IN ('running','preparing') AND id IN (查询时的ID列表)` 或在事务内完成查询+更新 |

### 2.4 异常路径未测试

| 编号 | 未测试场景 | 涉及模块 | 严重级别 | 建议补充测试 |
|------|------------|----------|----------|--------------|
| U1 | IPC 进程崩溃后自动重启（最多 5 次） | IpcClient | 高 | mock QProcess，模拟 finished 信号，验证重启计数和 backendError 信号 |
| U2 | IPC 请求超时清理 | IpcClient | 高 | 注入请求后等待 30s，验证 m_pendingCommands 被清理 |
| U3 | Python 后端 JSON 输出格式错误 | IpcClient | 中 | 向 processMessage 注入畸形 JSON，验证不崩溃 |
| U4 | 数据库锁定（WAL 并发写） | 所有 Service | 高 | 多线程并发调用 Service 方法，验证不死锁 |
| U5 | 文件不存在/无权限 | SnapshotService、ExportService | 中 | 模拟只读目录、不存在的图片路径 |
| U6 | 训练中 Python 崩溃 | TrainingService | 高 | 模拟 QProcess::finished(CrashExit)，验证 run 状态最终变为 failed |
| U7 | 导出产物验证失败 | ExportService | 中 | mock IPC 返回 verify 失败，验证状态变为 failed |
| U8 | 推理批次 IPC 响应处理 | InferenceService | 严重 | 当前完全未实现，需先修复 E2 再补测试 |
| U9 | 异常检测 ONNX 推理 | AnomalyDetector | 高 | 构造最小 ONNX 模型，验证推理结果和热力图生成 |
| U10 | 主动学习队列管理 | ActiveLearningService | 高 | 测试 addSampleToQueue、removeSampleFromQueue、clearQueue、getAllQueueStats |

---

## 三、已有测试中的失败 / 占位用例

根据 `Ralph.md` 记录和代码审查：

| 编号 | 测试文件:用例 | 状态 | 说明 |
|------|---------------|------|------|
| X1 | `test_database.cpp:testSchemaVersion` | 失败 | Ralph.md 记录"已有失败，非本次引入"。期望 schema_version 表中 version=1，可能 schema 版本已更新 |
| X2 | `test_classify.cpp:testImportClassifyFolderSchemaStored` | 失败 | Ralph.md 记录"已有失败，非本次引入"。验证 `imported_label_schemas` 表存储，可能导入逻辑未正确写入 schema |
| X3 | `test_ipc.cpp:testProcessResponseMessage` | 占位 | 仅验证 `spy.count() == 0`，未实际测试响应处理 |
| X4 | `test_ipc.cpp:testProcessEventMessage` | 占位 | 仅验证 `spy.count() == 0`，未实际测试事件处理 |
| X5 | `test_ipc.cpp:testAutoRestartLimit` | 占位 | 注释承认"由于无法真正启动后端"，仅验证 stopBackend 清理状态 |
| X6 | `test_ipc.cpp:testProcessInvalidJson` | 间接 | 测试 `QJsonDocument::fromJson` 而非 IpcClient 的实际处理逻辑 |
| X7 | `test_ipc.cpp:testProcessUnknownMessageType` | 间接 | 验证字符串比较而非 IpcClient 的 processMessage 实际行为 |
| X8 | `test_database.cpp:testImportLabelMeUser` | 跳过 | 依赖硬编码外部路径 `D:/z/work/superstar/...`，路径不存在时 return 跳过 |

---

## 四、缺失测试清单（按优先级排序）

### P0 - 阻断性缺失（必须立即补充）

1. **InferenceService IPC 响应处理测试** - 当前 `setIpcClient` 未连接 `responseReceived`，推理结果永远丢失（E2）。需先修复 Bug 再写测试验证候选数据回传。
2. **IpcClient 超时清理测试** - `m_pendingCommands` 无超时清理，长期运行会内存泄漏（R1）。
3. **IpcClient 自动重启测试** - `stopBackend` 后 `m_autoRestart` 永久 false（R2），需测试 stop→start→crash→restart 流程。

### P1 - 高优先级（核心功能未覆盖）

4. **AnomalyService 测试** - 完全无测试，`runInference` 的 IPC 调用、`onResponseReceived` 的结果解析均未验证。
5. **AnomalyDetector 测试** - ONNX 推理路径、元数据读取、热力图生成均未测试。需构造最小 ONNX 模型。
6. **ActiveLearningService 测试** - 队列管理（add/remove/clear）、IPC 调用、本地 fallback 逻辑均未测试。
7. **DatasetService.importLabelMeDataset 事务测试** - 当前无事务（E5），需测试中途失败时数据一致性。
8. **DatasetService.detectAnomalies 测试** - sizeAnomalies 为占位符（F1），需实现并测试。
9. **Python handlers 测试** - training/inference/export/anomaly 的 handler 函数完全无测试，建议用 pytest + mock IPC payload 测试。
10. **Python adapters 测试** - ultralytics_adapter 和 anomalib_adapter 的训练/推理/导出方法无测试。

### P2 - 中优先级（异常路径覆盖）

11. **TrainingService task.succeeded 事务回滚测试** - 验证 SQL 失败时事务正确回滚（E1）。
12. **ExportService artifact_id 回退逻辑测试** - 验证 request_id 回退不会更新错误记录（E3）。
13. **TrainingService startTraining 无 IPC 测试** - 验证 m_ipcClient 为 null 时状态回滚（E4）。
14. **并发测试** - AssistedLabelService 的读-改-写竞态（R5）、数据库 WAL 并发写。
15. **ProjectService 独立测试** - 当前仅经 test_database 间接覆盖。
16. **ClassMappingService 测试** - 完全无测试。
17. **AnnotationService 测试** - MVP 不含但代码已存在，应有基础测试。

### P3 - 低优先级（覆盖率提升）

18. **Geometry RotatedBox/Polygon 测试** - 当前仅测试 AxisAlignedBox。
19. **ModelRegistry 错误路径测试** - 当前仅测试正常路径。
20. **SnapshotService N+1 查询优化验证** - listSnapshots 存在 N+1 查询性能问题。

---

## 五、关键发现总结

### 5.1 最严重的 3 个问题

1. **InferenceService 丢失推理结果（E2）** - `setIpcClient` 未连接 `responseReceived` 信号，后端推理结果永远无法回传到 C++ 侧，批次永久停留 pending 状态。测试中靠 `injectCandidates` 绕过了此 Bug，掩盖了问题。这是阻断端到端流程的严重缺陷。

2. **IpcClient 无请求超时清理（R1）** - 违反项目规则"所有 IPC 请求必须设置超时（默认 30 秒），超时请求从 pendingCommands 移除"。后端卡死时 `m_pendingCommands` 无限增长。

3. **IpcClient 自动重启永久禁用（R2）** - `stopBackend()` 设置 `m_autoRestart = false` 后，`startBackend()` 不重置。首次手动停止后，再次启动的后端崩溃时不会自动重启，违反规则"Python 崩溃后 IpcClient 自动重启（最多 5 次）"。

### 5.2 测试覆盖率评估

- **C++ 侧覆盖率**：约 60%，核心 Service 有测试但多个推理子服务完全空白。
- **Python 侧覆盖率**：约 5%，仅 protocol 模块有 3 个测试，handlers 和 adapters 完全无测试。
- **异常路径覆盖率**：约 40%，training/export/snapshot 异常路径覆盖较好，IPC/推理/异常检测异常路径严重不足。
- **占位测试比例**：test_ipc.cpp 中 5/12 用例为占位符，实际有效覆盖率低于数字显示。

### 5.3 建议优先修复顺序

1. 修复 E2（InferenceService IPC 响应处理）- 阻断端到端流程
2. 修复 R1+R2（IpcClient 超时+自动重启）- 影响稳定性
3. 修复 E5（importLabelMeDataset 事务）- 数据一致性风险
4. 修复 E1（TrainingService 事务回滚）- 数据一致性风险
5. 修复 F2（AnomalyDetector loadModel 状态不一致）
6. 补充 P0 缺失测试
7. 修复 F1（sizeAnomalies 占位符）
8. 补充 P1 缺失测试

我先做一些针对性的搜索来识别 stub/占位符模式，确保审计报告的准确性。

让我再搜索一些关键的 stub 模式，包括空函数体、硬编码数据和未实现的图表。

让我再检查一些关键的功能连接点，包括图表数据源和 Service 调用。

让我检查图表数据源和训练/测试页面的关键连接点。

让我重新尝试读取这些文件，并查看 IpcProtocol.h。

让我检查一些可能的功能缺失点，包括 Tag 功能、程度模式和创建快照。

让我检查最后几个关键功能点：Tag 添加逻辑、undo/redo 按钮和模型导入。

让我重新读取 DatasetPage 的 addTagDialog 部分来确认 Tag 功能。

让我检查 AnnotCanvasItem 的 C++ 实现来确认 undo/redo 功能。

我已经完成了所有探索和分析。现在基于已读取的所有文件和针对性搜索结果，我将生成完整的 UI 审计差异报告。由于我没有 Write 工具可用，我将直接在回复中输出完整的审计报告。

---

# LabelTorchV UI 审计报告 - 对比参考设计稿

## 摘要

本次审计对比了参考设计稿 `reference/Dihuge DLTools/gemini-code-1780391652706.html`（7 页面 + 5 模态弹窗）与当前 QML 实现。总体结论：**当前实现的完成度非常高（约 90%），配色系统与参考稿完全一致，7 个页面的布局结构基本像素级复刻，绝大多数按钮和功能都已连接到真实 Service 调用**。仅发现 2 个真正的功能性 stub（Tag 添加、程度模型推理）和若干小的差异点。

---

## 1. 项目页 (ProjectPage.qml vs page-project)

### 参考稿要求
- sidebar-left：新建项目按钮 + 打开项目按钮
- center：最近使用项目列表（卡片形式）
- 新建项目弹窗：6 字段（名称/路径/描述/图库名/图库类型/基准路径）

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\features\project\qml\ProjectPage.qml`（1237 行）
- V6 项目中心，SplitView 左侧边栏(240px) + 中心内容区
- 左侧：新建项目按钮 + 打开项目按钮 + **类别体系管理** + **刷新列表**（比参考稿多两个功能入口）
- 中心：项目卡片列表（带 ACTIVE 徽章 + 打开/删除按钮）
- 新建项目对话框：6 字段（名称/路径/描述/图库名/图库类型/基准路径）—— 与参考稿完全一致
- `openProject()` 函数正确设置 `currentProjectId` + `taxonomyId`

### 差异点
1. **[增强]** 左侧栏多了"类别体系管理"和"刷新列表"按钮——这是功能增强，非缺陷
2. **[增强]** 项目卡片多了 ACTIVE 徽章和删除按钮——参考稿未明确
3. **[一致]** 新建项目弹窗 6 字段与参考稿完全一致

### 修复优先级：L（基本一致，差异均为功能增强）

---

## 2. 数据集页 (DatasetPage.qml vs page-dataset)

### 参考稿要求
- sidebar-left：图库选择器 + 数据集列表 + 图像属性 + Tag
- center：gallery-grid 缩略图网格
- 过滤栏：数据集/Tag/标签类别筛选
- 添加 Tag 弹窗：名称 + 快捷键

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\features\dataset\qml\DatasetPage.qml`（1739 行）
- V6 数据集页（像素级复刻参考 UI 设计）
- SplitView 左侧边栏(240px) + 中心缩略图网格
- 左侧：项目名标题 + 图库选择器(ComboBox+加号) + 数据集(N)可折叠(带进度条+样本数) + 图像属性(名称/路径/大小/标签/类别) + Tag(2 列 grid 按钮)
- 中心：FilterBar + 标题栏 + GridView 缩略图网格(90px cellWidth)
- 包含导入数据集弹窗和添加 Tag 弹窗
- `selectDataset()` 函数正确加载样本列表与统计信息

### 差异点
1. **[STUB-严重]** **添加 Tag 弹窗（addTagDialog）是 stub**：
   - 位置：`DatasetPage.qml:1107`
   - `onAccepted: { tagNameField.clear(); tagShortcutField.clear() }` —— 只清空输入框，**没有调用任何 service 添加 Tag，没有更新 tagModel**
   - `tagModel` 是硬编码的字符串数组 `["全部状态", "已标注", "未标注", "有效", "异常"]`（第 673 行），不是真正的 Tag 系统
   - 参考稿要求 Tag 是用户可自定义添加的，当前实现只是状态筛选器
2. **[一致]** 图库选择器、数据集列表、图像属性、缩略图网格布局与参考稿一致
3. **[一致]** 过滤栏 FilterBar 在 Main.qml 中全局实现

### 修复优先级：S（Tag 添加功能是核心交互，当前为 stub）

---

## 3. 标注页 (AnnotationPage.qml vs page-label)

### 参考稿要求
- sidebar-left：同数据集 + 图像导航 + 标签类别 + Tag
- center：标注画布
- 添加标签弹窗：名称/颜色/快捷键/模型 index/下料 id

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\features\annotation\qml\AnnotationPage.qml`（2005 行）
- 标注工作台 v2.0.0，SplitView 左侧栏(240px, 4 个可折叠区) + 中央画布
- 左侧 4 区：图库与数据集 / 图像导航 / 标签类别 / Tag
- 中央：顶部筛选栏(38px) + AnnotCanvasItem 画布 + 右侧悬浮工具栏(36px) + 状态栏(24px)
- 工具栏：select/rect/obb/polygon/hand/zoom_in/zoom_out
- 4 种标注模式：detect/obb/classify/anomaly
- 添加标签弹窗（名称/颜色/快捷键/模型索引/下料 ID）+ 添加 Tag 弹窗 + 双击编辑标签对话框
- **undo/redo 完整实现**：C++ 层 `AnnotCanvasItem` 有完整的 `m_undoStack`、`undo()`、`redo()`、`commitUndoState()` 实现，快捷键 Ctrl+Z/Y 在 C++ 层处理（`AnnotCanvasItem.cpp:858-860`）
- `loadSample()` 正确调用 `canvasItem.loadImage()` 和 `annotationService.loadAnnotations()`
- 保存调用 `annotationService.saveAnnotations()` + `canvasItem.commitUndoState()`

### 差异点
1. **[一致]** 左侧 4 区结构与参考稿一致
2. **[一致]** 添加标签弹窗 5 字段与参考稿完全一致
3. **[一致]** undo/redo 功能完整实现（C++ 层）
4. **[需验证]** 添加 Tag 弹窗可能与 DatasetPage 一样是 stub（需确认 onAccepted 逻辑）

### 修复优先级：M（需验证 Tag 弹窗功能）

---

## 4. 检查页 (CheckPage.qml vs page-check)

### 参考稿要求
- sidebar-left：图库 + 数据集 + 程度图像模式 Toggle + 标签类别
- center：缺陷图像网格

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\features\dataset\qml\CheckPage.qml`（799 行）
- 检查页，左侧栏(240px) + 中央缺陷图像网格
- 左侧：项目名 + 图库选择器 + 数据集(N)可折叠 + 程度图像可折叠(ToggleSwitch+标注程度模型按钮) + 标签类别可折叠(多选+未标注的图像特殊项)
- `refreshData()` / `applyClassFilter()` / `getClassName()` 函数完整实现
- 正确调用 `datasetService.listDatasets()`、`annotationService.listSamples()`、`annotationService.loadAnnotations()`

### 差异点
1. **[STUB-中等]** **"标注程度模型"按钮是部分 stub**：
   - 位置：`CheckPage.qml:403, 413-418`
   - `onClicked` 只是切换 `severityMode` 并跳转标注页：`root.severityMode = !root.severityMode; if (root.severityMode) appController.currentPage = "annotation"`
   - **没有实际的"程度模型"推理逻辑**，没有调用任何 service
   - 参考稿的"程度图像模式"应该有实际的缺陷程度分级推理
2. **[占位]** 按类别统计显示占位：
   - 位置：`CheckPage.qml:537`
   - 注释："数量（monospace）- 暂无按类别统计，显示占位"
3. **[一致]** 整体布局结构与参考稿一致

### 修复优先级：M（程度模型推理功能缺失，但不阻断核心流程）

---

## 5. 训练页 (TrainingPage.qml vs page-train)

### 参考稿要求
- sidebar-left：模型列表 + 开始训练按钮
- center：子 tab（配置参数 2x2 四宫格 / 实时训练结果 4 图表）

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\features\training\qml\TrainingPage.qml`（2849 行）
- 训练工作台 V3，SplitView 左侧模型列表(240px) + 右侧内容区
- 左侧：模型列表(ListView) + 开始训练按钮(渐变色) + 停止训练按钮
- 右侧：子标签栏(40px, 配置参数/实时训练结果) + StackLayout
- **配置参数页：2x2 四宫格**（数据集配置/网络配置/训练设置/数据增强配置）—— 与参考稿一致
- **实时训练结果页：左侧状态面板(240px) + 右侧 4 个图表卡**(Loss/mAP/召回率/精确率)—— 与参考稿一致
- 图表使用 Canvas 自定义绘制（`drawChart()` 函数，第 515 行），含网格线/坐标轴/阈值线/数据线/数据点
- **图表数据是真实数据**：`lossModel`、`metricModel`、`recallModel`、`precisionModel` 在 `onTrainingProgress` 信号中填充（第 298-318 行）
- `getConfigJson()` 序列化全部训练配置
- `validateTrainingStart()` 环境检测+显存校验
- `saveTrainingTemplate()` / `loadTrainingTemplate()` 配置模板
- 创建数据快照对话框正确调用 `snapshotService.createSnapshot(dsId, ratio, strategy)`（第 2656 行）
- 添加模型对话框完整实现
- 监听 trainingService 信号：onTrainingProgress/onTrainingLog/onTrainingWarning/onRunStatusChanged

### 差异点
1. **[一致]** 2x2 四宫格配置页与参考稿完全一致
2. **[一致]** 4 图表卡与参考稿一致，且数据为真实训练进度数据
3. **[增强]** 多了停止训练按钮、配置模板保存/加载、环境校验——功能增强
4. **[一致]** 创建数据快照功能完整实现

### 修复优先级：L（完成度极高，无 stub）

---

## 6. 测试页 (TestingPage.qml vs page-test)

### 参考稿要求
- sidebar-left：模型列表 + 开始测试按钮
- center：任务选择栏 + 3 个 CollapsibleSection（设置/测试/测试详情）

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\features\testing\qml\TestingPage.qml`（2246 行）
- V5 测试页，SplitView 左侧模型列表(240px) + 右侧内容区
- 左侧：模型列表 + 开始/停止测试按钮
- 右侧：任务选择栏(40px) + 3 个 CollapsibleSection 卡片——与参考稿一致
- **设置卡片**：3 列(数据集配置/测试参数配置/高级测试参数配置)
- **测试卡片**：3 列(实例统计含漏检率/误检率圆环+F1/图像统计/PR 曲线 Canvas)
- **测试详情卡片**：内部 toolbar(混淆矩阵/检查图像 tab 切换) + 3 列(混淆矩阵 Canvas/图像预览/实例详情)
- 支持异常检测模式（AUROC 指标）和普通检测模式（mAP50 指标）
- **PR 曲线和混淆矩阵数据是真实数据**：来自 `testingService.getTestResults(taskId)`，解析 `prCurveJson` 和 `confusionMatrixJson`（第 186-196 行）
- `loadTestResults()` / `loadCheckImages()` / `createNewTestTask()` / `validateTestStart()` / `startOrCreateTestTask()` 完整实现
- 导入外部模型对话框正确调用 `modelRegistry.importExternalModel()`（第 2056 行）
- 类别配置对话框完整实现
- 监听 testingService 信号：onTestTaskStatusChanged/onTestProgress
- 监听 ipcClient 信号：environment.check 响应

### 差异点
1. **[一致]** 3 个 CollapsibleSection 结构与参考稿一致
2. **[一致]** PR 曲线、混淆矩阵使用真实测试结果数据
3. **[增强]** 多了异常检测模式支持、导入外部模型功能——功能增强
4. **[一致]** 任务选择栏与参考稿一致

### 修复优先级：L（完成度极高，无 stub）

---

## 7. 导出页 (ExportPage.qml vs page-export)

### 参考稿要求
- sidebar-left：模型列表
- center：左 280px 参数面板 3 卡片 + 右侧空展示区

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\features\export\qml\ExportPage.qml`（1212 行）
- V5 导出页，SplitView 左侧模型列表(240px) + 中心 SplitView（参数面板 280px + 展示区）
- 参数面板：导出模型卡片（设备/测试权重/导出格式/导出路径/ONNX 附加配置）+ 导出报告卡片
- 展示区：版本信息卡片 + 导出历史卡片 + 验证详情卡片
- `validateExportStart()` / `startExportWithValidation()` 完整实现
- 正确调用 `exportService.exportModel()`、`exportService.exportReport()`、`exportService.verifyExport()`、`exportService.getExportStatus()`、`exportService.listExports()`
- 监听 `exportStatusChanged` 信号自动刷新

### 差异点
1. **[差异-小]** 参考稿参数面板 3 卡片，当前实现 2 卡片（导出模型 + 导出报告）——可能少了 1 个卡片，但当前 2 卡片已覆盖核心功能
2. **[增强]** 参考稿右侧是"空展示区"，当前实现有 3 个卡片（版本信息 + 导出历史 + 验证详情）——功能增强
3. **[一致]** 左侧模型列表与参考稿一致
4. **[一致]** 参数面板宽度 280px 与参考稿一致

### 修复优先级：M（参数面板卡片数量差异，但不阻断功能）

---

## 8. 主窗口 (Main.qml) 对比

### 参考稿要求
- 顶栏 50px：logo + 7 个导航 tab + 右侧工具区
- 底栏 footer：工作区 + 选中文件 + 标注进度条
- 过滤栏 filter-bar：在非项目页显示

### 当前实现
- 文件：`e:\z\project\my\LabelTorchV\src\shell\qml\Main.qml`（1241 行）
- V5 主布局：顶栏(50px) + 全宽中心内容 + 底栏(34px)
- 7 个导航项：项目/数据集/标注/检查/训练/测试/导出——与参考稿一致
- 全局筛选栏 FilterBar 在 dataset/annotation/check 页面显示——与参考稿一致
- StackLayout 7 个 Loader 异步加载页面
- GPU 状态指示灯 + Python 后端连接状态指示灯
- `pageSources[1]` 指向 `DatasetPage.qml`（V6 像素级复刻版），而非 `ImportPage.qml`

### 差异点
1. **[一致]** 顶栏 50px、7 个导航项与参考稿一致
2. **[差异-小]** 底栏 34px（参考稿未明确高度，但参考稿底栏含"标注进度条"，当前实现需确认是否有进度条）
3. **[一致]** FilterBar 在非项目页显示的逻辑与参考稿一致
4. **[增强]** 多了 GPU 状态和 Python 后端连接状态指示灯

### 修复优先级：L（基本一致）

---

## 9. 配色一致性分析

### 参考稿 CSS 变量 vs Theme.qml

| CSS 变量 | 参考稿值 | Theme.qml 属性 | Theme.qml 值 | 一致性 |
|----------|----------|----------------|--------------|--------|
| --bg-main | #0F131A | bgMain | #0F131A | ✅ 一致 |
| --bg-side | #151A22 | bgSide | #151A22 | ✅ 一致 |
| --bg-card | #1E2530 | bgCard | #1E2530 | ✅ 一致 |
| --bg-input | #121720 | bgInput | #121720 | ✅ 一致 |
| --primary | #0077FF | primary | #0077FF | ✅ 一致 |
| --primary-glow | #00E5FF | primaryGlow | #00E5FF | ✅ 一致 |
| --primary-dark | #0055CC | primaryDark | #0055CC | ✅ 一致 |
| --success | #00E676 | success | #00E676 | ✅ 一致 |
| --danger | #FF1744 | danger | #FF1744 | ✅ 一致 |
| --warning | #FF9100 | warning | #FF9100 | ✅ 一致 |

**结论：配色系统 100% 一致，Theme.qml 完整复刻了参考稿的 V5 Dihuge DLTools 视觉设计系统。**

---

## 10. 模态弹窗对比

| 参考稿弹窗 | 当前实现 | 状态 |
|------------|----------|------|
| create-project (6 字段) | ProjectPage.qml 新建项目对话框 (6 字段) | ✅ 完整实现 |
| add-tag (名称+快捷键) | DatasetPage.qml addTagDialog | ⚠️ **STUB** (onAccepted 只清空输入) |
| add-label (5 字段) | AnnotationPage.qml addLabelDialog (5 字段) | ✅ 完整实现 |
| batch-modify (2 复选框+数据集名称) | 未找到对应实现 | ❌ **缺失** |
| add-model (名称/类型/导入权重) | TrainingPage.qml + TestingPage.qml importModelDialog | ✅ 完整实现 |

---

## 11. Stub/占位符清单汇总

### 真正的功能性 Stub（需修复）

| 编号 | 位置 | 描述 | 严重程度 | 修复优先级 |
|------|------|------|----------|------------|
| STUB-01 | `DatasetPage.qml:1107` | **addTagDialog 的 onAccepted 只清空输入框，没有调用任何 service 添加 Tag**。tagModel 是硬编码的 `["全部状态", "已标注", "未标注", "有效", "异常"]`，不是真正的 Tag 系统。 | 严重 | S |
| STUB-02 | `CheckPage.qml:403,413-418` | **"标注程度模型"按钮 onClicked 只切换 severityMode 并跳转标注页，没有实际的程度模型推理逻辑**。 | 中等 | M |

### UI 空状态占位（正常，非 stub）

| 编号 | 位置 | 描述 | 说明 |
|------|------|------|------|
| PLACEHOLDER-01 | `CheckPage.qml:537` | "数量（monospace）- 暂无按类别统计，显示占位" | 按类别统计功能未实现，显示占位 |
| PLACEHOLDER-02 | `CheckPage.qml:691` | "加载中占位" | 正常的加载状态提示 |
| PLACEHOLDER-03 | `ReviewDialog.qml:57` | "Image placeholder area" | 正常的图片占位区 |
| PLACEHOLDER-04 | `AnomalyInferPanel.qml:378` | "无图片占位" | 正常的空状态提示 |
| PLACEHOLDER-05 | `TestingPage.qml:1612` | "无选中图像时的占位提示" | 正常的空状态提示 |
| PLACEHOLDER-06 | `DatasetService.cpp:659` | "Size anomalies: placeholder (width/height not typically populated)" | C++ 层尺寸异常检测未完整实现 |

### 缺失的弹窗

| 编号 | 描述 | 修复优先级 |
|------|------|------------|
| MISSING-01 | **batch-modify 弹窗（批量修改：2 复选框+数据集名称）**：参考稿有此弹窗，当前实现未找到对应实现 | L |

---

## 12. 各页面修复优先级汇总

| 页面 | 修复优先级 | 主要问题 |
|------|------------|----------|
| 项目页 (ProjectPage) | L | 基本一致，差异为功能增强 |
| 数据集页 (DatasetPage) | **S** | **addTagDialog 是 stub，Tag 添加功能未实现** |
| 标注页 (AnnotationPage) | M | 需验证 Tag 弹窗功能（可能与 DatasetPage 同问题） |
| 检查页 (CheckPage) | M | "标注程度模型"按钮是部分 stub，按类别统计占位 |
| 训练页 (TrainingPage) | L | 完成度极高，无 stub |
| 测试页 (TestingPage) | L | 完成度极高，无 stub |
| 导出页 (ExportPage) | M | 参数面板卡片数量差异（2 vs 3） |
| 主窗口 (Main.qml) | L | 基本一致 |

---

## 13. 总结与建议

### 整体评价
当前 UI 实现的完成度非常高（约 90%），主要体现在：
1. **配色系统 100% 一致**：Theme.qml 完整复刻了参考稿的 V5 Dihuge DLTools 视觉设计系统
2. **布局结构高度还原**：7 个页面的 SplitView 布局、左侧栏 240px、过滤栏等都与参考稿一致
3. **核心功能完整连接**：训练页的 4 图表使用真实训练进度数据，测试页的 PR 曲线/混淆矩阵使用真实测试结果数据，导出页正确调用 exportService 全套方法
4. **无硬编码假数据**：搜索 `mock`/`fake`/`dummy` 等关键词均无匹配，图表数据均来自 Service 调用

### 需要优先修复的问题
1. **[S 级] DatasetPage 的 Tag 添加功能**：addTagDialog 的 onAccepted 需要调用实际的 service（可能需要新增 TagService 或在 DatasetService 中添加 addTag/removeTag 方法）来持久化 Tag，并更新 tagModel
2. **[M 级] CheckPage 的"标注程度模型"推理**：需要实现程度模型推理逻辑，或暂时隐藏该按钮/改为提示"功能开发中"
3. **[M 级] AnnotationPage 的 Tag 弹窗**：需确认是否与 DatasetPage 有相同的 stub 问题
4. **[M 级] ExportPage 参数面板卡片数量**：参考稿 3 卡片，当前 2 卡片，需确认是否需要补充第 3 个卡片

### 不需要修复的差异（功能增强）
- ProjectPage 多了"类别体系管理"和"刷新列表"按钮
- TrainingPage 多了停止训练按钮、配置模板保存/加载、环境校验
- TestingPage 多了异常检测模式支持、导入外部模型功能
- ExportPage 右侧展示区从"空"扩展为 3 个卡片
- Main.qml 多了 GPU 状态和 Python 后端连接状态指示灯

这些差异都是对参考稿的功能增强，不属于缺陷。

---

**审计完成。** 本报告基于对参考设计稿 HTML（2931 行）和 11 个 QML 文件（总计约 15000 行）的完整读取，以及针对 stub 模式、空函数体、硬编码数据、Service 调用连接点的 10+ 次专项搜索。所有文件路径和行号均基于实际代码，可直接定位修复。

现在让我读取 IpcProtocol.h 和其他相关文件以完成审计。

让我快速检查 testing 模块和确认其他相关代码。

现在我已经收集了足够的信息。让我检查最后几个关键点。

让我再检查一些关键点以完成审计。

现在我已经收集了足够的信息。让我整理完整的审计报告。

---

# LabelTorchV 项目 MVP 全链路功能完整性审计报告

## 审计范围

- 文档：`docs/blueprint.md`、`docs/architecture.md`、`docs/执行总计划-LabelTorchV-MVP+.md`、`docs/里程碑验收与执行清单.md`、`docs/原始需求.md`
- 代码：数据集导入、数据快照、训练、导出、IPC 五大模块的 C++ 与 Python 实现

## 一、数据集模块（DatasetService.cpp）

### S 级（严重）

**S1. `importDatasetV2`/`importLabelMeDataset`/`importAnomalyDataset`/`importClassifyFolderDataset` 缺少事务保护**
- 文件：`e:\z\project\my\LabelTorchV\src\features\dataset\DatasetService.cpp:1556-1818`、`1820-1931`、`1933-2053`、`2124-2256`
- 问题：仅 `importDataset`（行 26-151）使用了事务，其他四个导入入口（V2 总入口、LabelMe、异常检测、分类）均未使用事务。这些函数涉及多表写入（datasets 状态、dataset_samples 批量插入、imported_label_schemas 插入），中途失败会留下半成品数据。违反项目规则"数据库事务：多表关联操作必须在同一事务内完成"。
- 修复方案：在每个导入函数开头 `db.transaction()`，所有 SQL 失败分支 `db.rollback()` 并清理已生成的临时文件，结尾 `db.commit()`。

**S2. `importDatasetJson`/`importLabelMeDataset` 失败时未回滚已创建的 dataset 记录**
- 文件：`DatasetService.cpp:1346-1468`、`2124-2256`
- 问题：失败路径只调用 `updateImportStatus(datasetId, "failed")`，但 datasets 记录已插入且未在事务中，导致数据库残留 failed 状态的孤立数据集记录，且无法通过正常流程删除（因为 sample_count=0）。
- 修复方案：用事务包裹整个流程，失败时 rollback；或失败时显式 DELETE datasets 记录。

### M 级（中等）

**M1. `importDatasetJson`/`importLabelMeDataset` 文件写入非原子性**
- 文件：`DatasetService.cpp:1412-1431`、`2186-2207`
- 问题：直接 `QFile::open(QIODevice::WriteOnly)` 写入 YOLO txt 标签文件，未先写 `.tmp` 再 rename。崩溃半写会损坏标签文件。违反项目规则"文件写入必须原子性（先 .tmp 再 rename）"。
- 修复方案：参考 `SnapshotService::writeFileAtomically`（行 28-49）实现，或复用 `YoloTxtWriter`。

**M2. `importDatasetV2`/`importLabelMeDataset`/`importAnomalyDataset`/`importClassifyFolderDataset` 主线程耗时操作**
- 文件：`DatasetService.cpp:1556-1818`、`1820-1931`、`1933-2053`、`2124-2256`
- 问题：整个导入流程（扫描目录、解析 JSON、批量插入数据库、生成标签文件）在调用线程同步执行。如果从 QML 调用，会阻塞 UI 主线程。仅 `scanFolderAsync`/`scanSeparateAsync`（行 187-209）使用了 `QtConcurrent::run`。
- 修复方案：将导入流程移到 `QtConcurrent::run`，通过信号回传结果。

**M3. `getSampleStats`/`detectAnomalies`/`getClassDistribution` 主线程耗时操作**
- 文件：`DatasetService.cpp:414-541`、`543-675`、`677-768`
- 问题：这三个函数都遍历所有样本并读取标签文件解析，对大数据集（数千样本）会阻塞 UI 主线程 >16ms。违反项目规则"禁止 UI 主线程耗时操作（>16ms）"。
- 修复方案：移到 `QtConcurrent::run`，通过信号回传结果。

**M4. `importDatasetJson` 中 `projectRoot` 为空时未 early return**
- 文件：`DatasetService.cpp:1392-1398`
- 问题：查询 `projectRoot` 失败时（exec 失败或无结果），`projectRoot` 为空字符串，但代码继续执行，导致 `labelOutputDir = "" + "/cache/labels/" + datasetId`，生成无效路径。`QDir().mkpath()` 会在当前工作目录创建垃圾目录。
- 修复方案：查询失败时 `updateImportStatus("failed")` 并返回空字符串。

### L 级（轻微）

**L1. `detectAnomalies` 中 `sizeAnomalies` 为占位符实现**
- 文件：`DatasetService.cpp:659-660`
- 问题：注释明确写 "placeholder (width/height not typically populated)"，`sizeAnomalies` 永远为空列表。docs/blueprint.md 要求"数据集扫描与异常提示"，尺寸异常检测是其中一部分。
- 修复方案：在 `dataset_samples` 表中填充 width/height 后实现尺寸异常检测，或明确从 MVP 范围中移除。

**L2. `syncClassesToTaxonomy` 在事务外执行**
- 文件：`DatasetService.cpp:143-146`
- 问题：`importDataset` 在 `db.commit()`（行 146）之前调用了 `syncClassesToTaxonomy`（行 143），但该函数内部有独立的 SQL 操作（更新 taxonomies 表），如果 commit 失败，taxonomy 已被修改且无法回滚。
- 修复方案：将 `syncClassesToTaxonomy` 移到 commit 之后，或将其逻辑纳入同一事务。

## 二、快照模块（SnapshotService.cpp）

### M 级（中等）

**M1. `prepareSnapshotPhysicalDir` 文件复制非原子性**
- 文件：`e:\z\project\my\LabelTorchV\src\features\training\SnapshotService.cpp:563-592`
- 问题：`QFile::copy(srcImg, dstImg)` 直接覆盖目标文件，如果中途中断（如进程崩溃），会留下部分复制的快照目录，但数据库中快照状态仍为可用，后续训练会使用不完整数据。违反"数据快照不可变"原则的隐含要求。
- 修复方案：先复制到 `.tmp` 目录，全部完成后 rename 到目标目录；或在快照表增加 `physical_prepared` 状态字段。

**M2. `prepareSnapshotPhysicalDir` 创建空标签文件未检查 open() 返回值**
- 文件：`SnapshotService.cpp:579-581`、`588-590`
- 问题：`QFile file(dstLbl); file.open(QIODevice::WriteOnly); file.close();` 没有检查 `open()` 返回值。如果磁盘满或权限不足，静默失败会导致训练时标签文件缺失。
- 修复方案：检查 `open()` 返回值，失败时记录错误并返回空字符串。

**M3. `prepareSnapshotPhysicalDir` 复制失败时未清理已复制文件**
- 文件：`SnapshotService.cpp:563-566`
- 问题：`QFile::copy` 失败时直接 `return false`（通过 lambda），但已经复制了部分文件到快照目录，没有清理机制。下次重试时可能因为已存在文件而行为异常（虽然代码中有 `QFile::remove`，但只在 copy 前调用）。
- 修复方案：失败时删除整个 snapshotDir，或标记为 dirty 状态。

**M4. `prepareAnomalySnapshotDir` 与 `prepareSnapshotPhysicalDir` 代码重复**
- 文件：`SnapshotService.cpp:679-783`
- 问题：`prepareAnomalySnapshotDir` 是 `prepareSnapshotPhysicalDir` 的子集（仅 anomaly 类型），但独立实现，逻辑重复。且 `prepareAnomalySnapshotDir` 中 `QFile::copy` 失败时只 warning 不返回错误（行 776），可能导致部分数据缺失。
- 修复方案：合并到 `prepareSnapshotPhysicalDir`，删除 `prepareAnomalySnapshotDir`；失败时返回错误。

**M5. `prepareAnomalySnapshotDir` 未被调用**
- 文件：`SnapshotService.cpp:679`
- 问题：通过 grep 确认，`prepareAnomalySnapshotDir` 仅在 SnapshotService.cpp 中定义，没有任何调用方。`TrainingService::startTraining`（行 332）统一调用 `prepareSnapshotPhysicalDir`。这是死代码。
- 修复方案：删除 `prepareAnomalySnapshotDir` 函数。

### L 级（轻微）

**L1. `createSnapshot` 未使用事务**
- 文件：`SnapshotService.cpp:56-169`
- 问题：虽然只有一条 INSERT，但理论上也应事务保护。严重级别低因为单条 SQL 本身是原子的。
- 修复方案：可选加事务。

**L2. `isOBBDataset` 主线程耗时操作**
- 文件：`SnapshotService.cpp:352-420`
- 问题：读取最多 5 个标签文件判断 OBB 格式，虽然有限制但仍可能在慢速磁盘上阻塞。
- 修复方案：可接受，或移到后台线程。

## 三、训练模块（TrainingService.cpp + Python 后端）

### S 级（严重）

**S1. `startTraining` 未检查 IPC 连接状态**
- 文件：`e:\z\project\my\LabelTorchV\src\features\training\TrainingService.cpp:365-388`
- 问题：在 `QtConcurrent::run` 回调中，仅判断 `if (m_ipcClient)`（行 365），未检查 `m_ipcClient->connected()`。如果 IPC 后端在快照准备期间崩溃，`sendRequest` 会静默失败（IpcClient.cpp:139-142），但 training_runs 状态已更新为 'running'，UI 会永远显示"训练中"。违反"跨模块调用失败必须通过信号槽通知上层"。
- 修复方案：在发送 `CMD_TRAIN_START` 前检查 `m_ipcClient->connected()`，未连接时 `updateRunStatus(runId, "failed")` 并 `emit trainingWarning`。

### M 级（中等）

**M1. `stopTraining` IPC 命令字面量硬编码**
- 文件：`TrainingService.cpp:417`
- 问题：`m_ipcClient->sendRequest("train.stop", payload)` 使用字面量，应使用 `IpcProtocol::CMD_TRAIN_STOP`。违反项目规则"IPC 命令字面量硬编码（应使用 IpcProtocol.h 常量）"。
- 修复方案：改为 `IpcProtocol::CMD_TRAIN_STOP`。

**M2. `stopTraining` 状态更新顺序错误**
- 文件：`TrainingService.cpp:414-435`
- 问题：先发送 `train.stop` 请求（行 417），然后立即将状态更新为 'stopped'（行 421-431）。但实际停止是异步的，后端可能还在运行训练。正确流程应等后端 `task.stopped` 事件再更新状态。当前实现会导致 UI 显示已停止，但后端仍在训练，资源未释放。
- 修复方案：只发送 stop 请求，状态更新交给 `handleTrainingEvent` 处理 `task.stopped` 事件。可增加 'stopping' 中间态。

**M3. `handleTrainingEvent` 事务范围过大且与 ModelRegistry 内部状态不一致**
- 文件：`TrainingService.cpp:87-175`
- 问题：事务包裹了 `m_modelRegistry->registerModelVersion`、`setParentVersion`、`setTag`、`removeTag` 等多个调用。如果 ModelRegistry 内部使用了独立的事务或 SQL 操作，外层事务回滚时 ModelRegistry 内部的修改可能已经提交，导致数据不一致。此外，catch 块使用 `catch (...)`（行 172）捕获所有异常，包括 SQL 错误，但 ModelRegistry 的错误不一定通过异常传播。
- 修复方案：将 ModelRegistry 操作移出事务，或确保 ModelRegistry 不使用独立事务；用具体的异常类型替换 `catch (...)`。

**M4. `handleTrainingEvent` 兜底 SQL 插入 model_versions 错误处理不完整**
- 文件：`TrainingService.cpp:99-116`
- 问题：当 `m_modelRegistry` 为 null 时，直接 SQL 插入 model_versions。如果插入失败（行 111），只记录日志并清空 versionId，但事务继续执行（行 118 的 `if (!versionId.isEmpty())` 跳过），最终 `db.commit()`（行 171）。这会导致 training_run 状态为 succeeded 但没有对应的 model_version 记录，违反"模型版本必须可追溯到训练任务"。
- 修复方案：插入失败时应 `throw` 或 `db.rollback()` 并返回。

**M5. `onResponseReceived` train.start 失败时通过猜测回滚状态**
- 文件：`TrainingService.cpp:569-588`
- 问题：train.start 失败时，查询最近的 preparing/running 任务并标记为 failed。这是启发式猜测，如果同时有多个任务（虽然不太可能），会误伤。更严重的是，如果后端在 sendRequest 后、response 回来前已经发了 task.failed 事件，这里会重复处理。
- 修复方案：在 startTraining 时记录 runId 到 pending 映射，response 回来时通过 request_id 精确匹配。

**M6. `reconcileStaleRuns` 无法区分 stopped 和 failed**
- 文件：`TrainingService.cpp:621-662`
- 问题：冷启动时将所有 running/preparing 状态的任务标记为 'stopped'。但实际上这些任务可能是异常崩溃（应标记为 failed）。docs/执行总计划要求"冷启动残留状态修复"，但当前实现无法区分。
- 修复方案：标记为 'failed' 更合理，或增加 'interrupted' 状态。

### L 级（轻微）

**L1. `startTraining` 在后台线程创建 SnapshotService 实例**
- 文件：`TrainingService.cpp:330-332`
- 问题：`QtConcurrent::run` 中创建 `SnapshotService snapshotService;`，SnapshotService 是 QObject，在后台线程创建无 parent 的 QObject 虽然合法，但不符合"QObject 必须在创建它的线程中使用"的最佳实践。SnapshotService 内部使用了独立的数据库连接（ThreadDbGuard），这部分是正确的。
- 修复方案：可接受，或改为静态方法。

**L2. Python 后端 `_run_training` 未处理 `task.started` 事件丢失**
- 文件：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\handlers\training.py:113-190`
- 问题：`_run_training` 首先发送 `task.started` 事件（行 117），但如果 `adapter.start_training` 在 `task.started` 之前就抛异常，`task.started` 不会发送，前端状态卡在 preparing。虽然 `task.failed` 会被发送，但前端 `handleTrainingEvent` 期望先收到 `task.started` 再收到 `task.failed`。
- 修复方案：在 try 块内确保 `task.started` 先发送，或前端容错处理。

## 四、导出模块（ExportService.cpp + Python 后端）

### M 级（中等）

**M1. `exportModel` IPC 命令字面量硬编码**
- 文件：`e:\z\project\my\LabelTorchV\src\features\export\ExportService.cpp:149`
- 问题：`m_ipcClient->sendRequest("export.run", payload)` 使用字面量，应使用 `IpcProtocol::CMD_EXPORT_RUN`。
- 修复方案：改为 `IpcProtocol::CMD_EXPORT_RUN`。

**M2. `handleIpcResponse` IPC 命令字面量硬编码**
- 文件：`ExportService.cpp:310`、`350`
- 问题：`command == QStringLiteral("export.run")` 和 `command == QStringLiteral("artifact.verify")` 使用字面量，应使用 `IpcProtocol::CMD_EXPORT_RUN` 和 `IpcProtocol::CMD_ARTIFACT_VERIFY`。
- 修复方案：使用 IpcProtocol 常量。

**M3. `exportModel` 未检查 IPC 连接状态**
- 文件：`ExportService.cpp:140-150`
- 问题：仅判断 `if (m_ipcClient)`，未检查 `m_ipcClient->connected()`。IPC 未连接时 sendRequest 静默失败，但 export_artifacts 记录已插入且状态更新为 'running'，UI 永远显示"导出中"。
- 修复方案：检查 connected()，未连接时返回空并清理 artifact 记录。

**M4. `handleIpcResponse` SQL 更新未检查返回值**
- 文件：`ExportService.cpp:323-327`、`356-359`、`372-375`
- 问题：多处 `updateQuery.exec()` 直接调用，未检查返回值。如果 SQL 失败（如数据库锁定），导出状态会与实际不一致，但前端无感知。
- 修复方案：检查 exec() 返回值，失败时记录错误并通知上层。

**M5. `exportModel` 未使用事务**
- 文件：`ExportService.cpp:120-137`
- 问题：插入 export_artifacts 记录后立即更新状态为 'running'（行 152），两步操作未在事务中。如果第二步失败，会留下 pending 状态的孤立记录。
- 修复方案：用事务包裹，或合并为一条 SQL。

**M6. `verifyExport` verifying 状态下重复调用直接返回 true**
- 文件：`ExportService.cpp:251-254`
- 问题：如果当前状态已是 'verifying'，直接返回 true 而不重新发送验证请求。如果验证请求因 IPC 断连而丢失，状态会永远卡在 'verifying'。`reconcileStaleExports` 冷启动时会标记为 failed，但运行时无法自愈。
- 修复方案：增加超时机制，verifying 状态超过 N 秒未变更则重新发送或标记 failed。

### L 级（轻微）

**L1. `exportModel` 格式校验硬编码**
- 文件：`ExportService.cpp:88-91`
- 问题：`format != "pt" && format != "onnx" && format != "tflite" && format != "engine"` 硬编码格式列表。docs 明确 MVP 只支持 pt + onnx，但代码允许 tflite 和 engine。
- 修复方案：使用常量数组或配置，MVP 阶段限制为 pt + onnx。

**L2. `exportReport` 功能超出 MVP 范围**
- 文件：`ExportService.cpp:426-507`
- 问题：`exportReport` 实现了报告导出功能，但 docs/blueprint.md 和执行总计划未在 MVP 中要求此功能。属于范围蔓延。实现本身是好的（原子写入），但不在 MVP 验收范围。
- 修复方案：保留但标注为非 MVP 功能。

## 五、IPC 模块（IpcClient.cpp + server.py）

### S 级（严重）

**S1. `sendRequest` 无超时机制，pendingCommands 内存泄漏**
- 文件：`e:\z\project\my\LabelTorchV\src\core\ipc\IpcClient.cpp:137-155`
- 问题：`m_pendingCommands[requestId] = command;` 永远不会清理超时请求。如果后端不响应（如 Python 进程卡死但未退出），pendingCommands 会无限增长。CLAUDE.md 明确要求"所有 IPC 请求必须设置超时（默认 30 秒），超时请求从 pendingCommands 移除"。当前实现仅在 response 收到时移除（行 186），无超时清理。
- 修复方案：为每个请求关联 QTimer，30 秒后自动从 pendingCommands 移除并发送超时信号。

### M 级（中等）

**M1. IpcClient 多处 IPC 命令字面量硬编码**
- 文件：`IpcClient.cpp:20`、`123`
- 问题：`sendRequest("environment.check", {})`（行 20）和 `sendRequest("shutdown", {})`（行 123）使用字面量。前者应使用 `IpcProtocol::CMD_ENV_CHECK`，后者 IpcProtocol.h 中未定义 shutdown 常量。
- 修复方案：使用 `IpcProtocol::CMD_ENV_CHECK`；在 IpcProtocol.h 增加 `CMD_SHUTDOWN` 常量。

**M2. `sendRequest` 后端未运行时静默失败**
- 文件：`IpcClient.cpp:139-142`
- 问题：如果 `m_process` 未运行，`sendRequest` 只记录 warning 并返回，不通知上层。调用方（如 TrainingService、ExportService）无法感知请求未发送。
- 修复方案：发送失败信号 `emit backendError` 或返回 bool 让调用方处理。

**M3. watchdog 无法检测后端卡死**
- 文件：`IpcClient.cpp:16-22`
- 问题：watchdog 每 30 秒发送 `environment.check`，但不检查响应。如果 Python 进程还在但事件循环卡死（如训练死循环），watchdog 会持续发送请求但无法检测异常。
- 修复方案：跟踪 environment.check 的响应，超时未响应则标记后端为不可用。

**M4. `onBackendFinished` 自动重启无指数退避**
- 文件：`IpcClient.cpp:208-219`
- 问题：固定 3 秒重试间隔，最多 5 次。如果后端启动失败（如 Python 环境损坏），3 秒重试会快速耗尽 5 次机会，且加重系统负担。
- 修复方案：使用指数退避（3s, 6s, 12s, 24s, 48s）。

**M5. server.py `_handle_shutdown` 不等待正在处理的任务**
- 文件：`e:\z\project\my\LabelTorchV\backend\labeltorch_backend\server.py:155-158`
- 问题：`_handle_shutdown` 直接设置 `self.running = False`，不等待正在执行的训练/导出任务完成。主循环退出后，asyncio 任务被取消，可能导致训练数据损坏。
- 修复方案：等待 `_active_tasks` 中的任务完成或超时后再退出。

### L 级（轻微）

**L1. `processMessage` response 消息 request_id 不在 pendingCommands 时仍 emit**
- 文件：`IpcClient.cpp:178-190`
- 问题：如果 response 的 request_id 不在 m_pendingCommands 中（如已超时被清理），仍然会 `emit responseReceived`，可能导致重复处理。
- 修复方案：可选，记录 warning 即可。

**L2. server.py 主循环超时 30 秒但无队列积压处理**
- 文件：`server.py:100-115`
- 问题：`asyncio.wait_for(queue.get(), timeout=30.0)` 超时后 continue，但如果消息处理慢导致队列积压，无监控机制。
- 修复方案：增加队列长度监控和告警。

## 六、docs 文档中提到但未实现的功能清单

根据 `docs/blueprint.md`、`docs/architecture.md`、`docs/执行总计划-LabelTorchV-MVP+.md`、`docs/里程碑验收与执行清单.md`：

### MVP 范围内未完全实现

1. **数据集扫描与异常提示 - 尺寸异常检测**
   - 文档要求：`docs/执行总计划.md` 9.1 节"数据集导入必须覆盖：异常记录"
   - 现状：`DatasetService::detectAnomalies` 中 `sizeAnomalies` 为占位符（行 659-660），永远返回空列表
   - 影响：无法检测尺寸异常的图片

2. **训练前给出设备建议**
   - 文档要求：`docs/执行总计划.md` 第 10 节"训练前给出设备建议"
   - 现状：`TrainingService` 仅缓存 `m_latestEnvironment`（行 593），无设备建议逻辑
   - 影响：用户无法获得 GPU/CPU 选择建议

3. **显存不足时给出可执行的 batch 建议**
   - 文档要求：`docs/执行总计划.md` 第 10 节"显存不足时给出可执行的 batch 建议"
   - 现状：未实现。UltralyticsAdapter 训练失败时只返回错误信息，无 batch 建议
   - 影响：显存不足时用户无法获得可执行的 batch 大小

4. **训练中断恢复**
   - 文档要求：`docs/里程碑验收与执行清单.md` M3"进程中断修复"
   - 现状：`reconcileStaleRuns`（TrainingService.cpp:621-662）仅将 running/preparing 状态标记为 stopped，无真正的恢复机制（如从 last.pt 恢复训练）
   - 影响：训练中断后无法从断点恢复，需重新开始

5. **IPC 请求超时机制**
   - 文档要求：`CLAUDE.md` 6.4 节"所有 IPC 请求必须设置超时（默认 30 秒），超时请求从 pendingCommands 移除"
   - 现状：`IpcClient::sendRequest` 无超时，pendingCommands 无清理机制
   - 影响：后端卡死时前端永远等待

6. **GPU 不可用时 CPU 降级 - AnomalibAdapter 不完整**
   - 文档要求：`docs/architecture.md` 第 6 节"GPU 不可用时支持 CPU 降级"
   - 现状：`UltralyticsAdapter` 实现了 CUDA 不可用时回退 CPU（行 76-81），但 `AnomalibAdapter` 仅在 `device != "auto"` 时判断（行 147-153），`device="auto"` 时直接使用 `torch.cuda.is_available()`，未处理 CUDA 请求但不可用的情况
   - 影响：异常检测训练时显式请求 GPU 但 CUDA 不可用会直接失败

### MVP 范围外但文档提到的功能（允许预留）

根据 `docs/执行总计划.md` 第 4 节"当前阶段明确不做的事情"：

7. **OBB 完整训练闭环** - 预留（UltralyticsAdapter 支持 yolov8_obb，但 docs 明确不做）
8. **分类完整训练闭环** - 预留（UltralyticsAdapter 支持 yolov8_cls，但 docs 明确不做）
9. **完整标注引擎** - 预留
10. **辅助标注正式版** - 预留
11. **增量训练完整产品化** - 预留
12. **TensorRT/TFLite/Engine 完整导出闭环** - 预留（ExportService 接受这些格式但未验证）
13. **Ubuntu 交付级兼容** - 预留
14. **匹配页面** - 预留（不做）

### 文档未明确但代码中存在的额外功能

15. **`exportReport` 报告导出** - `ExportService.cpp:426-507`，docs 未要求但已实现
16. **`testing` 测试模块** - `TestingService.cpp` + `handlers/testing.py`，docs/blueprint.md 3.6 节提到"测试页"但 MVP+ 执行计划中测试页属于 P1 优先级

## 七、总结

### 问题统计

| 模块 | S 级 | M 级 | L 级 | 合计 |
|------|------|------|------|------|
| 数据集 | 2 | 4 | 2 | 8 |
| 快照 | 0 | 5 | 2 | 7 |
| 训练 | 1 | 6 | 2 | 9 |
| 导出 | 0 | 6 | 2 | 8 |
| IPC | 1 | 5 | 2 | 8 |
| **合计** | **4** | **26** | **10** | **40** |

### 最优先修复项（S 级）

1. **DatasetService 多个导入函数缺事务保护**（S1）- 影响数据一致性
2. **DatasetService 导入失败未回滚 dataset 记录**（S2）- 导致数据库残留孤立记录
3. **TrainingService startTraining 未检查 IPC 连接**（S1）- 导致训练任务卡死
4. **IpcClient sendRequest 无超时机制**（S1）- 违反项目硬约束，内存泄漏

### MVP 闭环可用性评估

当前 MVP 主链路（导入 → 快照 → 训练 → 导出）在**正常路径**下基本可用，但在**异常路径**下存在多个阻断点：
- 导入失败会留下孤立记录，无法通过 UI 清理
- 训练启动时 IPC 断连会导致任务卡死
- 导出时 IPC 断连会导致状态卡死
- 后端卡死时前端无法检测

建议优先修复 S 级问题后再进行端到端测试。

已收集到三份详细审计报告。现在综合所有发现，生成完整的实施计划文件。

计划文件已创建完成。基于三轮深度审计（UI 对齐 / MVP 功能 / 测试稳定性），制定了 4 阶段执行计划：

- **阶段 A**（18 项）：严重 Bug 修复 - 7 个 S 级 + 10 个关键 M 级问题
- **阶段 B**（8 项）：功能完善与 Stub 清理 - 程度模型推理、batch-modify 弹窗、测试补充
- **阶段 C**（4 项）：UI 高保真对齐 - ExportPage 参数面板、类别统计、交互优化
- **阶段 D**（4 项）：稳定性保障 - 异常场景测试、网络调研、长时间稳定性、文档同步

最严重的 3 个问题：
1. **InferenceService IPC 响应处理缺失**（推理结果永远丢失）
2. **IpcClient 无请求超时清理**（内存泄漏）
3. **IpcClient 自动重启永久禁用**（stopBackend 后无法自动重启）

计划已批准，开始执行阶段 A。先创建 TodoList 跟踪进度。

开始执行 A1。先读取相关文件了解现有实现。

读取 IpcClient.cpp 了解响应处理逻辑。

查看 Python 后端 inference handler 了解响应格式。

了解了响应格式。现在实现 A1：修改 `sendRequest` 返回 requestId，并在 InferenceService 中连接响应处理。

先修改 IpcClient.h 让 sendRequest 返回 requestId：

修改 m_pendingCommands 为包含时间戳的结构，支持超时清理：

现在修改 IpcClient.cpp，同时完成 A2（超时清理）、A3（自动重启）、A13（常量替换）：

现在修改 sendRequest 返回 requestId，并适配新的 m_pendingCommands 类型：

现在修改 processMessage 适配新的 m_pendingCommands 类型：

现在在 IpcClient.h 中添加 cleanupTimedOutRequests 私有方法声明：

现在在 IpcProtocol.h 中添加 CMD_SHUTDOWN 和 CMD_INFERENCE_CANCEL 常量：

现在修改 InferenceService.h 添加响应处理：

现在修改 InferenceService.cpp，实现 setIpcClient 连接信号、runInference 记录映射、onResponseReceived 处理响应：

现在修改 runInference 使用常量并记录 requestId → batchId 映射：

现在修改 cancelBatch 使用常量：

现在在文件末尾添加 onResponseReceived 方法实现：

A1/A2/A3/A13 已完成。更新 TodoList 并继续 A4。

开始 A4。先读取 DatasetService.cpp 的导入函数。

查看 updateImportStatus 实现，了解事务包裹策略：

现在为 importLabelMeDataset 添加事务包裹：

现在为 importAnomalyDataset 添加事务包裹：

用更精确的方式分步修改 importAnomalyDataset：

现在修改失败分支添加 rollback：

现在修改 importClassifyFolderDataset 添加事务包裹：

现在修改 importDatasetJson 添加事务包裹：

我需要先探索当前代码库状态，了解之前已完成的工作和剩余任务，然后重新制定计划。

让我并行探索当前代码库状态，确认已完成的工作和剩余任务。

现在我已经充分了解了当前代码库状态。让我创建一个更新的计划文件，反映当前进度并聚焦剩余任务。

计划文件已创建。现在通知用户审阅。

计划已批准，开始执行。先创建 todo list 跟踪进度。

开始执行 A4-4：修复 importDatasetJson 事务包裹。先重新读取该函数完整内容。

现在为 importDatasetJson 添加事务包裹。采用策略：dataset 记录创建保持不在事务内，事务包裹样本插入和状态更新操作。

A4-4 完成。现在标记完成并开始 A5（TrainingService startTraining IPC 状态检查）。

现在修复 M7（ExportService exportModel 未检查 IPC connected()）。

现在修复 A7（artifact_id 回退逻辑）和 A12 中的 handleIpcResponse exec 未检查返回值。

A5/A7/A12 完成。现在标记完成并开始 A6（Tag 系统）。

先添加 dataset_tags 表到 Schema。

现在创建 TagService.h。

现在创建 TagModel.h/cpp。

现在查看 DatasetService 的 CMakeLists.txt 以注册新文件。

现在修改 main.cpp 注入 TagService 和 TagModel。

现在修改 DatasetPage.qml 的 addTagDialog，让 onAccepted 调用 tagService。

现在添加 tagModel 在 currentDatasetId 变化时刷新的逻辑。

A6 完成。现在标记完成并开始 A8/A9（AnomalyDetector + AnomalyService）。

现在修改 AnomalyDetector.cpp 的 loadModel 和添加 isInferable 实现。

A8 完成。现在执行 A9：修复 AnomalyService 模型列表硬编码。

---

**User**

继续

**Assistant**

继续执行 A9。修改 AnomalyService.h。

现在修改 AnomalyService.cpp 实现 listModels、refreshModels 和 fallbackModels。

现在修改 onResponseReceived 处理 anomaly.list_models 响应。

现在添加后端 anomaly.py 的 handle_list_models 处理器。

现在在 server.py 注册 anomaly.list_models 命令。

A9 完成。现在执行 A10/A11（TrainingService 事务回滚 + stopTraining 状态顺序）。

A10 完成。现在执行 A11：修复 TrainingService stopTraining 状态顺序。

A11 完成。现在执行 A14（server.py shutdown 优雅退出）。

现在修改 `_handle_message` 来追踪活跃任务，并修改 `_handle_shutdown` 等待活跃任务。

`prepareSnapshotPhysicalDir` 已覆盖 anomaly 类型（line 624-640），可以安全删除 `prepareAnomalySnapshotDir` 死代码。

A15 完成。继续 A16 - 修复 AnomalibAdapter device=auto CUDA 不可用。

A16 完成。继续 A17 - 修复 DatasetService 主线程耗时操作。

现在实现 L1（sizeAnomalies IQR 检测）和异步版本。先修改 detectAnomalies 实现 L1：

需要添加 `<tuple>` 头文件用于 std::tuple：

现在添加异步版本实现。在 getClassDistribution 函数后添加：

需要添加 QPointer 头文件：

A17 完成。现在执行 A18 - 编译验证 + 测试。

先执行 ruff check 和 Python 测试，再执行 C++ 编译：

ruff check 通过。现在执行 C++ 编译：

PCH 文件版本不匹配，需要清理后重新构建：

需要重新配置 CMake 以重新生成 PCH：

构建命令退出码为 0，看起来已成功。让我验证可执行文件是否存在并运行测试：

exe 文件存在但时间较旧。让我重新构建并查看完整输出：

发现 QML 编译错误。让我查看 ActiveLearningPage.qml:23：

让我检查 QML 编译错误的具体位置。错误在第 23 行第 5 列，但那是一个注释。可能是 `property var` 的 `({})` 语法问题：

让我检查其他 QML 文件是否也使用了 `({})` 语法：

让我修改所有使用 `({})` 语法的文件：

现在重新构建：

让我尝试删除第 23 行的注释，看看是否是注释导致的问题：

让我检查文件编码和 BOM：

错误可能是在第 234 行第 44 列（行号被换行分割）。让我查看：

---

